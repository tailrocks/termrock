schema = 1
recorded_at = "2026-07-16"
release = "v0.6.0"
termrock_implementation_revision = "f4368a3ee018e80e5c50dbcef508d5ed41e1ce67"
jackin_implementation_revision = "27c450e9af7a299171034f98267e0fa26bd3057f"
rust_version_floor = "1.95"
tested_toolchain = "1.97.1"
ratatui_core = "0.1.2"
ratatui_crossterm = "0.1.2"
crossterm = "0.29.0"
canonical_render_platform = "linux"
supported_operating_systems = ["linux", "macos"]

# Current canonical presentation command:
# cargo run -p termrock-catalog -- render --out docs/public/preview-frames
#
# Verification records below are historical evidence tied to their recorded
# revisions. Their commands and results remain unchanged; they are not current
# TermRock preview entry points or parity claims for the catalog migration.

[[verification]]
repository = "tailrocks/termrock"
platform = "linux"
command = "mise x -- cargo fmt --all -- --check && mise x -- cargo clippy --workspace --all-targets --all-features --locked -- -D warnings && mise x -- cargo nextest run --workspace --all-features --locked && mise x -- cargo test --doc --workspace --locked"
result = "pass"

[[verification]]
repository = "tailrocks/termrock"
platform = "linux"
command = "mise x -- cargo hack check --workspace --feature-powerset --all-targets --locked && mise x -- cargo deny check advisories bans licenses sources && mise x -- cargo shear --deny-warnings && mise x -- cargo package -p termrock --locked"
result = "pass"

[[verification]]
repository = "jackin-project/jackin"
platform = "macos"
command = "cargo check --workspace --all-targets --locked at 27c450e9af7a299171034f98267e0fa26bd3057f"
result = "pass against Jackin's pinned compatible TermRock 41482e9f baseline"

[[verification]]
repository = "jackin-project/jackin"
platform = "macos"
command = "temporarily pin TermRock 179745902bd0dbe266cbf54481afeaf0212b1d06, cargo update -p termrock --precise 179745902bd0dbe266cbf54481afeaf0212b1d06, then cargo check --workspace --all-targets --locked at 27c450e9af7a299171034f98267e0fa26bd3057f"
result = "pass; temporary Cargo.toml and Cargo.lock changes restored afterward; Jackin working tree clean"

[[verification]]
repository = "tailrocks/termrock"
platform = "macos"
command = "cargo test -p termrock --features crossterm --lib session::tests --locked && cargo test --workspace --all-features --locked && cargo clippy --workspace --all-targets --all-features --locked -- -D warnings && cargo semver-checks check-release -p termrock --baseline-rev v0.6.0 && cargo deny check advisories bans licenses sources && cargo shear --deny-warnings && gitleaks detect --no-banner --redact --source ."
result = "pass at f4368a3ee018e80e5c50dbcef508d5ed41e1ce67; 224 workspace tests including 7/7 session lifecycle tests; 196/196 semver checks; two distinct cleanup failures prove continued cleanup and earliest-error identity"

[[verification]]
repository = "jackin-project/jackin"
platform = "macos"
command = "consumer evidence inherited from direct all-target compilation against 824783b0b46c47b91418d227047d7fdaf35ddde7"
result = "valid for f4368a3ee018e80e5c50dbcef508d5ed41e1ce67 because the descendant adds only private cfg(test) evidence; its non-test library compilation and public contract are unchanged from the directly compiled implementation revision"

[[verification]]
repository = "jackin-project/jackin"
platform = "macos"
command = "cargo xtask ci at 27c450e9af7a299171034f98267e0fa26bd3057f"
result = "baseline failure unrelated to TermRock: Rust 1.97 unused_self lints in jackin-agent-status plus codex-launch and capsule owned-state tests; 5,370/5,372 workspace tests pass in isolated nextest rerun"

[[verification]]
repository = "tailrocks/termrock"
platform = "macos"
command = "GitHub required rust-required and docs-required workflows"
result = "required compatibility contract; canonical SVG generation remains Linux-only"

[[verification]]
repository = "tailrocks/termrock"
platform = "macos"
command = "cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features --locked -- -D warnings && cargo test --workspace --all-features --locked && cargo test --doc --workspace --locked && cargo test -p termrock --test tree_hot_path -- --nocapture && cargo semver-checks check-release -p termrock --baseline-rev 41482e9fdb3dff96177410f64a5026016d805ae4 && cargo deny check advisories bans licenses sources && cargo shear --deny-warnings && cargo package -p termrock --locked"
result = "pass at 8a6f76233a539ed126cda36ee2205656c5a8adc7; 200 workspace tests, including 11/11 Tree suite tests; warmed 10,000-node Tree rendered 100 times in 40.40 ms with zero allocations or reallocations under the 250 ms debug-profile budget"

[[verification]]
repository = "tailrocks/termrock"
platform = "macos"
command = "cargo fmt --all -- --check && cargo test -p termrock --features crossterm --lib session::tests --locked && cargo test --workspace --all-features --locked && cargo clippy --workspace --all-targets --all-features --locked -- -D warnings && cargo check -p termrock --no-default-features --locked && cargo check --workspace --examples --locked && cargo check --workspace --examples --features crossterm --locked && cargo doc --workspace --all-features --no-deps --locked && cargo semver-checks check-release -p termrock --baseline-rev v0.6.0 && cargo deny check advisories bans licenses sources && cargo shear --deny-warnings && gitleaks detect --no-banner --redact --source ."
result = "pass at 824783b0b46c47b91418d227047d7fdaf35ddde7; 223 workspace tests including 6/6 session lifecycle tests; 196/196 semver checks; exact reverse restoration, partial write and flush rollback, failed-cleanup retry, earliest-error preservation, and idempotence proven"

[[verification]]
repository = "jackin-project/jackin"
platform = "macos"
command = "temporarily pin TermRock 824783b0b46c47b91418d227047d7fdaf35ddde7, cargo update -p termrock --precise 824783b0b46c47b91418d227047d7fdaf35ddde7, then cargo check --workspace --all-targets --locked at 27c450e9af7a299171034f98267e0fa26bd3057f"
result = "pass; temporary Cargo.toml and Cargo.lock changes restored afterward; Jackin working tree clean"
