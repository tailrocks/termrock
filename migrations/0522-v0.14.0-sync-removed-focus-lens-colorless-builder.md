# 0522: Synchronize removed FocusLens colorless builder

## Boundary

The dead-accessor sweep removed the unused `FocusLens::colorless` builder. The
generated public API snapshot retained its signature even though the builder
is no longer available.

## Change

Removed the one stale snapshot entry for `FocusLens::colorless` deleted by
`855e4147`.

## Host migration

Apply reduced-color policy through the host's capability/design-system setup;
use the remaining `FocusLens::mode` builder for mode selection. No
compatibility alias or deprecation period is provided.
