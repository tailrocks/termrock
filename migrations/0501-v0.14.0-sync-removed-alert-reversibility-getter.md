# 0501: Synchronize removed AlertReversibility getter

## Boundary

The getter sweep removed the unused stable-id accessor from
`AlertReversibility`. The generated public API snapshot retained its
signature even though the getter is no longer available.

## Change

Removed the one stale snapshot entry for `AlertReversibility::id` deleted by
`fb4d5f74`.

## Host migration

Use the remaining `AlertReversibility::label` projection for user-facing
descriptions. No compatibility alias or deprecation period is provided.
