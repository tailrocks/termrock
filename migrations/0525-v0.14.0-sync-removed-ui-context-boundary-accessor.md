# 0525: Synchronize removed UiContext boundary accessor

## Boundary

The endgame purge removed the unused `UiContext::boundary` accessor. The
generated public API snapshot retained its signature even though the accessor
is no longer available.

## Change

Removed the one stale snapshot entry for `UiContext::boundary` deleted by
`204e62a4`.

## Host migration

Keep capability-boundary policy at the host or context-construction boundary;
widgets should consume only the supported context projections. No
compatibility alias or deprecation period is provided.
