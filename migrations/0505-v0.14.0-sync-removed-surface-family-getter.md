# 0505: Synchronize removed SurfaceFamily getter

## Boundary

The getter sweep removed the unused stable-id accessor from `SurfaceFamily`.
The generated public API snapshot retained its signature even though the
getter is no longer available.

## Change

Removed the one stale snapshot entry for `SurfaceFamily::id` deleted by
`fb4d5f74`.

## Host migration

Use the remaining `SurfaceFamily::index` projection for table lookup. No
compatibility alias or deprecation period is provided.
