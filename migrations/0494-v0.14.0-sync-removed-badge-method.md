# 0494: Synchronize removed Badge protocol method

## Boundary

The dead-protocol sweep removed the unused `EventResult` wrapper from
`Badge`. The generated public API snapshot retained its signature even though
the method is no longer available.

## Change

Removed the one stale snapshot entry for `Badge::handle_key_result` deleted by
`6faa9fb0`.

## Host migration

Handle badge keyboard outcomes through `Badge::handle_key`. Direct mouse,
intent, and semantic registration APIs remain available. No compatibility alias
or deprecation period is provided.
