# 0515: Synchronize removed ProcessStatus label getter

## Boundary

The dead-accessor sweep removed the unused `ProcessStatus::label` projection.
The generated public API snapshot retained its signature even though the
projection is no longer available.

## Change

Removed the one stale snapshot entry for `ProcessStatus::label` deleted by
`855e4147`.

## Host migration

Use `ProcessStatus::id` or `semantic` for supported process-status
projections. No compatibility alias or deprecation period is provided.
