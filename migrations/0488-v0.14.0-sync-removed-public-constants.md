# 0488: Synchronize removed public constants

## Boundary

The zero-reference constant sweep removed public budgets and one keymap glyph
constant that no code path used. The generated public API snapshot still
advertised those constants.

## Change

Removed the 16 stale constant entries from `docs/api/public-api.txt` for the
definitions deleted by `d1fa9d88` across keymap, layout, patterns, and
widgets.

## Host migration

Remove references to the deleted constants. Use surviving component-specific
budgets or explicit values where the host owns the policy. Sibling constants,
including `ROWS_10K`, remain unchanged. No compatibility aliases or
deprecation period is provided.
