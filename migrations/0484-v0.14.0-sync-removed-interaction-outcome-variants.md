# 0484: Synchronize removed interaction and outcome variants

## Boundary

The interaction cleanup removed enum variants that no code path in the
workspace could construct. The generated public API snapshot still listed
those removed variants and their payload fields, making the snapshot promise
unreachable behavior.

## Change

Synchronized `docs/api/public-api.txt` with the current core source after
`b77f379c` removed dead `UiIntent` and `OverlayRequest` variants and
`b89d7583` removed never-constructed interaction outcome variants.

## Host migration

Remove matches and constructors for the deleted variants. Use the remaining
interaction outcomes and component-specific APIs. Physical editing keys such
as `KeyCode::Delete` are unaffected. No compatibility aliases or deprecation
period is provided.
