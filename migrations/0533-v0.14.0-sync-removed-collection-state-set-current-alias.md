# 0533: Synchronize removed CollectionState set_current alias

## Boundary

The endgame purge removed the unused `CollectionState::set_current` alias.
The generated public API snapshot retained its signature even though the
alias is no longer available.

## Change

Removed the one stale snapshot entry for `CollectionState::set_current`
deleted by `204e62a4`.

## Host migration

Use the canonical `CollectionState::set_active` setter when changing the
active identity. No compatibility alias or deprecation period is provided.
