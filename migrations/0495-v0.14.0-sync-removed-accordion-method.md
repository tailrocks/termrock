# 0495: Synchronize removed Accordion protocol method

## Boundary

The dead-protocol sweep removed the unused `EventResult` wrapper from
`Accordion`. The generated public API snapshot retained its signature even
though the method is no longer available.

## Change

Removed the one stale snapshot entry for `Accordion::handle_key_result` deleted
by `6faa9fb0`.

## Host migration

Handle accordion keyboard outcomes through `Accordion::handle_key`. Direct
mouse and intent handling remain available. No compatibility alias or
deprecation period is provided.
