# 0520: Synchronize removed AnsiSegment plain constructor

## Boundary

The dead-accessor sweep removed the unused `AnsiSegment::plain` constructor.
The generated public API snapshot retained three duplicate signatures even
though the constructor is no longer available.

## Change

Removed all three stale snapshot entries for `AnsiSegment::plain` deleted by
`855e4147`.

## Host migration

Construct segments with their supported public fields (`text`, `style`, and
`href`) as needed. No compatibility alias or deprecation period is provided.
