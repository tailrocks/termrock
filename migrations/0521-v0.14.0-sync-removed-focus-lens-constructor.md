# 0521: Synchronize removed FocusLens constructor

## Boundary

The dead-accessor sweep removed the unused `FocusLens::new` constructor. The
generated public API snapshot retained its signature even though the
constructor is no longer available.

## Change

Removed the one stale snapshot entry for `FocusLens::new` deleted by
`855e4147`.

## Host migration

Construct focus-lens state through the supported focus-graph integration path;
use the remaining `FocusLens::mode` builder for mode selection. No
compatibility alias or deprecation period is provided.
