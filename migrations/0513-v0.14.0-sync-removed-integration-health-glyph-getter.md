# 0513: Synchronize removed IntegrationHealth glyph getter

## Boundary

The dead-accessor sweep removed the unused `IntegrationHealth::glyph`
projection. The generated public API snapshot retained its signature even
though the projection is no longer available.

## Change

Removed the one stale snapshot entry for `IntegrationHealth::glyph` deleted by
`855e4147`.

## Host migration

Use `IntegrationHealth::id` or `semantic` for supported health projections;
the host owns visual glyph selection. No compatibility alias or deprecation
period is provided.
