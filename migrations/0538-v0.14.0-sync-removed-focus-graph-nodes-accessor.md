# 0538: Synchronize removed FocusGraph nodes accessor

## Boundary

The orphan sweep removed the unused `FocusGraph::nodes` accessor. The
generated public API snapshot retained its signature even though the
accessor is no longer available.

## Change

Removed the one stale snapshot entry for `FocusGraph::nodes` deleted by
`b802fc45`.

## Host migration

Keep focus-node registration and traversal inside the focus host boundary;
consume focus outcomes instead of reading graph storage directly. No
compatibility alias or deprecation period is provided.
