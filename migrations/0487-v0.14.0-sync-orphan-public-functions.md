# 0487: Synchronize orphan public function removals

## Boundary

The orphan-function sweep deleted public functions with zero call sites across
the workspace. The generated public API snapshot was not synchronized with
that source cleanup and retained their signatures.

## Change

Removed the 22 stale function entries from `docs/api/public-api.txt` for the
definitions deleted by `263dfb5d` across interaction, layout, patterns,
runtime, style, and widgets.

## Host migration

Remove calls to the deleted helpers. Use the remaining owning component or
layout API. Surviving same-name siblings, including unit-specialized helpers,
are unchanged. No compatibility aliases or deprecation period is provided.
