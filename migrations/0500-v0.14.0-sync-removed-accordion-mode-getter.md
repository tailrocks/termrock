# 0500: Synchronize removed AccordionMode getter

## Boundary

The getter sweep removed the unused stable-id accessor from `AccordionMode`.
The generated public API snapshot retained its signature even though the
getter is no longer available.

## Change

Removed the one stale snapshot entry for `AccordionMode::id` deleted by
`fb4d5f74`.

## Host migration

Use the enum variant directly or the remaining `AccordionRecipe::id` projection
for recipe identifiers. No compatibility alias or deprecation period is
provided.
