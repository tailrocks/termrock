# 0519: Synchronize removed AnsiStream plain projection

## Boundary

The dead-accessor sweep removed the unused `AnsiStream::plain` projection. The
generated public API snapshot retained three duplicate signatures even though
the projection is no longer available.

## Change

Removed all three stale snapshot entries for `AnsiStream::plain` deleted by
`855e4147`.

## Host migration

Use the supported stream line-collection and `pending_plain` APIs when host
code needs parsed or pending text. No compatibility alias or deprecation
period is provided.
