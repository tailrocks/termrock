# 0259 — Neutral panel presets; product example nav seeds in patterns

## Mission

Finish widgets genericity polish:

- Rename `workbench_panels` → `three_pane_panels`, `dashboard_panels` →
  `main_end_panels` (product-neutral layout presets).
- Rename `modes_to_workbench` → `modes_to_ribbon`.
- Move `example_agent_workbench_nav` / `example_database_nav` seed data to
  `termrock::patterns` (lookbook/demo only). Keep generic
  `example_settings_nav` / `example_sectioned_sidebar_nav` on widgets.

## Consumer edits

```rust
// Panel presets
use termrock::widgets::{three_pane_panels, main_end_panels};
// was workbench_panels / dashboard_panels

// Mode bridge
use termrock::widgets::modes_to_ribbon; // was modes_to_workbench

// Nav seeds for demos
use termrock::patterns::{example_agent_workbench_nav, example_database_nav};
```

## Validation

```bash
rtk cargo check -p termrock -p termrock-lookbook
rtk cargo test -p termrock --lib patterns::connection_manager
```
