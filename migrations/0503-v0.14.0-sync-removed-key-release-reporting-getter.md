# 0503: Synchronize removed KeyReleaseReporting getter

## Boundary

The getter sweep removed the unused stable-id accessor from
`KeyReleaseReporting`. The generated public API snapshot retained its
signature even though the getter is no longer available.

## Change

Removed the one stale snapshot entry for `KeyReleaseReporting::id` deleted by
`fb4d5f74`.

## Host migration

Use `KeyReleaseReporting::can_wait_for_release` for behavior checks. No
compatibility alias or deprecation period is provided.
