# 0530: Synchronize removed DismissableLayer rect accessor

## Boundary

The endgame purge removed the unused `DismissableLayer::rect` accessor. The
generated public API snapshot retained its signature even though the
accessor is no longer available.

## Change

Removed the one stale snapshot entry for `DismissableLayer::rect` deleted by
`204e62a4`.

## Host migration

Keep dismissal geometry at the host or layout boundary and update the layer
with `set_rect` when hit testing needs current bounds. No compatibility alias
or deprecation period is provided.
