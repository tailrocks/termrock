# 0508: Synchronize removed SourceCitation mouse handler

## Boundary

The follow-up handler sweep removed the unused mouse-entry point from
`SourceCitation`. The generated public API snapshot retained its signature
even though the handler is no longer available.

## Change

Removed the one stale snapshot entry for `SourceCitation::handle_mouse`
deleted by `855e4147`.

## Host migration

Route citation activation through the remaining supported keyboard/open
behavior and host-level pointer handling as applicable. No compatibility alias
or deprecation period is provided.
