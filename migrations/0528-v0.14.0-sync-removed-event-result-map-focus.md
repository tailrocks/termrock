# 0528: Synchronize removed EventResult map_focus method

## Boundary

The endgame purge removed the unused `EventResult::map_focus` method. The
generated public API snapshot retained its signature even though the method
is no longer available.

## Change

Removed the one stale snapshot entry for `EventResult::map_focus` deleted by
`204e62a4`.

## Host migration

Map domain messages with `EventResult::map` and construct the desired focus
identity at the host boundary. No compatibility alias or deprecation period
is provided.
