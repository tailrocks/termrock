# 0038 — Universal collection intents

## Canonical addition

`UiIntent::Expand` / `Collapse`. Adapters:

- `default_tree_intent` — list map + left/right expand-collapse
- `default_table_intent` — list map without Space toggle
- `TreeState::handle_intent`

Prefer `handle_intent` + keymap over raw key matching in new code.
