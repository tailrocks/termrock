# 0532: Synchronize removed CollectionOutcome changed predicate

## Boundary

The endgame purge removed the unused `CollectionOutcome::changed` predicate.
The generated public API snapshot retained its signature even though the
predicate is no longer available.

## Change

Removed the one stale snapshot entry for `CollectionOutcome::changed` deleted
by `204e62a4`.

## Host migration

Handle each `CollectionOutcome` variant explicitly, or use the narrower
`active_changed` predicate when only active identity changes matter. No
compatibility alias or deprecation period is provided.
