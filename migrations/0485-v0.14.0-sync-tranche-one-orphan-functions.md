# 0485: Synchronize tranche-one orphan function removals

## Boundary

The first orphan-function purge removed helpers with no call sites anywhere in
the workspace. The generated public API snapshot was not regenerated with that
source change and continued to advertise the deleted functions.

## Change

Synchronized `docs/api/public-api.txt` with the 66 public function definitions
deleted by `8925252a`, including the pattern, widget, layout, crossterm,
highlighted-text, and dependency-graph helper removals.

## Host migration

Remove calls to the deleted helpers. Use the owning component or layout API
that remains public. Sibling density/layout APIs and other same-name methods
are unchanged. No compatibility aliases or deprecation period is provided.
