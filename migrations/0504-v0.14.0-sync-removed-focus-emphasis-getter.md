# 0504: Synchronize removed FocusEmphasis getter

## Boundary

The getter sweep removed the unused stable-id accessor from `FocusEmphasis`.
The generated public API snapshot retained its signature even though the
getter is no longer available.

## Change

Removed the one stale snapshot entry for `FocusEmphasis::id` deleted by
`fb4d5f74`.

## Host migration

Use `FocusEmphasis::for_family` or the enum value directly for focus styling.
No compatibility alias or deprecation period is provided.
