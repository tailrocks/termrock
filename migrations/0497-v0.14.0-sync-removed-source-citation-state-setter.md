# 0497: Synchronize removed SourceCitationState setter

## Boundary

The dead-setter sweep removed the unused focus mutator from
`SourceCitationState`. The generated public API snapshot retained its
signature even though the setter is no longer available.

## Change

Removed the one stale snapshot entry for
`SourceCitationState::set_focused` deleted by `6faa9fb0`.

## Host migration

Set `SourceCitationState::focused` directly when constructing or updating the
public state value. No compatibility alias or deprecation period is provided.
