# 0536: Synchronize removed apply_mouse_scroll_u16 helper

## Boundary

The scroll cleanup removed the unused `apply_mouse_scroll_u16` helper. The
generated public API snapshot retained its signature even though the helper
is no longer available.

## Change

Removed the one stale snapshot entry for `scroll::apply_mouse_scroll_u16`
deleted by `6ea9be1c`.

## Host migration

Handle mouse-scroll deltas through the live scroll primitives and host-owned
viewport state. No compatibility alias or deprecation period is provided.
