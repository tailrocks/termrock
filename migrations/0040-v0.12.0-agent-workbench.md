# 0040 — Agent Workbench pattern

## Canonical addition

`patterns::agent_workbench_layout` + `WorkbenchPane` using `Workspace` tree
(TaskRail | Transcript / Prompt / Status). Domain content remains consumer-owned.
