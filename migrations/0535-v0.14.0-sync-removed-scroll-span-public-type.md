# 0535: Synchronize removed ScrollSpan public type

## Boundary

The endgame purge removed the callerless public `ScrollSpan` type. The
generated public API snapshot retained its complete type block even though
the type is no longer available.

## Change

Removed the stale `ScrollSpan` type, constructor, fields, and generated trait
implementation entries deleted by `e02d206c`.

## Host migration

Use the live scroll geometry types and host-owned viewport measurements when
calculating scroll state. No compatibility alias or deprecation period is
provided.
