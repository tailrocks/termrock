# 0506: Synchronize removed RecipeFamily getter

## Boundary

The getter sweep removed the unused stable-id accessor from `RecipeFamily`.
The generated public API snapshot retained its signature even though the
getter is no longer available.

## Change

Removed the one stale snapshot entry for `RecipeFamily::id` deleted by
`fb4d5f74`.

## Host migration

Use the enum value or `RecipeFamily::ALL` for recipe-family selection. No
compatibility alias or deprecation period is provided.
