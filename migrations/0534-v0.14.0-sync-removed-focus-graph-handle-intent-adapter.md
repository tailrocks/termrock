# 0534: Synchronize removed FocusGraph handle_intent adapter

## Boundary

The endgame purge removed the unused `FocusGraph::handle_intent` adapter.
The generated public API snapshot retained its signature even though the
adapter is no longer available.

## Change

Removed the one stale snapshot entry for `FocusGraph::handle_intent` deleted
by `204e62a4`.

## Host migration

Translate host input to `KeyEvent` and use the canonical `FocusGraph::handle_key`
boundary for focus navigation. No compatibility alias or deprecation period
is provided.
