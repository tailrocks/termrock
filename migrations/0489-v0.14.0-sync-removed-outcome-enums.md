# 0489: Synchronize removed outcome enum blocks

## Boundary

The widget cleanup removed three never-constructed outcome wrapper enums and
their re-exports. The generated public API snapshot retained their full enum
and trait-implementation blocks even though no value could be produced.

## Change

Removed the 161 stale snapshot lines for `CalloutOutcome`, `DataViewOutcome`,
and `StreamingMarkdownOutcome` deleted by `416aa87c`.

## Host migration

Remove references to the deleted wrapper enums. Use the inner component
outcome types or plain state values exposed by the current widget APIs. Other
`Outcome` types are unchanged. No compatibility aliases or deprecation period
is provided.
