# 0537: Synchronize removed FocusGraph is_focused accessor

## Boundary

The orphan sweep removed the unused `FocusGraph::is_focused` accessor. The
generated public API snapshot retained its signature even though the
accessor is no longer available.

## Change

Removed the one stale snapshot entry for `FocusGraph::is_focused` deleted by
`b802fc45`.

## Host migration

Track focus transitions through the live focus outcomes and navigation
boundary rather than querying graph internals. No compatibility alias or
deprecation period is provided.
