# 0507: Synchronize removed CitationSourceType letter getter

## Boundary

The follow-up getter sweep removed the unused ASCII-letter projection from
`CitationSourceType`. The generated public API snapshot retained its
signature even though the getter is no longer available.

## Change

Removed the one stale snapshot entry for `CitationSourceType::letter` deleted
by `855e4147`.

## Host migration

Use the remaining `CitationSourceType::glyph` or `infer` APIs as applicable to
the host surface. No compatibility alias or deprecation period is provided.
