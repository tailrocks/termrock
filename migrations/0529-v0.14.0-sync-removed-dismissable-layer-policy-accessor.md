# 0529: Synchronize removed DismissableLayer policy accessor

## Boundary

The endgame purge removed the unused `DismissableLayer::policy` accessor.
The generated public API snapshot retained its signature even though the
accessor is no longer available.

## Change

Removed the one stale snapshot entry for `DismissableLayer::policy` deleted
by `204e62a4`.

## Host migration

Configure dismissal policy through construction or `set_policy` at the host
boundary. No compatibility alias or deprecation period is provided.
