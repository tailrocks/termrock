# 0527: Synchronize removed UiContext keymap accessor

## Boundary

The endgame purge removed the unused `UiContext::keymap` accessor. The
generated public API snapshot retained its signature even though the accessor
is no longer available.

## Change

Removed the one stale snapshot entry for `UiContext::keymap` deleted by
`204e62a4`.

## Host migration

Keep semantic key mapping at the host boundary and pass only supported input
state into context operations. No compatibility alias or deprecation period
is provided.
