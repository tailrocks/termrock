# 0496: Synchronize removed CollapsibleState protocol method

## Boundary

The dead-protocol sweep removed the unused `EventResult` wrapper from
`CollapsibleState`. The generated public API snapshot retained its signature
even though the method is no longer available.

## Change

Removed the one stale snapshot entry for
`CollapsibleState::handle_key_result` deleted by `6faa9fb0`.

## Host migration

Handle collapsible keyboard outcomes through `CollapsibleState::handle_key`.
Direct intent and mouse handling remain available. No compatibility alias or
deprecation period is provided.
