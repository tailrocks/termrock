# 0511: Synchronize removed ConnectionKind glyph getter

## Boundary

The dead-accessor sweep removed the unused `ConnectionKind::glyph` projection.
The generated public API snapshot retained its signature even though the
projection is no longer available.

## Change

Removed the one stale snapshot entry for `ConnectionKind::glyph` deleted by
`855e4147`.

## Host migration

Use `ConnectionKind::id` for the stable connection-kind identity and let the
host choose its visual representation. No compatibility alias or deprecation
period is provided.
