# 0526: Synchronize removed UiContext capabilities accessor

## Boundary

The endgame purge removed the unused `UiContext::capabilities` accessor. The
generated public API snapshot retained its signature even though the accessor
is no longer available.

## Change

Removed the one stale snapshot entry for `UiContext::capabilities` deleted by
`204e62a4`.

## Host migration

Resolve terminal capabilities in the host and pass supported policy into
context construction; widgets should not read the removed accessor. No
compatibility alias or deprecation period is provided.
