# 0524: Synchronize removed ContextUiIntent string projection

## Boundary

The endgame purge removed the orphan `UiIntent::as_str` projection, which is
exposed publicly through the `ContextUiIntent` re-export. The generated public
API snapshot retained two duplicate signatures even though the projection is
no longer available.

## Change

Removed both stale snapshot entries for `ContextUiIntent::as_str` deleted by
`204e62a4`.

## Host migration

Match on the semantic intent variants at the host boundary and provide any
display labels there. No compatibility alias or deprecation period is
provided.
