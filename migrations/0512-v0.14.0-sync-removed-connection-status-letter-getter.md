# 0512: Synchronize removed ConnectionStatus letter getter

## Boundary

The dead-accessor sweep removed the unused `ConnectionStatus::letter`
projection. The generated public API snapshot retained its signature even
though the projection is no longer available.

## Change

Removed the one stale snapshot entry for `ConnectionStatus::letter` deleted by
`855e4147`.

## Host migration

Use `ConnectionStatus::label`, `glyph`, or `semantic` for the supported
connection-status projections. No compatibility alias or deprecation period
is provided.
