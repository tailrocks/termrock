# 0042 — Capability-aware preview host

## Canonical addition

`CapabilityPreviewHost` projects `DesignSystem` themes through `ColorCapability`
quantization for deterministic lookbook/previews (truecolor → 256 → ansi → mono).
