# 0486: Synchronize tranche-two orphan getter and method removals

## Boundary

The second orphan purge removed getters, builders, and methods with no call
sites anywhere in the workspace. The generated public API snapshot was not
updated with that source change and continued to advertise deleted functions.

## Change

Synchronized `docs/api/public-api.txt` with the 96 stale function entries
left by `9f56818b`, including duplicate snapshot entries for
`AnsiStream::options` and the removed registry helpers.

Public fields that remain part of the current contract are preserved; this
change removes function entries only.

## Host migration

Remove calls to the deleted getters, builders, and methods. Use the remaining
state or component APIs. No compatibility aliases or deprecation period is
provided.
