# 0523: Synchronize removed SelectionModel kind getter

## Boundary

The dead-accessor sweep removed the unused `SelectionModel::kind` getter. The
generated public API snapshot retained its signature even though the getter
is no longer available.

## Change

Removed the one stale snapshot entry for `SelectionModel::kind` deleted by
`855e4147`.

## Host migration

Use the selection model's supported count and selected-identity APIs; retain
selection-kind state at the host boundary when needed. No compatibility alias
or deprecation period is provided.
