# 0055 — Source-owned registry CLI spike (Plan 047)

## Addition

Workspace member `termrock-cli` binary `termrock`:

- `termrock plan <entry-dir>`
- `termrock add <entry-dir> [--force]`
- `termrock diff <entry-dir>`
- `termrock check <entry-dir>`

Offline-only core resolver. Path escape rejected. Dirty destinations refuse silent overwrite.

## Fixture

`registry/fixtures/demo-block/` — sample `entry.json` + source.

## Ownership

Kernel crate unchanged as capability dependency. Registry copies source into the app tree; manifest `termrock.lock.json`.

## Validation

```bash
cargo test -p termrock-cli
cargo run -p termrock-cli -- check registry/fixtures/demo-block
```

## Security

- Dest paths resolved under **canonical** workspace root.
- Intermediate **symlink components** that escape the root are refused (tested).
- `--force` writes a sibling `*.termrock.bak` before overwrite.

## Fixtures

- `registry/fixtures/demo-block`
- `registry/fixtures/tiny-component`
