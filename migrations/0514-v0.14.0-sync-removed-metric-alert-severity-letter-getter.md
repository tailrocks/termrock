# 0514: Synchronize removed MetricAlertSeverity letter getter

## Boundary

The dead-accessor sweep removed the unused `MetricAlertSeverity::letter`
projection. The generated public API snapshot retained its signature even
though the projection is no longer available.

## Change

Removed the one stale snapshot entry for `MetricAlertSeverity::letter` deleted
by `855e4147`.

## Host migration

Use `MetricAlertSeverity::id` or `semantic` for the supported severity
projections. No compatibility alias or deprecation period is provided.
