# 0030 — Theme system completion + image surface + pattern recipes

## Removed surface

None. Additive only.

## Canonical addition

### Style

- `ColorCapability`, `quantize_color`, `quantize_theme`, `Theme::quantized`, `rgb_to_xterm256`
- `Appearance`, `AppearanceThemeMap`, `theme_for_appearance`, `Appearance::detect`

### Widgets

- `ThemePicker`, `ThemePickerState`, `ThemePreset`, `BUILTIN_THEME_PRESETS`, `theme_from_preset_id`
- `ImageSurface`, `ImageMeta`, `ImageProtocol`, `protocol_emission_hint`

### Patterns

- `layout_ops_dashboard` / `OpsDashboardLayout` / `OpsDashboardSlots`
- `layout_resource_browser` / `ResourceBrowserLayout` / `ResourceBrowserSlots`

## Ownership

TermRock owns quantization math, appearance detection hooks, picker chrome, and
placeholder image frames. Callers own graphics stream emission (Kitty/Sixel),
pixel buffers, and which theme mapping to persist.

## Validation

```bash
cargo test -p termrock
cargo run -p termrock-lookbook -- check --dir docs/public/component-previews
```
