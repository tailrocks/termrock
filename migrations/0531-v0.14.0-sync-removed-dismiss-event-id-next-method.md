# 0531: Synchronize removed DismissEventId next method

## Boundary

The endgame purge removed the unused `DismissEventId::next` method. The
generated public API snapshot retained its signature even though the method
is no longer available.

## Change

Removed the one stale snapshot entry for `DismissEventId::next` deleted by
`204e62a4`.

## Host migration

Allocate dismissal event identities at the host boundary and pass the
current `DismissEventId` into dismissal operations. No compatibility alias
or deprecation period is provided.
