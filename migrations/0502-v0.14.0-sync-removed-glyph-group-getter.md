# 0502: Synchronize removed GlyphGroup getter

## Boundary

The getter sweep removed the unused stable-id accessor from `GlyphGroup`. The
generated public API snapshot retained its signature even though the getter is
no longer available.

## Change

Removed the one stale snapshot entry for `GlyphGroup::id` deleted by
`fb4d5f74`.

## Host migration

Use the group enum directly or the remaining glyph catalog projections for
display. No compatibility alias or deprecation period is provided.
