# Plan 003: Stop trusting stale generated artifacts — enforce public-api.txt freshness, retire dead fixtures, refresh stale baselines

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md`.
>
> **Drift check (run first)**: `git diff --stat da54a03..HEAD -- docs/api/ docs/scripts/ .github/workflows/docs.yml fixtures/ dependency-tree.txt performance-baseline.md`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.

## Status

- **Priority**: P2
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none (independent of Plans 001/002; touches docs.yml which 002 also touches — coordinate if run concurrently)
- **Category**: dx
- **Planned at**: commit `da54a03`, 2026-07-16

## Why this matters

Three committed artifacts present themselves as authoritative but nothing keeps them true:

1. `docs/api/public-api.txt` (509 KB, generated by cargo-public-api) is the *source of truth* for the catalog gate — `docs/scripts/check-catalog.ts` derives the public-widget inventory from it and then verifies every widget has a story, contract, and preview. But no CI step regenerates or diffs it. Add a widget without regenerating, and the gate silently never learns the widget exists — defeating the AGENTS.md guarantee "Every public widget must be represented by the catalog's generated API inventory…".
2. `dependency-tree.txt` says `termrock v0.7.0` at a foreign path (`/Users/donbeave/Projects/jackin-project/jackin/.termrock/...`) and lists a `similar v2.7.0` dependency that exists in neither `Cargo.toml` nor `Cargo.lock`. Wrong version, wrong repo, phantom dep.
3. `fixtures/renders/` (12 SVGs + `manifest.json`) is referenced by zero code, tests, or CI (verified by grep across `*.rs`, `*.yml`, `*.toml`, `*.json`, `*.ts`). The live golden set is `docs/public/component-previews/`, checked by `docs.yml`. The dead set will drift and mislead.
4. `performance-baseline.md` measures the "v0.6.0 candidate" and sets budgets "for the `0.6.x` line" while the workspace is 0.8.0 with widgets (Form, SplitPane, Tree) added since.

## Current state

- `docs/scripts/check-catalog.ts:11-16` — how the gate consumes the committed file:

```ts
const api = readFileSync(join(root, 'docs', 'api', 'public-api.txt'), 'utf8')
const publicComponents = new Set<string>()
for (const line of api.split('\n')) {
  const match = line.match(/^impl.*ratatui_core::widgets::(?:widget::Widget|stateful_widget::StatefulWidget) for &?termrock::widgets::([A-Z][A-Za-z0-9_]*)/)
  if (match) publicComponents.add(match[1])
}
```

- `.github/workflows/docs.yml` steps (no regeneration of public-api.txt anywhere):

```yaml
      - working-directory: docs
        run: bun run types:check
      - working-directory: docs
        run: bun run build
      - run: cargo run -p termrock-lookbook -- check --dir docs/public/component-previews
      - run: rm -rf target/render-a target/render-b
      - run: cargo run -p termrock-lookbook -- render --out target/render-a >/dev/null
      - run: cargo run -p termrock-lookbook -- render --out target/render-b >/dev/null
      - run: diff -r target/render-a target/render-b
```

- `mise.toml` pins `"cargo:cargo-public-api" = "0.52.0"` (never invoked in CI).
- `dependency-tree.txt:1` — `termrock v0.7.0 (/Users/donbeave/Projects/jackin-project/jackin/.termrock/crates/termrock)`.
- `.github/workflows/hygiene.yml` already runs `cargo tree --workspace --locked` live weekly — the committed file adds nothing that isn't stale.
- `fixtures/renders/` contents: `action-bar-basic.svg`, `backdrop-basic.svg`, `detail-table-basic.svg`, `dialog-message.svg`, `diff-basic.svg`, `hint-bar-wrapped.svg`, `list-selection.svg`, `manifest.json`, `panel-focused.svg`, `status-bar-basic.svg`, + 2 more.
- `performance-baseline.md:3` — "Measured on 2026-07-15 … v0.6.0 candidate"; `:15` — "Budgets for the `0.6.x` line". Workspace `Cargo.toml:6` — `version = "0.8.0"`.
- `crates/termrock/COMPONENTS.md` records a fresher hot-path baseline (2026-07-16, 45.09 ms / 250 ms budget) — that one is current; do not touch it.
- Repo conventions: Conventional Commits + DCO (`git commit -s`), work directly on `main`.

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Regenerate API report | `mise x -- cargo public-api -p termrock --all-features > docs/api/public-api.txt` (see Step 1 for exact flags) | file regenerated |
| Catalog gate | `cd docs && bun install --frozen-lockfile && bun run build` | exit 0 |
| Full tree | `cargo tree --workspace --locked` | exit 0 |
| Tests | `cargo test --workspace --all-features --locked` | all pass |

## Scope

**In scope**:
- `.github/workflows/docs.yml`
- `docs/api/public-api.txt` (regenerated, not hand-edited)
- `dependency-tree.txt` (delete)
- `fixtures/renders/` (delete directory)
- `performance-baseline.md` (annotate; see Step 4)
- `PUBLISH-AUDIT.md` only if it references `dependency-tree.txt` or `fixtures/` (check with grep; update the reference lines only)

**Out of scope**:
- `docs/scripts/check-catalog.ts` logic — the gate is correct once its input is fresh.
- `compatibility.toml` — append-only log; do not edit.
- `docs/public/component-previews/` — this is the live golden set; untouched.
- Re-measuring performance numbers (requires controlled hardware; Step 4 only annotates staleness).

## Git workflow

- Directly on `main`; Conventional Commits + sign-off, e.g. `git commit -s -m "ci(docs): fail when public-api.txt is stale"`.

## Steps

### Step 1: Determine the exact regeneration command and regenerate

First discover how `public-api.txt` was generated: run `mise x -- cargo public-api -p termrock --all-features | head -5` and compare its format to `head -5 docs/api/public-api.txt`. Adjust flags until the output format matches the committed file (candidate variations: `--simplified`, omitting `--all-features`). Then regenerate the file in place.

**Verify**: `git diff --stat docs/api/public-api.txt` — if the diff is *empty*, the file was already fresh (good). If the diff is non-empty, the file was stale: inspect that the changes are plausible API additions/removals, then run `cd docs && bun run build` → exit 0. If `bun run build` now FAILS (a widget lacks a story/contract), STOP and report — that is the exact silent failure this plan exists to expose.

### Step 2: Add a freshness gate to docs.yml

Add to `.github/workflows/docs.yml` before the `bun run build` step (rust toolchain is already installed by the existing `mise install rust` step):

```yaml
      - run: |
          mise x -- cargo public-api -p termrock --all-features > /tmp/public-api-fresh.txt
          diff -u docs/api/public-api.txt /tmp/public-api-fresh.txt
```

(Use the exact flags discovered in Step 1. Also ensure `cargo-public-api` is installed in that workflow — if the workflow only runs `mise install rust` and `mise install bun`, change to `mise install` or add `mise install cargo:cargo-public-api`.)

**Verify**: `mise x -- actionlint` → exit 0. Run the two commands locally → diff exits 0.

### Step 3: Delete the dead artifacts

```
git rm dependency-tree.txt
git rm -r fixtures/renders
```

Then check for dangling references: `grep -rn "dependency-tree\|fixtures/renders" --include="*.md" --include="*.toml" --include="*.yml" --include="*.ts" . | grep -v node_modules | grep -v plans/` — update any hit (expected: possibly `PUBLISH-AUDIT.md`; `provenance.toml` mentions retained paths — if `fixtures/` appears there, STOP: provenance is an immutable record, instead keep the directory and add a `fixtures/README.md` marking it historical).

**Verify**: grep above returns no hits outside `plans/` and `provenance.toml`-style historical records; `cargo test --workspace --all-features --locked` → all pass (nothing consumed those files).

### Step 4: Annotate the stale performance baseline

Add one paragraph at the top of `performance-baseline.md` (below the title):

```markdown
> **Staleness note (2026-07-16):** the measurements below predate the
> v0.7/v0.8 widget additions (Form, SplitPane, Tree) and describe the
> v0.6.0 candidate. The current enforced hot-path budget lives in
> `crates/termrock/COMPONENTS.md` (tree_hot_path, 250 ms batch budget).
> Re-measure on equivalent hardware before citing these numbers for the
> 0.8.x line.
```

**Verify**: `head -10 performance-baseline.md` shows the note.

## Test plan

No new Rust tests. Gates: `cd docs && bun run build` exit 0; `actionlint` exit 0; full workspace test suite still green after deletions.

## Done criteria

- [ ] `docs.yml` contains a `cargo public-api` + `diff` step
- [ ] `git diff docs/api/public-api.txt` empty after regeneration (fresh) OR the regenerated file is committed and `bun run build` passes
- [ ] `dependency-tree.txt` deleted; no dangling references
- [ ] `fixtures/renders/` deleted (or marked historical per Step 3's provenance escape)
- [ ] `performance-baseline.md` carries the staleness note
- [ ] `cargo test --workspace --all-features --locked` → all pass
- [ ] `cd docs && bun install --frozen-lockfile && bun run build` → exit 0
- [ ] `plans/README.md` status row updated

## STOP conditions

- Step 1's regenerated file makes `bun run build` fail → report the missing story/contract names; adding stories is Plan-outside work (it belongs with whoever adds the widget).
- You cannot reproduce the committed `public-api.txt` format with any flag combination → report the closest command and the diff; do not commit a format-changed file without noting the gate regex in `check-catalog.ts:14` must still match (`impl ... for &termrock::widgets::X` lines).
- `provenance.toml` lists `fixtures/` as a retained import path → keep the directory, add the historical README instead of deleting.

## Maintenance notes

- Whoever changes the public API must regenerate `docs/api/public-api.txt` in the same commit — that's now enforced, and the error message contributors see is the diff itself.
- Plans 008–014 change the public API heavily; each will trip this gate until they regenerate the file. That is intended behavior, not a defect in this plan.
- If cargo-public-api's output format changes on a version bump, the catalog regex in `check-catalog.ts:14` must be revalidated.
