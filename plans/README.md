# Implementation Plans — Component Visual Richness series

Generated by the improve skill on 2026-08-12 at commit `539e7d03` (deep,
scoped by explicit request to
[`docs/design/component-visual-richness-plan.md`](../docs/design/component-visual-richness-plan.md)
— every section of that design doc maps to a plan below; no separate audit
fan-out was run because the design doc is itself the vetted audit product,
and every excerpt in these plans was re-verified against live code at
`539e7d03`).

Execute in the order below. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Branch / refactoring policy (operator instruction + repo law)

- **One branch:** repo law (`AGENTS.md`) mandates all work directly on
  `main` with no feature branches — the requested "single branch" IS
  `main`. Execute plans 001→010 sequentially on `main`.
- **Full refactoring, forward-only:** no compatibility shims, no deprecated
  aliases, no dual paths (repo law). Old shapes are deleted in the same
  commit their replacement lands. Every visible default change ships its
  numbered `migrations/` file + `MIGRATING.md` entry in the same commit.
- Each plan = one or more Conventional Commits with DCO (`git commit -s`);
  commit only on green `mise run check`; push only on green `mise run gate`.
- Migration numbers are allocated at execution time (`ls migrations | tail -1`
  + 1); the numbers named in plans (0261–0270) assume strict plan order.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fill the surface ladder and expand the semantic role system | P1 | L | — | TODO |
| 002 | Border shape theme token (`BorderShape`) | P2 | M | 001 | TODO |
| 003 | Activate the spacing system (padding, gaps, dialog rhythm) | P1 | L | 001 | TODO |
| 004 | Enforce recipes — chips, badges, tinted selection, meters | P1 | L | 001, 003 | TODO |
| 005 | Overlay elevation — dialogs, backdrop, status band, toast | P1 | M | 001, 003 | TODO |
| 006 | Motion, text-effect, and glyph-catalog kit | P2 | M | 001 | TODO |
| 007 | Composition primitives — FieldRow, AccentRail, TreeList, PanelStack, HintBar v2 | P2 | L | 001, 003, 006 | TODO |
| 008 | Agent-surface wave — actor accents, rails, presence motion | P2 | L | 001, 004–007 | TODO |
| 009 | Data + overlay wave — tables, menus, palette, diff, forms | P2 | L | 001, 003–007 | TODO |
| 010 | Cascade sweep, consistency audit, policy enforcement | P3 | L | 001–009 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 is the foundation**: every later plan consumes the filled surface
  ladder and/or the new roles. Nothing else may start first.
- 003 before 004/005: fills painted by 004/005 must cover the padded
  geometry 003 introduces (avoids double test churn).
- 006 before 007: `AccentRail` animates via the motion kit; 007's stories
  need deterministic ticks.
- 007 before 008/009: agent and form waves adopt `FieldRow`/`AccentRail`.
- 002 is loosely coupled (only ordering constraint: after 001 to avoid
  `tokens.rs` merge churn). It may run any time before 010.
- 010 last: it re-derives sweep lists from live greps and adds the policy
  tests that lock the series' end state.

## Design doc → plan coverage map

| Design-doc section | Plan(s) |
|---|---|
| §4.1 surface ladder + fg pairing + `terminal_native` | 001 |
| §4.2 de-collapsed accents, tints, new roles, actor slots | 001 (roles) / 008 (consumption) |
| §4.3 border shape token | 002 |
| §4.4 spacing, dialog rhythm, reference-width dialogs, density collapse | 003 |
| §5 chrome unification, recipe enforcement, overlay elevation, status band | 004, 005, 010 |
| §6 new primitives table | 006, 007 |
| §7 shadcn→TUI matrix | expressed as constraints inside 001, 003, 004, 009 |
| §8 widget waves 1–4 | 004/005 (wave 1), 008 (wave 2), 009 (wave 3), 010 (wave 4) |
| §9 consumer validation (Grok parity stories) | 008 step 6; Jackin adoption is consumer-side (out of repo scope, noted in 010) |
| §10 success criteria | done-criteria greps in 004/005/010 + policy tests in 010 |

## Findings considered and rejected

- **Rounded borders as the new default** — rejected: phosphor identity law
  (square caps) and Jackin brand law; shipped as opt-in token instead
  (plan 002).
- **Solid-color dimmed backdrop** — rejected: `DIALOG_BACKDROP = Color::Reset`
  policy is deliberate (documented in `style/mod.rs:58-63`) so overlays
  inherit the operator's terminal theme; dimming ships as a glyph wash over
  Reset bg (plan 005).
- **Changing `StackSpec::default()` gap to density values** — rejected:
  bare `Stack` is a low-level primitive used outside design-system contexts;
  density flows in via `from_system`/`with_spacing` adoption instead
  (plan 003 step 4).
- **`BrandPill` primitive** — deferred: brand marks stay product-side per
  the design doc's boundary check; revisit only on a second consumer's
  evidence.
- **CI-level grep gate for `bg = None`** — deferred in favor of in-repo
  policy tests (plan 010 step 4); promote to CI later if drift recurs.
