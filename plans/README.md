# Implementation Plans

Generated by the improve skill (deep audit) on 2026-07-16, at commit `da54a03`.
Execute in the order below unless the dependency column says otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your row when done.

Repo ground rules that apply to EVERY plan (from AGENTS.md): work directly on
`main` (no branches/PRs), Conventional Commits with DCO sign-off
(`git commit -s`), every breaking public change adds the next sequential
`migrations/` file + `MIGRATING.md` row in the same commit, and forward-only
design — no deprecated aliases or compatibility facades, ever. Migration file
numbers are claimed at execution time in plan order (0003 goes to the first
breaking plan that lands, and so on).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification gate baseline (mise tasks, nextest/powerset/MSRV/REUSE in CI) | P1 | M | — | DONE |
| 002 | CI hardening: full cargo-deny on push, gitleaks, actionlint, rust-cache | P1 | S | 001 | DONE |
| 003 | Generated-artifact freshness (public-api.txt gate; delete dead fixtures/dep-tree) | P2 | S | — | DONE |
| 004 | OSC escape hardening (hyperlink/clipboard encoders) | P1 | S | — | DONE |
| 005 | Input correctness: KeyCode::Unknown, release guards | P1 | M | — | DONE |
| 006 | Key vocabulary unification (LogicalKey/Mods → KeyCode/KeyModifiers) | P1 | M | 005 | DONE |
| 007 | Characterization tests: Form + DetailTable | P1 | M | — | DONE |
| 008 | Theme constructor + full role threading (6 bypassing widgets) | P1 | M | — | DONE |
| 009 | Brand-constant purge (RAIN_*/MENU_*/jk-brand out of public API) | P1 | M | 008 | DONE |
| 010 | Second theme preset (slate) + dual-theme canary | P2 | S | 008, 009 | DONE |
| 011 | Event-model convergence (neutral Event/MouseEvent, state-owned handlers, outcome consolidation) | P1 | L | 005, 006, 007 | DONE |
| 012 | Module structure & surface trim (geometry split, single export paths, PointerShape merge) | P2 | M-L | 009, 011 | DONE |
| 013 | Construction idiom, non_exhaustive, owned Widget impls, serde feature | P2 | M | 008, 011 | TODO |
| 014 | missing_docs enforcement, doctests, real showcase example | P2 | M | 011, 013 | TODO |
| 015 | Viewport/dialog slice-before-clone (per-frame O(viewport)) | P2 | M | — | TODO |
| 016 | Measurement caching + visible-window paint loops | P2 | M | 015 | TODO |
| 017 | Core-math test coverage (geometry/ANSI/focus/diff) + 2 small fixes | P2 | M | — | TODO |
| 018 | SPIKE: runtime disposition + app-runner design | P3 | M-L | 011 | TODO |
| 019 | SPIKE: runtime-configurable keymaps | P3 | M | 006 | TODO |
| 020 | SPIKE: lookbook → storybook (knobs, theme switcher, installable) | P3 | M | 010, 018 | TODO |
| 021 | SPIKE: Picker / command-palette composition | P3 | M | 011 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first**: every other plan's done-criteria reference `mise run gate`; 002 edits the same workflow file.
- **007 before 011**: the characterization suites are the only behavioral net under the event-model rewrite.
- **005 → 006 → 011**: `Unknown` variant, then one vocabulary, then the converged contract — each builds on the previous enum shape.
- **008 → 009 → 010**: constructible theme, then purge the constants the theme replaced, then the second preset that proves it.
- **011 before 012/013/014**: method names, construction idioms, and docs/examples should be written once, against the final API.
- **015 before 016**: slice-before-clone establishes the O(viewport) baseline the caching plan measures against.
- **018 before 020**: the storybook builds on the runner-cleaned lookbook loop.
- Plans 003/004/007/015/017 are independent and can run in parallel with the main chains.
- Concurrency caution: 001+002 (same workflow file), 008+015 (both touch viewport.rs), 012+017 (017 tests functions 012 moves — 017 adapts via its drift check).

## Additional findings recorded during planning

- **F-A**: `crates/termrock/src/scroll/render.rs` `render_scrollable_block_at` hardcodes `let theme = crate::Theme::default();` and `crate::theme::GREEN` inside a render helper — a theming bypass outside Plan 008's widget scope. Needs a theme parameter (breaking) — fold into whichever of 012/016 touches that function's signature first, or a small follow-up after 008.
- Executors of Plans 007/017 may add `// CHARACTERIZATION: BUG` pins — list them here as they appear.

## Findings considered and rejected (do not re-audit)

- **Lookbook duplicating library modules (keymap/geometry/ansi_text)**: false — those files are `crates/termrock/src/`, lookbook imports the library properly.
- **tui-scrollbar abandonment risk**: false — it's part of the ratatui maintainers' tui-widgets suite, version-tracked with ratatui-core.
- **README git-dependency snippet broken**: false — verified correct.
- **Migration docs drift**: false — every symbol in migration 0002 verified present in current code.
- **ratatui-core vs umbrella-ratatui duplicate dependency paths**: false — single ratatui-core 0.1.2 in the graph; only a warn-level transitive hashbrown 0.16/0.17 dupe (not actionable here).
- **Dependency advisories**: `cargo audit` + `cargo deny check advisories` clean at audit time (2026-07-16).
- **Per-frame `format!` in small chrome widgets (tabs/action_bar/panel/hint_bar)**: micro-optimization, N is a handful — folded into Plan 016's scratch-buffer work only where those files are touched anyway; not worth standalone plans.
- **StatusBar `placements()` re-sort caching**: single row, small-N; caching requires content hashing that costs more than the sort — rejected (noted in Plan 016).
- **Lookbook `run_terminal` god-function refactor as standalone**: superseded — Plan 018's runner prototype rewrites it as a side effect.
- **shadcn-style registry/copy-and-adapt distribution spike**: premature per AGENTS.md (crate is today's distribution unit; future mechanism must not constrain it) — revisit after the API stabilizes post-011/013.
- **`Tabs` keyboard handling as its own plan (audit finding D5)**: absorbed — Plan 011 adds `DetailTable::handle_key` and establishes the contract; a `TabsState::handle_key` follows the same recipe and can ride any post-011 change; not selected for a standalone plan this round.
- **compatibility.toml header inconsistency (release=v0.6.0, recorded 2026-07-16)**: likely intentional first-consumer pin semantics; Plan 003 annotates the perf baseline instead; the append-only log itself is untouchable by convention.
- **crates.io publication, Windows support, fuzz/mutation/benchmarks, shared testing helpers**: all explicitly deferred/rejected by TODO.md, README, and PUBLISH-AUDIT.md — honored, not re-proposed.
