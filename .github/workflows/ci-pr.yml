# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / PR
run-name: CI / PR · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  pull_request:
  workflow_dispatch:
    inputs:
      runner:
        description: Execution backend
        required: false
        default: both
        type: choice
        options:
          - velnor
          - github
          - both
      scope:
        description: Verification scope
        required: true
        default: affected
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      full_units: ${{ steps.plan.outputs.full_units }}
      bun_matrix: ${{ steps.plan.outputs.bun_matrix }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        if: ${{ runner.environment == 'github-hosted' }}
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@03f9409b3f649a8973169dde0ebba3fe58a92ac9
        with:
          rev: 03f9409b3f649a8973169dde0ebba3fe58a92ac9
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=03f9409b3f649a8973169dde0ebba3fe58a92ac9" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_LANES: ${{ github.event.inputs.runner || 'both' }}
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          velnor-workflow plan --config .github/ci/project.toml

      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 03f9409b3f649a8973169dde0ebba3fe58a92ac9
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          [[ "$policy_revision" == "$EXPECTED_REVISION" ]] || { echo "::error::policy runtime reports revision $policy_revision, expected $EXPECTED_REVISION" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-03f9409b3f649a8973169dde0ebba3fe58a92ac9-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  velnor-lane-admission:
    name: "Control / Velnor admission"
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Velnor lane omitted for fork
        run: |
          echo '::notice::Velnor lane omitted for fork pull request; validating GitHub lane only.'
  github-bun-termrock-docs:
    name: "Bun · Bun package (termrock-docs)"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',bun-termrock-docs,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-termrock-docs
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: "~/.bun/install/cache"
      cache_key_files: |
        docs/package.json
        docs/bun.lock
  velnor-bun-termrock-docs:
    name: "Bun · Bun package (termrock-docs)"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',bun-termrock-docs,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-termrock-docs
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      host_warm_layers: cargo
  prepare-cargo:
    name: "Control / Prepare Cargo"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-oldrev-harness,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-oldrev-harness
      lane: control
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-oldrev-harness:
    name: "Rust · oldrev-harness"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-oldrev-harness,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-oldrev-harness
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: cc8618f6c9f0
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
        tools/oldrev-harness/Cargo.lock
      mbx_freshness_files: |
        crates/termrock-raster/**/*.rs
        crates/termrock-raster/benches/**
        crates/termrock-raster/examples/**
        crates/termrock-raster/src/**
        crates/termrock-raster/tests/**
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
        tools/oldrev-harness/**/*.rs
        tools/oldrev-harness/Cargo.lock
        tools/oldrev-harness/benches/**
        tools/oldrev-harness/examples/**
        tools/oldrev-harness/src/**
        tools/oldrev-harness/tests/**
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        tools/oldrev-harness/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "tools/oldrev-harness"
      cargo_net_offline: true
  velnor-rust-oldrev-harness:
    name: "Rust · oldrev-harness"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-oldrev-harness,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-oldrev-harness
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-policy:
    name: "Rust · Rust dependency policy"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: "030fe4ced8ce"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        deny.toml
      mbx_freshness_files: |
        .cargo/audit.toml
        .cargo/deny.toml
        Cargo.lock
        audit.toml
        deny.toml
      cargo_bin_tools: cargo-deny
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
  velnor-rust-policy:
    name: "Rust · Rust dependency policy"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "cargo:cargo-deny"
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-termrock:
    name: "Rust · termrock"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "0f11d25a1e56"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/termrock"
      cargo_net_offline: true
  velnor-rust-termrock:
    name: "Rust · termrock"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-termrock-catalog:
    name: "Rust · termrock-catalog"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-catalog,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-catalog
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "40e861d5f353"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/termrock-catalog/**/*.rs
        crates/termrock-catalog/benches/**
        crates/termrock-catalog/examples/**
        crates/termrock-catalog/src/**
        crates/termrock-catalog/tests/**
        crates/termrock-raster/**/*.rs
        crates/termrock-raster/benches/**
        crates/termrock-raster/examples/**
        crates/termrock-raster/src/**
        crates/termrock-raster/tests/**
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/termrock-catalog"
      cargo_net_offline: true
  velnor-rust-termrock-catalog:
    name: "Rust · termrock-catalog"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-catalog,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-catalog
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-termrock-catalog-web:
    name: "Rust · termrock-catalog-web"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-catalog-web,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-catalog-web
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: d909b5572308
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/termrock-catalog-web/**/*.rs
        crates/termrock-catalog-web/benches/**
        crates/termrock-catalog-web/examples/**
        crates/termrock-catalog-web/src/**
        crates/termrock-catalog-web/tests/**
        crates/termrock-catalog/**/*.rs
        crates/termrock-catalog/benches/**
        crates/termrock-catalog/examples/**
        crates/termrock-catalog/src/**
        crates/termrock-catalog/tests/**
        crates/termrock-raster/**/*.rs
        crates/termrock-raster/benches/**
        crates/termrock-raster/examples/**
        crates/termrock-raster/src/**
        crates/termrock-raster/tests/**
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/termrock-catalog-web"
      cargo_net_offline: true
  velnor-rust-termrock-catalog-web:
    name: "Rust · termrock-catalog-web"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-catalog-web,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-catalog-web
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-termrock-cli:
    name: "Rust · termrock-cli"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-cli,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-cli
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "36a6091d7c5f"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/termrock-cli/**/*.rs
        crates/termrock-cli/benches/**
        crates/termrock-cli/examples/**
        crates/termrock-cli/src/**
        crates/termrock-cli/tests/**
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/termrock-cli"
      cargo_net_offline: true
  velnor-rust-termrock-cli:
    name: "Rust · termrock-cli"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-cli,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-cli
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-termrock-contract-audit:
    name: "Rust · termrock-contract-audit"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-contract-audit,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-contract-audit
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: fbaf76a098f7
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        docs/contract-audit/Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/termrock-catalog/**/*.rs
        crates/termrock-catalog/benches/**
        crates/termrock-catalog/examples/**
        crates/termrock-catalog/src/**
        crates/termrock-catalog/tests/**
        crates/termrock-raster/**/*.rs
        crates/termrock-raster/benches/**
        crates/termrock-raster/examples/**
        crates/termrock-raster/src/**
        crates/termrock-raster/tests/**
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/contract-audit/**/*.rs
        docs/contract-audit/Cargo.lock
        docs/contract-audit/benches/**
        docs/contract-audit/examples/**
        docs/contract-audit/src/**
        docs/contract-audit/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        docs/contract-audit/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "docs/contract-audit"
      cargo_net_offline: true
  velnor-rust-termrock-contract-audit:
    name: "Rust · termrock-contract-audit"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-contract-audit,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-contract-audit
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-termrock-raster:
    name: "Rust · termrock-raster"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-raster,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-raster
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: aa642eb8bcb6
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/termrock-raster/**/*.rs
        crates/termrock-raster/benches/**
        crates/termrock-raster/examples/**
        crates/termrock-raster/src/**
        crates/termrock-raster/tests/**
        crates/termrock/**/*.rs
        crates/termrock/benches/**
        crates/termrock/examples/**
        crates/termrock/src/**
        crates/termrock/src/patterns/activity_shelf.rs
        crates/termrock/src/patterns/agent_status_header.rs
        crates/termrock/src/patterns/agent_workbench.rs
        crates/termrock/src/patterns/approval_queue.rs
        crates/termrock/src/patterns/background_task_panel.rs
        crates/termrock/src/patterns/connection_manager.rs
        crates/termrock/src/patterns/database_workbench.rs
        crates/termrock/src/patterns/error_recovery.rs
        crates/termrock/src/patterns/file_manager.rs
        crates/termrock/src/patterns/git_workbench.rs
        crates/termrock/src/patterns/help_center.rs
        crates/termrock/src/patterns/integration_status.rs
        crates/termrock/src/patterns/metrics_dashboard.rs
        crates/termrock/src/patterns/observability_dashboard.rs
        crates/termrock/src/patterns/plan_review.rs
        crates/termrock/src/patterns/process_table.rs
        crates/termrock/src/patterns/project_launcher.rs
        crates/termrock/src/patterns/prompt_queue.rs
        crates/termrock/src/patterns/query_editor.rs
        crates/termrock/src/patterns/result_grid.rs
        crates/termrock/src/patterns/schema_browser.rs
        crates/termrock/src/patterns/session_picker.rs
        crates/termrock/src/patterns/settings_screen.rs
        crates/termrock/src/patterns/setup_wizard.rs
        crates/termrock/src/patterns/subagent_card.rs
        crates/termrock/src/patterns/task_rail.rs
        crates/termrock/src/patterns/terminal_run_card.rs
        crates/termrock/src/patterns/working_state_card.rs
        crates/termrock/src/widgets/action_bar.rs
        crates/termrock/src/widgets/agent.rs
        crates/termrock/src/widgets/agent_blocks.rs
        crates/termrock/src/widgets/attachment_chips.rs
        crates/termrock/src/widgets/checkpoint_timeline.rs
        crates/termrock/src/widgets/citation.rs
        crates/termrock/src/widgets/context_meter.rs
        crates/termrock/src/widgets/data_table.rs
        crates/termrock/src/widgets/dependency_graph.rs
        crates/termrock/src/widgets/mention.rs
        crates/termrock/src/widgets/message_thread.rs
        crates/termrock/src/widgets/model_mode_selectors.rs
        crates/termrock/src/widgets/permission.rs
        crates/termrock/src/widgets/picker.rs
        crates/termrock/src/widgets/prompt_composer.rs
        crates/termrock/src/widgets/question_flow.rs
        crates/termrock/src/widgets/search_results.rs
        crates/termrock/src/widgets/slash_command_menu.rs
        crates/termrock/src/widgets/spinner.rs
        crates/termrock/src/widgets/status_bar.rs
        crates/termrock/src/widgets/status_strip.rs
        crates/termrock/src/widgets/streaming_markdown.rs
        crates/termrock/src/widgets/terminal_output.rs
        crates/termrock/src/widgets/tool_call_card.rs
        crates/termrock/src/widgets/trace_waterfall.rs
        crates/termrock/src/widgets/transcript.rs
        crates/termrock/tests/**
        docs/design/shadcn-tui-direction.md
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/termrock-raster"
      cargo_net_offline: true
  velnor-rust-termrock-raster:
    name: "Rust · termrock-raster"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-termrock-raster,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-termrock-raster
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  ci-required:
    name: ci-required
    if: ${{ always() }}
    needs: [plan, velnor-lane-admission, github-bun-termrock-docs, velnor-bun-termrock-docs, prepare-cargo, github-rust-oldrev-harness, velnor-rust-oldrev-harness, github-rust-policy, velnor-rust-policy, github-rust-termrock, velnor-rust-termrock, github-rust-termrock-catalog, velnor-rust-termrock-catalog, github-rust-termrock-catalog-web, velnor-rust-termrock-catalog-web, github-rust-termrock-cli, velnor-rust-termrock-cli, github-rust-termrock-contract-audit, velnor-rust-termrock-contract-audit, github-rust-termrock-raster, velnor-rust-termrock-raster]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          FORK_PR: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job velnor-lane-admission)"
          case "$result" in
            success|skipped) ;;
            *) echo "required CI prerequisite velnor-lane-admission did not pass: $result" >&2; exit 1 ;;
          esac
          selected=",$SELECTED_UNITS,"
          if [[ "$selected" == *",bun-termrock-docs,"* ]]; then
            result="$(result_for_job github-bun-termrock-docs)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-bun-termrock-docs did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-bun-termrock-docs)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-bun-termrock-docs failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",bun-termrock-docs,"* ]]; then
            result="$(result_for_job velnor-bun-termrock-docs)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-bun-termrock-docs did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-bun-termrock-docs did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-bun-termrock-docs)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-bun-termrock-docs failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-oldrev-harness,"* ]]; then
            result="$(result_for_job prepare-cargo)"
            if [[ "$result" != success ]]; then
              echo "selected CI prerequisite prepare-cargo did not pass: $result" >&2
              exit 1
            fi
          else
            result="$(result_for_job prepare-cargo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI prerequisite prepare-cargo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-oldrev-harness,"* ]]; then
            result="$(result_for_job github-rust-oldrev-harness)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-oldrev-harness did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-oldrev-harness)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-oldrev-harness failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-oldrev-harness,"* ]]; then
            result="$(result_for_job velnor-rust-oldrev-harness)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-oldrev-harness did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-oldrev-harness did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-oldrev-harness)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-oldrev-harness failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-policy,"* ]]; then
            result="$(result_for_job github-rust-policy)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-policy did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-policy)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-policy failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-policy,"* ]]; then
            result="$(result_for_job velnor-rust-policy)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-policy)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-policy failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock,"* ]]; then
            result="$(result_for_job github-rust-termrock)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-termrock did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-termrock)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-termrock failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock,"* ]]; then
            result="$(result_for_job velnor-rust-termrock)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-termrock did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-termrock did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-termrock)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-termrock failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-catalog,"* ]]; then
            result="$(result_for_job github-rust-termrock-catalog)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-termrock-catalog did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-termrock-catalog)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-termrock-catalog failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-catalog,"* ]]; then
            result="$(result_for_job velnor-rust-termrock-catalog)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-termrock-catalog did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-termrock-catalog did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-termrock-catalog)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-termrock-catalog failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-catalog-web,"* ]]; then
            result="$(result_for_job github-rust-termrock-catalog-web)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-termrock-catalog-web did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-termrock-catalog-web)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-termrock-catalog-web failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-catalog-web,"* ]]; then
            result="$(result_for_job velnor-rust-termrock-catalog-web)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-termrock-catalog-web did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-termrock-catalog-web did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-termrock-catalog-web)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-termrock-catalog-web failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-cli,"* ]]; then
            result="$(result_for_job github-rust-termrock-cli)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-termrock-cli did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-termrock-cli)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-termrock-cli failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-cli,"* ]]; then
            result="$(result_for_job velnor-rust-termrock-cli)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-termrock-cli did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-termrock-cli did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-termrock-cli)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-termrock-cli failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-contract-audit,"* ]]; then
            result="$(result_for_job github-rust-termrock-contract-audit)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-termrock-contract-audit did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-termrock-contract-audit)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-termrock-contract-audit failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-contract-audit,"* ]]; then
            result="$(result_for_job velnor-rust-termrock-contract-audit)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-termrock-contract-audit did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-termrock-contract-audit did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-termrock-contract-audit)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-termrock-contract-audit failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-raster,"* ]]; then
            result="$(result_for_job github-rust-termrock-raster)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-termrock-raster did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-termrock-raster)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-termrock-raster failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-termrock-raster,"* ]]; then
            result="$(result_for_job velnor-rust-termrock-raster)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-termrock-raster did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-termrock-raster did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-termrock-raster)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-termrock-raster failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ always() }}
    needs: [ci-required]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
