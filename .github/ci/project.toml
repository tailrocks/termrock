# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "tailrocks/termrock"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-deny-policy", "cargo-lockfile", "cargo-nextest-policy", "mise-present", "package-manager:bun:docs", "package-script:docs:build", "renovate-configuration", "rust-binaries:termrock-catalog:2", "rust-binaries:termrock-cli:1", "rust-features:termrock-catalog:2", "rust-features:termrock:3", "rust-ffi:termrock-catalog-web", "rust-test-targets:termrock-catalog:1", "rust-test-targets:termrock-cli:1", "rust-test-targets:termrock-raster:1", "rust-test-targets:termrock:1", "rust-workspace-members:7"]
limitations = ["No test script is declared in docs/package.json; no package test command was invented.", "Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-bun.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 on providers [github-hosted]. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Local-provider jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "bun-termrock-docs"
label = "Bun package (termrock-docs)"
kind = "bun"
root = "docs"
watch = ["docs/**/*.js", "docs/**/*.json", "docs/**/*.jsx", "docs/**/*.ts", "docs/**/*.tsx", "docs/bun.lock", "docs/bun.lockb", "docs/package-lock.json", "docs/package.json"]
pr_commands = ["cd -- 'docs' && bun install --frozen-lockfile", "cd -- 'docs' && bun run build"]
full_commands = ["cd -- 'docs' && bun install --frozen-lockfile", "cd -- 'docs' && bun run build"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = ["docs/package.json", "docs/bun.lock"]
paths = ["~/.bun/install/cache"]

[[unit]]
id = "rust-oldrev-harness"
label = "Rust crate (oldrev-harness)"
kind = "rust"
root = "tools/oldrev-harness"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "tools/oldrev-harness/**/*.rs", "tools/oldrev-harness/Cargo.lock", "tools/oldrev-harness/Cargo.toml", "tools/oldrev-harness/benches/**", "tools/oldrev-harness/examples/**", "tools/oldrev-harness/src/**", "tools/oldrev-harness/tests/**"]
pr_commands = ["cd -- 'tools/oldrev-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/oldrev-harness' && mbx clippy --locked --profile test --all-targets --all-features --package 'oldrev-harness' -- -D warnings", "cd -- 'tools/oldrev-harness' && mbx nextest run --locked --all-features --package 'oldrev-harness' --no-tests pass"]
full_commands = ["cd -- 'tools/oldrev-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'tools/oldrev-harness' && mbx clippy --locked --profile test --all-targets --all-features --package 'oldrev-harness' -- -D warnings", "cd -- 'tools/oldrev-harness' && mbx nextest run --locked --all-features --package 'oldrev-harness' --no-tests pass"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'tools/oldrev-harness' && mbx check --locked --profile test --all-targets --all-features --package 'oldrev-harness'"]
depends_on = ["rust-termrock", "rust-termrock-raster"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "tools/oldrev-harness/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-policy"
label = "Rust dependency policy"
kind = "rust"
root = "."
watch = [".cargo/audit.toml", ".cargo/deny.toml", "Cargo.lock", "Cargo.toml", "audit.toml", "deny.toml"]
pr_commands = ["mbx deny check"]
full_commands = ["mbx deny check"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "deny.toml"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termrock"
label = "Rust crate (termrock)"
kind = "rust"
root = "crates/termrock"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/termrock/**/*.rs", "crates/termrock/Cargo.toml", "crates/termrock/benches/**", "crates/termrock/examples/**", "crates/termrock/src/**", "crates/termrock/src/interaction/mod.rs", "crates/termrock/src/lib.rs", "crates/termrock/src/patterns/activity_shelf.rs", "crates/termrock/src/patterns/agent_status_header.rs", "crates/termrock/src/patterns/agent_workbench.rs", "crates/termrock/src/patterns/approval_queue.rs", "crates/termrock/src/patterns/background_task_panel.rs", "crates/termrock/src/patterns/connection_manager.rs", "crates/termrock/src/patterns/database_workbench.rs", "crates/termrock/src/patterns/error_recovery.rs", "crates/termrock/src/patterns/file_manager.rs", "crates/termrock/src/patterns/git_workbench.rs", "crates/termrock/src/patterns/help_center.rs", "crates/termrock/src/patterns/integration_status.rs", "crates/termrock/src/patterns/metrics_dashboard.rs", "crates/termrock/src/patterns/observability_dashboard.rs", "crates/termrock/src/patterns/plan_review.rs", "crates/termrock/src/patterns/process_table.rs", "crates/termrock/src/patterns/project_launcher.rs", "crates/termrock/src/patterns/prompt_queue.rs", "crates/termrock/src/patterns/query_editor.rs", "crates/termrock/src/patterns/result_grid.rs", "crates/termrock/src/patterns/schema_browser.rs", "crates/termrock/src/patterns/session_picker.rs", "crates/termrock/src/patterns/settings_screen.rs", "crates/termrock/src/patterns/setup_wizard.rs", "crates/termrock/src/patterns/subagent_card.rs", "crates/termrock/src/patterns/task_rail.rs", "crates/termrock/src/patterns/terminal_run_card.rs", "crates/termrock/src/patterns/working_state_card.rs", "crates/termrock/src/style/mod.rs", "crates/termrock/src/style/tokens.rs", "crates/termrock/src/widgets/action_bar.rs", "crates/termrock/src/widgets/agent.rs", "crates/termrock/src/widgets/agent_blocks.rs", "crates/termrock/src/widgets/attachment_chips.rs", "crates/termrock/src/widgets/checkpoint_timeline.rs", "crates/termrock/src/widgets/citation.rs", "crates/termrock/src/widgets/context_meter.rs", "crates/termrock/src/widgets/data_table.rs", "crates/termrock/src/widgets/dependency_graph.rs", "crates/termrock/src/widgets/mention.rs", "crates/termrock/src/widgets/message_thread.rs", "crates/termrock/src/widgets/model_mode_selectors.rs", "crates/termrock/src/widgets/panel.rs", "crates/termrock/src/widgets/permission.rs", "crates/termrock/src/widgets/picker.rs", "crates/termrock/src/widgets/prompt_composer.rs", "crates/termrock/src/widgets/question_flow.rs", "crates/termrock/src/widgets/search_results.rs", "crates/termrock/src/widgets/slash_command_menu.rs", "crates/termrock/src/widgets/spinner.rs", "crates/termrock/src/widgets/status_bar.rs", "crates/termrock/src/widgets/status_strip.rs", "crates/termrock/src/widgets/streaming_markdown.rs", "crates/termrock/src/widgets/terminal_output.rs", "crates/termrock/src/widgets/tool_call_card.rs", "crates/termrock/src/widgets/trace_waterfall.rs", "crates/termrock/src/widgets/transcript.rs", "crates/termrock/tests/**", "docs/design/shadcn-tui-direction.md", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/termrock' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock' -- -D warnings", "cd -- 'crates/termrock' && mbx nextest run --locked --all-features --package 'termrock' --no-tests pass", "cd -- 'crates/termrock' && mbx test --locked --doc --all-features --package 'termrock'"]
full_commands = ["cd -- 'crates/termrock' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock' -- -D warnings", "cd -- 'crates/termrock' && mbx nextest run --locked --all-features --package 'termrock' --no-tests pass", "cd -- 'crates/termrock' && mbx test --locked --doc --all-features --package 'termrock'"]
phases = ["fmt", "clippy", "test", "doctest"]
check_commands = ["cd -- 'crates/termrock' && mbx check --locked --profile test --all-targets --all-features --package 'termrock'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termrock-catalog"
label = "Rust crate (termrock-catalog)"
kind = "rust"
root = "crates/termrock-catalog"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/termrock-catalog/**/*.rs", "crates/termrock-catalog/Cargo.toml", "crates/termrock-catalog/benches/**", "crates/termrock-catalog/examples/**", "crates/termrock-catalog/src/**", "crates/termrock-catalog/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/termrock-catalog' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-catalog' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-catalog' -- -D warnings", "cd -- 'crates/termrock-catalog' && mbx nextest run --locked --all-features --package 'termrock-catalog' --no-tests pass", "cd -- 'crates/termrock-catalog' && mbx test --locked --doc --all-features --package 'termrock-catalog'"]
full_commands = ["cd -- 'crates/termrock-catalog' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-catalog' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-catalog' -- -D warnings", "cd -- 'crates/termrock-catalog' && mbx nextest run --locked --all-features --package 'termrock-catalog' --no-tests pass", "cd -- 'crates/termrock-catalog' && mbx test --locked --doc --all-features --package 'termrock-catalog'"]
phases = ["fmt", "clippy", "test", "doctest"]
check_commands = ["cd -- 'crates/termrock-catalog' && mbx check --locked --profile test --all-targets --all-features --package 'termrock-catalog'"]
depends_on = ["rust-termrock", "rust-termrock-raster"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termrock-catalog-web"
label = "Rust crate (termrock-catalog-web)"
kind = "rust"
root = "crates/termrock-catalog-web"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/termrock-catalog-web/**/*.rs", "crates/termrock-catalog-web/Cargo.toml", "crates/termrock-catalog-web/benches/**", "crates/termrock-catalog-web/examples/**", "crates/termrock-catalog-web/src/**", "crates/termrock-catalog-web/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/termrock-catalog-web' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-catalog-web' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-catalog-web' -- -D warnings", "cd -- 'crates/termrock-catalog-web' && mbx nextest run --locked --all-features --package 'termrock-catalog-web' --no-tests pass", "cd -- 'crates/termrock-catalog-web' && mbx test --locked --doc --all-features --package 'termrock-catalog-web'"]
full_commands = ["cd -- 'crates/termrock-catalog-web' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-catalog-web' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-catalog-web' -- -D warnings", "cd -- 'crates/termrock-catalog-web' && mbx nextest run --locked --all-features --package 'termrock-catalog-web' --no-tests pass", "cd -- 'crates/termrock-catalog-web' && mbx test --locked --doc --all-features --package 'termrock-catalog-web'"]
phases = ["fmt", "clippy", "test", "doctest"]
check_commands = ["cd -- 'crates/termrock-catalog-web' && mbx check --locked --profile test --all-targets --all-features --package 'termrock-catalog-web'"]
depends_on = ["rust-termrock-catalog"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termrock-cli"
label = "Rust crate (termrock-cli)"
kind = "rust"
root = "crates/termrock-cli"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/termrock-cli/**/*.rs", "crates/termrock-cli/Cargo.toml", "crates/termrock-cli/benches/**", "crates/termrock-cli/examples/**", "crates/termrock-cli/src/**", "crates/termrock-cli/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/termrock-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-cli' -- -D warnings", "cd -- 'crates/termrock-cli' && mbx nextest run --locked --all-features --package 'termrock-cli' --no-tests pass", "cd -- 'crates/termrock-cli' && mbx test --locked --doc --all-features --package 'termrock-cli'"]
full_commands = ["cd -- 'crates/termrock-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-cli' -- -D warnings", "cd -- 'crates/termrock-cli' && mbx nextest run --locked --all-features --package 'termrock-cli' --no-tests pass", "cd -- 'crates/termrock-cli' && mbx test --locked --doc --all-features --package 'termrock-cli'"]
phases = ["fmt", "clippy", "test", "doctest"]
check_commands = ["cd -- 'crates/termrock-cli' && mbx check --locked --profile test --all-targets --all-features --package 'termrock-cli'"]
depends_on = ["rust-termrock"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termrock-contract-audit"
label = "Rust crate (termrock-contract-audit)"
kind = "rust"
root = "docs/contract-audit"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "docs/contract-audit/**/*.rs", "docs/contract-audit/Cargo.lock", "docs/contract-audit/Cargo.toml", "docs/contract-audit/benches/**", "docs/contract-audit/examples/**", "docs/contract-audit/src/**", "docs/contract-audit/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'docs/contract-audit' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'docs/contract-audit' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-contract-audit' -- -D warnings", "cd -- 'docs/contract-audit' && mbx nextest run --locked --all-features --package 'termrock-contract-audit' --no-tests pass"]
full_commands = ["cd -- 'docs/contract-audit' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'docs/contract-audit' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-contract-audit' -- -D warnings", "cd -- 'docs/contract-audit' && mbx nextest run --locked --all-features --package 'termrock-contract-audit' --no-tests pass"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'docs/contract-audit' && mbx check --locked --profile test --all-targets --all-features --package 'termrock-contract-audit'"]
depends_on = ["rust-termrock-catalog"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "docs/contract-audit/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termrock-raster"
label = "Rust crate (termrock-raster)"
kind = "rust"
root = "crates/termrock-raster"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/termrock-raster/**/*.rs", "crates/termrock-raster/Cargo.toml", "crates/termrock-raster/assets/fonts/JetBrainsMono-Bold.ttf", "crates/termrock-raster/assets/fonts/JetBrainsMono-Italic.ttf", "crates/termrock-raster/assets/fonts/JetBrainsMono-Regular.ttf", "crates/termrock-raster/assets/fonts/NotoEmoji-Regular.ttf", "crates/termrock-raster/assets/fonts/NotoSansMath-Regular.ttf", "crates/termrock-raster/assets/fonts/NotoSansSymbols2-Regular.ttf", "crates/termrock-raster/benches/**", "crates/termrock-raster/examples/**", "crates/termrock-raster/src/**", "crates/termrock-raster/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/termrock-raster' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-raster' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-raster' -- -D warnings", "cd -- 'crates/termrock-raster' && mbx nextest run --locked --all-features --package 'termrock-raster' --no-tests pass", "cd -- 'crates/termrock-raster' && mbx test --locked --doc --all-features --package 'termrock-raster'"]
full_commands = ["cd -- 'crates/termrock-raster' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/termrock-raster' && mbx clippy --locked --profile test --all-targets --all-features --package 'termrock-raster' -- -D warnings", "cd -- 'crates/termrock-raster' && mbx nextest run --locked --all-features --package 'termrock-raster' --no-tests pass", "cd -- 'crates/termrock-raster' && mbx test --locked --doc --all-features --package 'termrock-raster'"]
phases = ["fmt", "clippy", "test", "doctest"]
check_commands = ["cd -- 'crates/termrock-raster' && mbx check --locked --profile test --all-targets --all-features --package 'termrock-raster'"]
depends_on = ["rust-termrock"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
