# Generated by the harness; nothing here is a source of truth.
out/
last-report.json

# Local-only advisory rasters (font/OS dependent — never CI baselines, see README).
reference/scenes/*.png

# Local Pillow venv, created on demand by bin/diff_png.py users.
.venv/

# Python bytecode (run.py imports ansi2grid/diff_grid directly).
__pycache__/
