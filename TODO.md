# Follow-ups

- Add portable benchmarks, fuzz targets, and mutation testing after the first stable consumer migration.
- Evaluate shared testing helpers only after two independent consumers require the same API.
- Measure at least 20 CI runs before considering a compiler cache.
- Add Windows support only through a separately reviewed platform decision.
