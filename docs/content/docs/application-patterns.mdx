---
title: Application patterns
---

Seven examples render the same neutral screen while varying ownership: `direct`, `tea`, `component`, `flux`, `buffer_only`, `crossterm_manual`, and `crossterm_managed`. TermRock does not require any architecture.
