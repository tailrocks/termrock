---
title: Connection Manager
description: 'Application recipe composed from public TermRock building blocks; domain state and effects remain host-owned.'
pattern: ConnectionManager
demo: connection-manager/full
interaction: activation
source: crates/termrock/src/patterns/connection_manager.rs
classification: application
---

This is an example composite from `termrock::patterns`, not a first-class widget. Copy the recipe, keep its reusable parts, and replace its sample domain model with your own.

## Live application preview

<TerminalPreview story="connection-manager/full" interactive caption="Connection Manager · shared Rust demo" />

## Classification and composition

**application** · default grid 72×18

Composed public building blocks: `ConnectivityPhase`, `Panel`, `PasswordInput`, `RevealPolicy`, `StatefulWidget`, `Widget`.

## Workflow

- `↑↓/wheel select`: move the pattern-owned viewport without scrolling the documentation page.
- `Enter connect`: invoke the focused public state-machine action and inspect its typed request.
- `/ filter`: focus the real field and edit its persistent Unicode-safe draft.
- `D delete dialog`: forward this normalized action to the mounted public pattern state.
- `N new`: forward this normalized action to the mounted public pattern state.

Accepted actions update the same persistent pattern instance in web docs and native Lookbook. Typed requests appear in the status line; the fixtures never authenticate, connect, execute, persist, or mutate external state.

## Ownership

TermRock owns composition, focus geometry, responsive layout, and reusable state transitions. The consuming application owns records, commands, persistence, secrets, authorization, and side effects.

## Source

- Pattern recipe: [`crates/termrock/src/patterns/connection_manager.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/patterns/connection_manager.rs)
- Demo: `connection-manager/full`
