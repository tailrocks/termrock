---
title: Integration Status
description: 'Application recipe composed from public TermRock building blocks; domain state and effects remain host-owned.'
pattern: IntegrationStatus
demo: integration-status/list
interaction: activation
source: crates/termrock/src/patterns/integration_status.rs
classification: composite
---

This is an example composite from `termrock::patterns`, not a first-class widget. Copy the recipe, keep its reusable parts, and replace its sample domain model with your own.

## Live application preview

<TerminalPreview story="integration-status/list" interactive caption="Integration Status · shared Rust demo" />

## Classification and composition

**composite** · default grid 64×14

Composed public building blocks: `Panel`, `StatefulWidget`, `Widget`.

## Workflow

- `↑↓ select`: move visible focus, selection, pane, or hierarchy state for the next action.
- `Enter inspect`: invoke the focused public state-machine action and inspect its typed request.
- `R reconnect`: forward this normalized action to the mounted public pattern state.
- `Tab details`: move visible focus, selection, pane, or hierarchy state for the next action.

Accepted actions update the same persistent pattern instance in web docs and native Lookbook. Typed requests appear in the status line; the fixtures never authenticate, connect, execute, persist, or mutate external state.

## Ownership

TermRock owns composition, focus geometry, responsive layout, and reusable state transitions. The consuming application owns records, commands, persistence, secrets, authorization, and side effects.

## Source

- Pattern recipe: [`crates/termrock/src/patterns/integration_status.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/patterns/integration_status.rs)
- Demo: `integration-status/list`
