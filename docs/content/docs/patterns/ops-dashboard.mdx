---
title: Ops Dashboard
description: 'Application recipe composed from public TermRock building blocks; domain state and effects remain host-owned.'
pattern: OpsDashboard
demo: observability-dashboard/basic
interaction: activation
source: crates/termrock/src/patterns/ops_dashboard.rs
classification: application
---

This is an example composite from `termrock::patterns`, not a first-class widget. Copy the recipe, keep its reusable parts, and replace its sample domain model with your own.

## Live application preview

<TerminalPreview story="observability-dashboard/basic" interactive caption="Ops Dashboard · shared Rust demo" />

## Classification and composition

**application** · default grid 120×36

Composed public building blocks: `ColumnModel`, `DataColumn`, `DataColumnWidth`.

## Workflow

- `Tab cycle panes`: move visible focus, selection, pane, or hierarchy state for the next action.
- `↑↓ navigate`: forward this normalized action to the mounted public pattern state.
- `Space pause/resume`: invoke the focused public state-machine action and inspect its typed request.
- `Ctrl+R reconnect`: forward this normalized action to the mounted public pattern state.
- `/ search`: focus the real field and edit its persistent Unicode-safe draft.

Accepted actions update the same persistent pattern instance in web docs and native Lookbook. Typed requests appear in the status line; the fixtures never authenticate, connect, execute, persist, or mutate external state.

## Ownership

TermRock owns composition, focus geometry, responsive layout, and reusable state transitions. The consuming application owns records, commands, persistence, secrets, authorization, and side effects.

## Source

- Pattern recipe: [`crates/termrock/src/patterns/ops_dashboard.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/patterns/ops_dashboard.rs)
- Demo: `observability-dashboard/basic`
