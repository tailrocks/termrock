---
title: Auth Entry
description: 'Application recipe composed from public TermRock building blocks; domain state and effects remain host-owned.'
pattern: AuthEntry
demo: auth-entry/basic
interaction: activation
source: crates/termrock/src/patterns/auth_entry.rs
classification: application
---

This is an example composite from `termrock::patterns`, not a first-class widget. Copy the recipe, keep its reusable parts, and replace its sample domain model with your own.

## Live application preview

<TerminalPreview story="auth-entry/basic" interactive caption="Auth Entry · shared Rust demo" />

## Classification and composition

**application** · default grid 64×20

Composed public building blocks: `Checkbox`, `Panel`, `PanelVariant`, `PasswordInput`, `TextInput`, `Validation`.

## Workflow

- `type/paste field`: focus the real field and edit its persistent Unicode-safe draft.
- `Tab next field`: move visible focus, selection, pane, or hierarchy state for the next action.
- `Ctrl+Enter submit`: invoke the focused public state-machine action and inspect its typed request.
- `Ctrl+G switch mode`: forward this normalized action to the mounted public pattern state.
- `Esc cancel`: peel or cancel the current transient layer and preserve underlying state.

Accepted actions update the same persistent pattern instance in web docs and native Lookbook. Typed requests appear in the status line; the fixtures never authenticate, connect, execute, persist, or mutate external state.

## Ownership

TermRock owns composition, focus geometry, responsive layout, and reusable state transitions. The consuming application owns records, commands, persistence, secrets, authorization, and side effects.

## Source

- Pattern recipe: [`crates/termrock/src/patterns/auth_entry.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/patterns/auth_entry.rs)
- Demo: `auth-entry/basic`
