---
title: Background Task Panel
description: 'Application recipe composed from public TermRock building blocks; domain state and effects remain host-owned.'
pattern: BackgroundTaskPanel
demo: background-tasks/mixed-statuses
interaction: activation
source: crates/termrock/src/patterns/background_task_panel.rs
classification: composite
---

This is an example composite from `termrock::patterns`, not a first-class widget. Copy the recipe, keep its reusable parts, and replace its sample domain model with your own.

## Live application preview

<TerminalPreview story="background-tasks/mixed-statuses" interactive caption="Background Task Panel · shared Rust demo" />

## Classification and composition

**composite** · default grid 88×20

Composed public building blocks: `List`, `ListRow`, `NotificationItem`, `Panel`, `RowRole`, `SemanticStatus`, `StatefulWidget`, `TerminalCommandMeta`, `TerminalLine`, `TerminalOutput`, `TerminalRunStatus`, `TerminalStream`, `ToastPriority`, `Widget`.

## Workflow

- `Enter open action` or `click trigger` opens a real ChoiceDialog; confirm/cancel produce different outcomes.
- `S toggle sidebar region` changes the visible application chrome.
- `↑↓/wheel select row` changes the persistent row selection.
- `/ filter` focuses a real TextInput; type or paste, then Enter applies or Escape cancels.

The preview and native Lookbook instantiate the same Rust story and state machine. Outcomes appear in the preview status line.

## Ownership

TermRock owns composition, focus geometry, responsive layout, and reusable state transitions. The consuming application owns records, commands, persistence, secrets, authorization, and side effects.

## Source

- Pattern recipe: [`crates/termrock/src/patterns/background_task_panel.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/patterns/background_task_panel.rs)
- Demo: `background-tasks/mixed-statuses`

## Migrated handbook guidance

# BackgroundTaskPanel

Multi-job supervisor UI: list + live output detail, reconnect/lost states,
bounded scrollback with drop banners. Outcomes request stop/restart/detach only.


## When to use / when not

| Use BackgroundTaskPanel | Prefer |
|-------------------------|--------|
| Detached builds, watchers, servers | — |
| OS process browser | [ProcessTable](/docs/patterns/process-table) |
| Single agent shell card | [TerminalRunCard](/docs/patterns/terminal-run-card) |
| Agent task inventory | [TaskRail](/docs/components/task-rail) |

## Output history

`BackgroundOutputBuffer` caps lines (`BACKGROUND_TASK_DEFAULT_HISTORY`); paints
dropped-line indicator when eviction occurs.

## Keys

j/k · enter open · x stop · r restart · d detach · f follow · Shift+C clear done · esc close

## Related

TerminalOutput · TaskRail · ProcessTable · migration `0224`
