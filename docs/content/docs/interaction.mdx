---
title: Interaction conventions
---

Tabs use one focus stop with arrow navigation. Disabled items are not hit targets. Hover never moves keyboard focus. Scroll only over the owning region. Dialog geometry and inside/outside classification are pure; applications own modal stacks. Hints advertise primary bindings and hide aliases. Navigation uses stable IDs, never durable indices.
