---
title: PathInput
description: 'A filesystem-aware path field with host-projected status, completion/browse hooks, and no FS coupling.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `path-input/basic`

Path input

![PathInput — Path input](/component-previews/path-input-basic.svg)

### `path-input/destructive`

Path destructive

![PathInput — Path destructive](/component-previews/path-input-destructive.svg)

### `path-input/missing`

Path missing

![PathInput — Path missing](/component-previews/path-input-missing.svg)

### `path-input/relative`

Path relative

![PathInput — Path relative](/component-previews/path-input-relative.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{PathFsStatus, PathInput, PathInputState, PathStyle};

let system = DesignSystem::default();
let mut state = PathInputState::new()
    .with_style(PathStyle::Unix)
    .with_path("/usr/local/bin");
state.set_focused(true);
state.set_fs_status(PathFsStatus::Directory);
let _ = PathInput::new(&system).label("Install dir").show_browse(true);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `path-input/basic` — Path input
- `path-input/destructive` — Path destructive
- `path-input/missing` — Path missing
- `path-input/relative` — Path relative
