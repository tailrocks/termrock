---
title: Tree
description: 'A navigable flattened hierarchy with disclosure and multi-select support.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `tree/navigation`

Tree navigation

![Tree — Tree navigation](/component-previews/tree-navigation.svg)

### `tree/narrow`

Narrow Tree

![Tree — Narrow Tree](/component-previews/tree-narrow.svg)

### `tree/unicode`

Unicode Tree

![Tree — Unicode Tree](/component-previews/tree-unicode.svg)

## Usage

```rust
use ratatui_core::text::Line;
use termrock::{style::DesignTokens, Theme, input::{KeyCode, KeyEvent, KeyModifiers}, widgets::{Tree, TreeNode, TreeNodeStatus, TreeState}};

let theme = Theme::default();
let nodes = [TreeNode { id: "src", label: Line::from("src"), leading: None, secondary: None, badge: None, shortcut: None, trailing: None, depth: 0, branch: true, expanded: true, enabled: true, status: TreeNodeStatus::Ready }];
let tokens = DesignTokens::default();
let tree = Tree::new(&nodes, &tokens);
let mut state = TreeState::new(Some("src"));
let outcome = state.handle_key(&nodes, KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `tree/narrow` — Narrow Tree
- `tree/navigation` — Tree navigation
- `tree/unicode` — Unicode Tree
