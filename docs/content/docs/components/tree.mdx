---
title: Tree
description: 'Hierarchical collection: stable IDs, lazy children, loading/error, expansion, cursor/selection/check, icons, metadata, context actions, typeahead; Left collapse/parent, Right expand/enter; ancestor-preserving filter; virtual window + scroll anchors; ASCII glyphs.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `tree/navigation`

Tree navigation

![Tree — Tree navigation](/component-previews/tree-navigation.svg)

### `tree/actions`

Tree context actions

![Tree — Tree context actions](/component-previews/tree-actions.svg)

### `tree/ascii`

ASCII tree glyphs

![Tree — ASCII tree glyphs](/component-previews/tree-ascii.svg)

### `tree/composed`

Composed tree anatomy

![Tree — Composed tree anatomy](/component-previews/tree-composed.svg)

### `tree/deep`

Deep indent tree

![Tree — Deep indent tree](/component-previews/tree-deep.svg)

### `tree/empty`

Empty tree

![Tree — Empty tree](/component-previews/tree-empty.svg)

### `tree/filter`

Tree filter ancestors

![Tree — Tree filter ancestors](/component-previews/tree-filter.svg)

### `tree/lazy`

Tree lazy children

![Tree — Tree lazy children](/component-previews/tree-lazy.svg)

### `tree/loading-error`

Tree loading and error

![Tree — Tree loading and error](/component-previews/tree-loading-error.svg)

### `tree/narrow`

Narrow Tree

![Tree — Narrow Tree](/component-previews/tree-narrow.svg)

### `tree/tiny`

Tiny tree

![Tree — Tiny tree](/component-previews/tree-tiny.svg)

### `tree/unicode`

Unicode Tree

![Tree — Unicode Tree](/component-previews/tree-unicode.svg)

## Usage

```rust
use ratatui_core::text::Line;
use termrock::style::DesignSystem;
use termrock::widgets::{Tree, TreeNode, TreeState, filter_tree_with_ancestors};

let system = DesignSystem::default();
let nodes = [
    TreeNode::new("src", Line::from("src"), 0).branch().expanded(),
    TreeNode::new("lazy", Line::from("vendor"), 1).lazy_branch().parent("src"),
];
let mut state = TreeState::new(Some("src"));
// filter: filter_tree_with_ancestors(&nodes, "ven")
Tree::new(&nodes, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `tree/actions` — Tree context actions
- `tree/ascii` — ASCII tree glyphs
- `tree/composed` — Composed tree anatomy
- `tree/deep` — Deep indent tree
- `tree/empty` — Empty tree
- `tree/filter` — Tree filter ancestors
- `tree/lazy` — Tree lazy children
- `tree/loading-error` — Tree loading and error
- `tree/narrow` — Narrow Tree
- `tree/navigation` — Tree navigation
- `tree/tiny` — Tiny tree
- `tree/unicode` — Unicode Tree
