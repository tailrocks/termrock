---
title: Tree
description: 'Hierarchical collection: stable IDs, lazy children, loading/error, expansion, cursor/selection/check, icons, metadata, context actions, typeahead; Left collapse/parent, Right expand/enter; ancestor-preserving filter; virtual window + scroll anchors; ASCII glyphs.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="tree/navigation" interactive caption="Tree · Tree navigation — focus surface; use keys/click for states" />

## Usage

```rust
use ratatui_core::text::Line;
use termrock::style::DesignSystem;
use termrock::widgets::{Tree, TreeNode, TreeState, filter_tree_with_ancestors};

let system = DesignSystem::default();
let nodes = [
    TreeNode::new("src", Line::from("src"), 0).branch().expanded(),
    TreeNode::new("lazy", Line::from("vendor"), 1).lazy_branch().parent("src"),
];
let mut state = TreeState::new(Some("src"));
// filter: filter_tree_with_ancestors(&nodes, "ven")
Tree::new(&nodes, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `tree/navigation` | Tree navigation | **Primary (live preview)** |
| `tree/actions` | Tree context actions | actions |
| `tree/ascii` | ASCII tree glyphs | ASCII / low capability |
| `tree/composed` | Composed tree anatomy | composed |
| `tree/deep` | Deep indent tree | deep |
| `tree/empty` | Empty tree | Empty state |
| `tree/filter` | Tree filter ancestors | filter |
| `tree/lazy` | Tree lazy children | lazy |
| `tree/loading-error` | Tree loading and error | loading error |
| `tree/narrow` | Narrow Tree | Narrow terminal |
| `tree/tiny` | Tiny tree | Tiny viewport |
| `tree/unicode` | Unicode Tree | Unicode / wide glyphs |
