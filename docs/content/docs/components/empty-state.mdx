---
title: EmptyState
description: 'Centered empty surface with a non-color glyph.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `empty-state/basic`

Empty state

![EmptyState — Empty state](/component-previews/empty-state-basic.svg)

### `empty-state/narrow`

Narrow EmptyState

![EmptyState — Narrow EmptyState](/component-previews/empty-state-narrow.svg)

### `empty-state/unicode`

Unicode EmptyState

![EmptyState — Unicode EmptyState](/component-previews/empty-state-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::EmptyState};

let theme = Theme::default();
let empty = EmptyState::new("No results", &theme).detail("Try another query");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `empty-state/basic` — Empty state
- `empty-state/narrow` — Narrow EmptyState
- `empty-state/unicode` — Unicode EmptyState
