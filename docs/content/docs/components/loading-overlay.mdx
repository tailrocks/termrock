---
title: LoadingOverlay / BusyBoundary
description: 'Regional loading that blocks only the affected area — modes, cancel, nested boundaries.'
---
## Live terminal (Ghostty-class)

One focused interactive surface. Other variants are in the **Stories** table —
interactivity replaces multi-image galleries.

<TerminalPreview story="loading-overlay/blocking" interactive caption="LoadingOverlay / BusyBoundary · loading-overlay/blocking — focus surface; keys/click for states" />

## Usage

```rust
use termrock::style::{DesignSystem, Motion};
use termrock::runtime::FrameTick;
use termrock::widgets::{BusyBoundary, BusyBoundaryState, BusyMode, BusyRoute};

let system = DesignSystem::default();
let mut busy = BusyBoundaryState::new();
let _ = busy.begin(BusyMode::Cancellable, "Syncing workspace");
busy.set_unavailable(Some("Edits paused until sync finishes"));
busy.set_expected_ms(Some(2_000));

// paint content, then:
BusyBoundary::paint(pane, buf, &mut busy, &system, tick, Motion::Full);

match busy.route_key(key) {
    BusyRoute::Cancel => { /* abort */ }
    BusyRoute::Blocked => {}
    BusyRoute::Deliver => { /* normal */ }
    BusyRoute::Outside => {}
}
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered (route + cancel) |
| Mouse | covered (outside/block/cancel) |
| Focus | covered (trap while blocking) |
| Non-color cues | covered (spinner + labels + badges) |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

| Story | Title | Focus |
|---|---|---|
| `loading-overlay/blocking` | LoadingOverlay blocking | **Primary (live preview)** |
| `loading-overlay/cancellable` | LoadingOverlay cancellable | cancellable |
| `loading-overlay/non-blocking` | LoadingOverlay non-blocking | non blocking |
| `loading-overlay/optimistic` | LoadingOverlay optimistic | optimistic |
| `loading-overlay/stale` | LoadingOverlay stale | stale |
| `loading-overlay/nested` | BusyBoundary nested | nested |
