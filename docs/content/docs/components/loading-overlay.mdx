---
title: LoadingOverlay / BusyBoundary
description: 'Regional loading that blocks only the affected area — modes, cancel, nested boundaries.'
component: LoadingOverlay / BusyBoundary
demo: loading-overlay/blocking
interaction: timed-state
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/mod.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="loading-overlay/blocking" interactive caption="LoadingOverlay / BusyBoundary · LoadingOverlay blocking — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | covered |

## Configuration and variants

This component has one canonical catalog setup. Resize the live host to exercise its responsive contract; **Reset** remounts the same configuration.

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `loading-overlay/blocking`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn loading_overlay_blocking_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::style::MotionPolicy;
    use termrock::widgets::Panel;
    let _ = Panel::new(system)
        .title("table")
        .paint(area, frame.buffer_mut(), None);
    let (overlay, mut st) = example_busy_blocking(system);
    overlay.paint(
        area,
        frame.buffer_mut(),
        &mut st,
        loading_tick(),
        MotionPolicy::Off,
    );
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `loading-overlay/blocking`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

| Story | Title | Focus |
|---|---|---|
| `loading-overlay/blocking` | LoadingOverlay blocking | **Primary (live preview)** |
| `loading-overlay/cancellable` | LoadingOverlay cancellable | cancellable |
| `loading-overlay/non-blocking` | LoadingOverlay non-blocking | non blocking |
| `loading-overlay/optimistic` | LoadingOverlay optimistic | optimistic |
| `loading-overlay/stale` | LoadingOverlay stale | stale |
| `loading-overlay/nested` | BusyBoundary nested | nested |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `loading-overlay/blocking`

## Seen in applications

<SeenInApplications component="LoadingOverlay / BusyBoundary" />
