---
title: MultiSelect
description: 'A searchable multi-choice selector with chip summary, select-all, max selection, and highlight≠checked.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `multi-select/basic`

MultiSelect

![MultiSelect — MultiSelect](/component-previews/multi-select-basic.svg)

### `multi-select/open`

MultiSelect open

![MultiSelect — MultiSelect open](/component-previews/multi-select-open.svg)

### `multi-select/overflow`

MultiSelect overflow

![MultiSelect — MultiSelect overflow](/component-previews/multi-select-overflow.svg)

### `multi-select/search`

MultiSelect search

![MultiSelect — MultiSelect search](/component-previews/multi-select-search.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{MultiSelect, MultiSelectState, SelectOption};

let system = DesignSystem::default();
let options = [
  SelectOption::option("rs", "Rust"),
  SelectOption::option("go", "Go"),
];
let mut state = MultiSelectState::new().with_selected(["rs"]);
state.set_focused(true);
let _ = MultiSelect::new(&options, &system).label("Filters");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `multi-select/basic` — MultiSelect
- `multi-select/open` — MultiSelect open
- `multi-select/overflow` — MultiSelect overflow
- `multi-select/search` — MultiSelect search
