---
title: MultiSelect
description: 'A searchable multi-choice selector with chip summary, select-all, max selection, and highlight≠checked.'
component: MultiSelect
demo: multi-select/basic
interaction: disclosure-overlay
actions:
  - "Enter/click open"
  - "↑↓ highlight"
  - "Space toggle"
  - "type to filter"
  - "Enter close"
  - "Ctrl+A select all"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"↑↓ highlight\" action updates visible mounted state and reports its typed outcome."
  - "Editing changes the visible Unicode-safe draft and reports the public typed edit outcome."
  - "Cancel or close removes the transient state without performing a host-owned effect."
  - "Navigation moves visible focus or selection and preserves it for the next event."
source: crates/termrock/src/widgets/multi_select.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="multi-select/basic" interactive caption="MultiSelect · MultiSelect — live shared Rust state" />

## Try it

- `Enter/click open`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `↑↓ highlight`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Space toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `type to filter`: Focus the editable field, then type or paste Unicode text; the draft remains visible.
- `Enter close`: Open the transient state first, then cancel it; focus and the underlying state are preserved.
- `Ctrl+A select all`: Move through the real focus/selection model; the next action uses the new target.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `multi-select/basic` | MultiSelect | interactive |
| `multi-select/open` | MultiSelect open | paint-only |
| `multi-select/overflow` | MultiSelect overflow | paint-only |
| `multi-select/search` | MultiSelect search | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `multi-select/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn multi_select_basic_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let opts = multi_select_demo_options();
    let mut state = MultiSelectState::new()
        .with_recipe(SelectRecipe::Form)
        .with_selected(["rs", "go"]);
    state.set_focused(true);
    frame.render_stateful_widget(
        MultiSelect::new(&opts, system).label("Filters").ascii(true),
        area,
        &mut state,
    );
}
```

## Migrated handbook guidance

# MultiSelect

**Purpose.** Filters, permissions, task pickers — multi membership with a compact
closed summary and an open checklist.


## Check vs highlight

| | Meaning |
|--|---------|
| `selected()` / `is_checked` | Ordered membership |
| `highlight()` | Keyboard cursor in open list |

Space toggles check; reverse video is highlight only.

## Keys (open)

- **Space** — toggle · **Enter** — done/close · **Esc** — close keep checks  
- **Ctrl+A** — select all visible · **Ctrl+D** — clear  
- **Shift+↑/↓** — range check · type — search  

## Related

Select · Selection · TokenField · migration `0146`

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `multi-select/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `multi-select/basic` | MultiSelect | **Primary (live preview)** |
| `multi-select/open` | MultiSelect open | Open / expanded |
| `multi-select/overflow` | MultiSelect overflow | Overflow |
| `multi-select/search` | MultiSelect search | Search / filter |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/multi_select.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/multi_select.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `multi-select/basic`

## Seen in applications

<SeenInApplications component="MultiSelect" />
