---
title: CodeBlock
description: 'Source listing with optional line numbers and pluggable syntax.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `code-block/basic`

Code block

![CodeBlock — Code block](/component-previews/code-block-basic.svg)

### `code-block/highlights`

CodeBlock highlights

![CodeBlock — CodeBlock highlights](/component-previews/code-block-highlights.svg)

### `code-block/narrow`

Narrow CodeBlock

![CodeBlock — Narrow CodeBlock](/component-previews/code-block-narrow.svg)

### `code-block/no-color`

CodeBlock no-color

![CodeBlock — CodeBlock no-color](/component-previews/code-block-no-color.svg)

### `code-block/streaming`

CodeBlock streaming

![CodeBlock — CodeBlock streaming](/component-previews/code-block-streaming.svg)

### `code-block/unicode`

Unicode CodeBlock

![CodeBlock — Unicode CodeBlock](/component-previews/code-block-unicode.svg)

### `code-block/wrap`

CodeBlock wrap

![CodeBlock — CodeBlock wrap](/component-previews/code-block-wrap.svg)

## Usage

```rust
use termrock::{style::DesignSystem, widgets::CodeBlock};

let system = DesignSystem::default();
let lines = ["fn main() {}"];
let block = CodeBlock::new(&lines, &system).language("rust").line_numbers(true);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `code-block/basic` — Code block
- `code-block/highlights` — CodeBlock highlights
- `code-block/narrow` — Narrow CodeBlock
- `code-block/no-color` — CodeBlock no-color
- `code-block/streaming` — CodeBlock streaming
- `code-block/unicode` — Unicode CodeBlock
- `code-block/wrap` — CodeBlock wrap
