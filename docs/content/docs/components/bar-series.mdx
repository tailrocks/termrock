---
title: BarSeries
description: 'Labeled horizontal bars for density dashboards.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `bar-series/basic`

Bar series

![BarSeries — Bar series](/component-previews/bar-series-basic.svg)

### `bar-series/narrow`

Narrow BarSeries

![BarSeries — Narrow BarSeries](/component-previews/bar-series-narrow.svg)

### `bar-series/unicode`

Unicode BarSeries

![BarSeries — Unicode BarSeries](/component-previews/bar-series-unicode.svg)

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{BarDatum, BarSeries}};

let system = DesignSystem::default();
let bars = [BarDatum { label: "cpu", fraction: 0.72 }];
let series = BarSeries::new(&bars, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `bar-series/basic` — Bar series
- `bar-series/narrow` — Narrow BarSeries
- `bar-series/unicode` — Unicode BarSeries
