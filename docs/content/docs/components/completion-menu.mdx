---
title: CompletionMenu
description: 'Anchored suggestion surface for editors: groups, fuzzy ranges, kind glyphs, details, docs preview, async generation gates, loading/empty/stale, commit characters, active-descendant navigation (editor keeps focus), Tab/Enter/Esc intents, clamp/flip/fullscreen promotion.'
component: CompletionMenu
demo: completion-menu/basic
interaction: selection-navigation
actions:
  - "↑↓ select"
  - "Enter/Tab commit"
  - "Esc dismiss"
  - "click candidate"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/completion_menu.rs
---


## Live terminal (Ghostty-class)

This preview mounts one persistent Rust component instance. Use the listed keyboard and pointer actions; each accepted action updates the same state and reports its typed outcome.

<TerminalPreview story="completion-menu/basic" interactive caption="CompletionMenu · Completion menu — persistent Rust preview; available actions appear below" />

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::style::DesignSystem;
use termrock::widgets::{
    CompletionCandidate, CompletionMenu, CompletionMenuSize, CompletionMenuState,
    open_completion_overlay,
};

let system = DesignSystem::default();
let candidates = [
    CompletionCandidate::new("select", "SELECT")
        .kind("keyword")
        .group("Keywords")
        .documentation("Select rows."),
];
let mut state = CompletionMenuState::new(Some("select"));
state.set_commit_characters("().");
// async: let gen = state.begin_async(); … state.apply_results(gen, &candidates);
let bounds = Rect::new(0, 0, 80, 24);
let anchor = Rect::new(12, 6, 1, 1);
let _ = open_completion_overlay(&mut stack, bounds, anchor, CompletionMenuSize::default(), Some("editor"));
CompletionMenu::new(&candidates, &system, bounds, anchor).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `completion-menu/basic` | Completion menu | **Primary (live preview)** |
| `completion-menu/docs` | Completion menu docs | docs |
| `completion-menu/edge` | Completion menu edge flip | edge |
| `completion-menu/loading` | Completion menu loading | Loading state |
| `completion-menu/narrow` | Completion menu narrow | Narrow terminal |
| `completion-menu/unicode` | Completion menu Unicode | Unicode / wide glyphs |


## Try it

- Use the actions shown beside the terminal; mouse, keyboard, paste, drag, and resize are forwarded only when the component supports them.
- Confirm the visual state and typed outcome, then use **Reset** to mount a fresh instance.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `completion-menu/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/completion_menu.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/completion_menu.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `completion-menu/basic`
