---
title: CompletionMenu
description: 'Anchored suggestion surface for editors: groups, fuzzy ranges, kind glyphs, details, docs preview, async generation gates, loading/empty/stale, commit characters, active-descendant navigation (editor keeps focus), Tab/Enter/Esc intents, clamp/flip/fullscreen promotion.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `completion-menu/basic`

Completion menu

![CompletionMenu — Completion menu](/component-previews/completion-menu-basic.svg)

### `completion-menu/docs`

Completion menu docs

![CompletionMenu — Completion menu docs](/component-previews/completion-menu-docs.svg)

### `completion-menu/edge`

Completion menu edge flip

![CompletionMenu — Completion menu edge flip](/component-previews/completion-menu-edge.svg)

### `completion-menu/loading`

Completion menu loading

![CompletionMenu — Completion menu loading](/component-previews/completion-menu-loading.svg)

### `completion-menu/narrow`

Completion menu narrow

![CompletionMenu — Completion menu narrow](/component-previews/completion-menu-narrow.svg)

### `completion-menu/unicode`

Completion menu Unicode

![CompletionMenu — Completion menu Unicode](/component-previews/completion-menu-unicode.svg)

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::style::DesignSystem;
use termrock::widgets::{
    CompletionCandidate, CompletionMenu, CompletionMenuSize, CompletionMenuState,
    open_completion_overlay,
};

let system = DesignSystem::default();
let candidates = [
    CompletionCandidate::new("select", "SELECT")
        .kind("keyword")
        .group("Keywords")
        .documentation("Select rows."),
];
let mut state = CompletionMenuState::new(Some("select"));
state.set_commit_characters("().");
// async: let gen = state.begin_async(); … state.apply_results(gen, &candidates);
let bounds = Rect::new(0, 0, 80, 24);
let anchor = Rect::new(12, 6, 1, 1);
let _ = open_completion_overlay(&mut stack, bounds, anchor, CompletionMenuSize::default(), Some("editor"));
CompletionMenu::new(&candidates, &system, bounds, anchor).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `completion-menu/basic` — Completion menu
- `completion-menu/docs` — Completion menu docs
- `completion-menu/edge` — Completion menu edge flip
- `completion-menu/loading` — Completion menu loading
- `completion-menu/narrow` — Completion menu narrow
- `completion-menu/unicode` — Completion menu Unicode
