---
title: QuestionFlow
description: 'Multi-step interview flow with option selection outcomes.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `question-flow/basic`

Question flow

![QuestionFlow — Question flow](/component-previews/question-flow-basic.svg)

### `question-flow/narrow`

Narrow QuestionFlow

![QuestionFlow — Narrow QuestionFlow](/component-previews/question-flow-narrow.svg)

### `question-flow/unicode`

Unicode QuestionFlow

![QuestionFlow — Unicode QuestionFlow](/component-previews/question-flow-unicode.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{QuestionFlow, QuestionOption, QuestionStep}};

let tokens = DesignTokens::default();
let opts = [QuestionOption { id: "y", label: "Yes" }];
let steps = [QuestionStep { id: "q1", prompt: "Continue?", options: &opts, required: true }];
let flow = QuestionFlow::new(&steps, &tokens);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `question-flow/basic` — Question flow
- `question-flow/narrow` — Narrow QuestionFlow
- `question-flow/unicode` — Unicode QuestionFlow
