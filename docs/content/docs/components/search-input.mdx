---
title: SearchInput
description: 'A search field with query, status, clear, history, filter chips, and host-polled debounce signals.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `search-input/basic`

Search input

![SearchInput — Search input](/component-previews/search-input-basic.svg)

### `search-input/searching`

Search searching

![SearchInput — Search searching](/component-previews/search-input-searching.svg)

### `search-input/filters`

Search filters

![SearchInput — Search filters](/component-previews/search-input-filters.svg)

### `search-input/empty`

Search empty

![SearchInput — Search empty](/component-previews/search-input-empty.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{SearchInput, SearchInputState, SearchStatus};

let system = DesignSystem::default();
let mut state = SearchInputState::new().with_query("table");
state.set_focused(true);
// each frame: state.poll(tick) → DebouncedQuery
let _ = SearchInput::new(&system)
    .status(SearchStatus::Results { count: 12 })
    .placeholder("Search…");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `search-input/basic` — Search input
- `search-input/searching` — Search searching
- `search-input/filters` — Search filters
- `search-input/empty` — Search empty
