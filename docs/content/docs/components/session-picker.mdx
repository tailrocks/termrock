---
title: SessionPicker
description: 'Session list picker over borrowed session projections.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `session-picker/basic`

Session picker

![SessionPicker — Session picker](/component-previews/session-picker-basic.svg)

### `session-picker/narrow`

Narrow SessionPicker

![SessionPicker — Narrow SessionPicker](/component-previews/session-picker-narrow.svg)

### `session-picker/unicode`

Unicode SessionPicker

![SessionPicker — Unicode SessionPicker](/component-previews/session-picker-unicode.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{SessionItem, SessionPicker}};

let tokens = DesignTokens::default();
let sessions = [SessionItem { id: "s1", title: "Main", meta: None }];
let picker = SessionPicker::new(&sessions, &tokens);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `session-picker/basic` — Session picker
- `session-picker/narrow` — Narrow SessionPicker
- `session-picker/unicode` — Unicode SessionPicker
