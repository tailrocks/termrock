---
title: SessionPicker
description: 'Agent session selector — create, resume, search, rename, archive, delete.'
---
## Live terminal (Ghostty-class)

One focused interactive surface. Variants are listed in **Stories** — interactivity
replaces multi-image galleries.

<TerminalPreview story="session-picker/basic" interactive caption="SessionPicker · session-picker/basic — focus surface; keys/click for states" />

## `session-picker/basic`

### `session-picker/confirm`

### `session-picker/narrow`

## Usage

```rust
use termrock::widgets::{
    example_sessions, SessionPicker, SessionPickerOutcome, SessionPickerState,
};

let mut state = SessionPickerState::new();
state.set_sessions(example_sessions());
SessionPicker::new(&system).paint(area, buf, &mut state);

match state.handle_key(key) {
    SessionPickerOutcome::Opened { id } => { let _ = id; }
    SessionPickerOutcome::Cancelled => { /* draft preserved */ }
    _ => {}
}
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

| Story | Title | Focus |
|---|---|---|
| `session-picker/basic` | Session picker | **Primary (live preview)** |
| `session-picker/search` | SessionPicker search | search |
| `session-picker/confirm` | SessionPicker delete confirm | confirm |
| `session-picker/empty` | SessionPicker empty | empty |
| `session-picker/narrow` | Narrow SessionPicker | narrow |
| `session-picker/unicode` | Unicode SessionPicker | unicode |
