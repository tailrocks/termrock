---
title: VirtualGrid
description: 'A two-axis virtualized grid whose paint cost is bounded by caller-projected resident rows.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `virtual-grid/basic`

Virtual grid

![VirtualGrid — Virtual grid](/component-previews/virtual-grid-basic.svg)

### `virtual-grid/million`

Virtual grid million-row window

![VirtualGrid — Virtual grid million-row window](/component-previews/virtual-grid-million.svg)

### `virtual-grid/narrow`

Virtual grid narrow

![VirtualGrid — Virtual grid narrow](/component-previews/virtual-grid-narrow.svg)

### `virtual-grid/unicode`

Virtual grid Unicode

![VirtualGrid — Virtual grid Unicode](/component-previews/virtual-grid-unicode.svg)

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{GridCell, GridColumn, GridRow, VirtualGrid, VirtualGridState}};

let system = DesignSystem::default();
let columns = [
  GridColumn::fixed("id", "ID", 8),
  GridColumn::min("name", "Name", 16),
];
let cells = [GridCell::text("42"), GridCell::text("termrock")];
let rows = [GridRow::new("row-42", 42, &cells)];
let grid = VirtualGrid::new(&columns, &rows, &system).total_rows(1_000_000);
let state = VirtualGridState::<&str, &str>::new();
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `virtual-grid/basic` — Virtual grid
- `virtual-grid/million` — Virtual grid million-row window
- `virtual-grid/narrow` — Virtual grid narrow
- `virtual-grid/unicode` — Virtual grid Unicode
