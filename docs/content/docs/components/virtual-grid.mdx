---
title: VirtualGrid
description: 'A two-axis virtualized grid whose paint cost is bounded by caller-projected resident rows.'
component: VirtualGrid
demo: virtual-grid/basic
interaction: scrolling-virtualization
actions:
  - "arrows move cell"
  - "PgUp/PgDn move viewport"
  - "Enter activate"
  - "Shift+arrows select range"
  - "wheel scroll"
  - "click cell"
expectedOutcomes:
  - "Navigation moves visible focus or selection and preserves it for the next event."
  - "The \"PgUp/PgDn move viewport\" action updates visible mounted state and reports its typed outcome."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "Scrolling changes the mounted viewport while preserving selection and surrounding page position."
source: crates/termrock/src/widgets/virtual_grid.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="virtual-grid/basic" interactive caption="VirtualGrid · Virtual grid — live shared Rust state" />

## Try it

- `arrows move cell`: Move through the real focus/selection model; the next action uses the new target.
- `PgUp/PgDn move viewport`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Enter activate`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Shift+arrows select range`: Move through the real focus/selection model; the next action uses the new target.
- `wheel scroll`: Scroll over the preview; its own viewport moves while the docs page stays put.
- `click cell`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `virtual-grid/basic` | Virtual grid | interactive |
| `virtual-grid/million` | Virtual grid million-row window | paint-only |
| `virtual-grid/narrow` | Virtual grid narrow | paint-only |
| `virtual-grid/unicode` | Virtual grid Unicode | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `virtual-grid/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn virtual_grid_basic(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    render_virtual_grid(frame, area, system, 20);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `virtual-grid/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `virtual-grid/basic` | Virtual grid | **Primary (live preview)** |
| `virtual-grid/million` | Virtual grid million-row window | million |
| `virtual-grid/narrow` | Virtual grid narrow | Narrow terminal |
| `virtual-grid/unicode` | Virtual grid Unicode | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/virtual_grid.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/virtual_grid.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `virtual-grid/basic`

## Seen in applications

<SeenInApplications component="VirtualGrid" />
