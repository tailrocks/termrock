---
title: VirtualGrid
description: 'A two-axis virtualized grid whose paint cost is bounded by caller-projected resident rows.'
component: VirtualGrid
demo: virtual-grid/basic
interaction: scrolling-virtualization
actions:
  - "arrows move cell"
  - "PgUp/PgDn move viewport"
  - "Enter activate"
  - "Shift+arrows select range"
  - "wheel scroll"
  - "click cell"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/virtual_grid.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="virtual-grid/basic" interactive caption="VirtualGrid · Virtual grid — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{GridCell, GridColumn, GridRow, VirtualGrid, VirtualGridState}};

let system = DesignSystem::default();
let columns = [
  GridColumn::fixed("id", "ID", 8),
  GridColumn::min("name", "Name", 16),
];
let cells = [GridCell::text("42"), GridCell::text("termrock")];
let rows = [GridRow::new("row-42", 42, &cells)];
let grid = VirtualGrid::new(&columns, &rows, &system).total_rows(1_000_000);
let state = VirtualGridState::<&str, &str>::new();
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `virtual-grid/basic` | Virtual grid | **Primary (live preview)** |
| `virtual-grid/million` | Virtual grid million-row window | million |
| `virtual-grid/narrow` | Virtual grid narrow | Narrow terminal |
| `virtual-grid/unicode` | Virtual grid Unicode | Unicode / wide glyphs |


## Try it

- `wheel to scroll`: dispatches through the public TermRock interaction API.
- `↑↓ navigate`: dispatches through the public TermRock interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `virtual-grid/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/virtual_grid.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/virtual_grid.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `virtual-grid/basic`
