---
title: Checkbox
description: 'Controlled checkbox with typed value-change outcomes.'
component: Checkbox
demo: checkbox/switch
interaction: activation
actions:
  - "Space/Enter toggle"
  - "click toggle"
  - "move pointer to hover"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "Pointer movement updates visible hover chrome without activating the control."
source: crates/termrock/src/widgets/controls.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="checkbox/switch" interactive caption="Checkbox · Checkbox and Switch — live shared Rust state" />

## Try it

- `Space/Enter toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `move pointer to hover`: Move the pointer over the target; hover chrome changes without activation.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `checkbox/description` | Checkbox description | paint-only |
| `checkbox/disabled` | Disabled checkbox | paint-only |
| `checkbox/indeterminate` | Checkbox indeterminate | paint-only |
| `checkbox/list` | Checkbox list | paint-only |
| `checkbox/narrow` | Narrow Checkbox | paint-only |
| `checkbox/states` | Checkbox states | paint-only |
| `checkbox/switch` | Checkbox and Switch | interactive |
| `checkbox/unicode` | Unicode Checkbox | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `checkbox/switch`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn checkbox_switch_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let tokens = system.clone().density(Density::default());
    let mut cb = CheckboxState::new(true);
    cb.set_focused(true);
    let _ = Checkbox::new("enable", "Enable", &tokens).paint(
        Rect::new(area.x, area.y, area.width, 1),
        frame.buffer_mut(),
        &mut cb,
    );
    let mut sw = SwitchState::new(false);
    sw.set_focused(false);
    let _ = Switch::new("dark", "Dark mode", &tokens).paint(
        Rect::new(area.x, area.y.saturating_add(1), area.width, 1),
        frame.buffer_mut(),
        &mut sw,
    );
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `checkbox/switch`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `checkbox/switch` | Checkbox and Switch | **Primary (live preview)** |
| `checkbox/description` | Checkbox description | description |
| `checkbox/disabled` | Disabled checkbox | Disabled |
| `checkbox/indeterminate` | Checkbox indeterminate | indeterminate |
| `checkbox/list` | Checkbox list | list |
| `checkbox/narrow` | Narrow Checkbox | Narrow terminal |
| `checkbox/states` | Checkbox states | states |
| `checkbox/unicode` | Unicode Checkbox | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/controls.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/controls.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `checkbox/switch`

## Seen in applications

<SeenInApplications component="Checkbox" />
