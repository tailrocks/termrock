---
title: StatusBar
description: 'A one-row collection of prioritized, interactive status slots.'
component: StatusBar
demo: status-bar/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/status_bar.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="status-bar/basic" interactive caption="StatusBar · Status bar — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `status-bar/basic` | Status bar | paint-only |
| `status-bar/minimal` | Status bar minimal | paint-only |
| `status-bar/narrow` | Narrow status bar | paint-only |
| `status-bar/rich` | Status bar rich | paint-only |
| `status-bar/transient` | Status bar transient | paint-only |
| `status-bar/unicode` | Unicode StatusBar | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `status-bar/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn status_bar(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::{StatusRegion, StatusSlot};
    let left = [StatusSlot::mode("mode", "NOR")
        .style(Style::new().reversed())
        .hover_style(Style::new().bold().reversed())];
    let center = [StatusSlot::focus_zone("focus", "main")];
    let right = [
        StatusSlot::selection("sel", "3/12")
            .style(Style::new().dim())
            .hover_style(Style::new().bold()),
        StatusSlot::shortcut("hint", "? help").region(StatusRegion::Right),
    ];
    let mut state = StatusBarState::default();
    frame.render_stateful_widget(
        &StatusBar::with_center(&left, &center, &right, system)
            .rich()
            .alpha(1.0),
        area,
        &mut state,
    );
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `status-bar/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `status-bar/basic` | Status bar | **Primary (live preview)** |
| `status-bar/minimal` | Status bar minimal | minimal |
| `status-bar/narrow` | Narrow status bar | Narrow terminal |
| `status-bar/rich` | Status bar rich | rich |
| `status-bar/transient` | Status bar transient | transient |
| `status-bar/unicode` | Unicode StatusBar | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/status_bar.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/status_bar.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `status-bar/basic`

## Seen in applications

<SeenInApplications component="StatusBar" />
