---
title: StatusBar
description: 'A one-row collection of prioritized, interactive status slots.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `status-bar/basic`

Status bar

![StatusBar — Status bar](/component-previews/status-bar-basic.svg)

### `status-bar/minimal`

Status bar minimal

![StatusBar — Status bar minimal](/component-previews/status-bar-minimal.svg)

### `status-bar/narrow`

Narrow status bar

![StatusBar — Narrow status bar](/component-previews/status-bar-narrow.svg)

### `status-bar/rich`

Status bar rich

![StatusBar — Status bar rich](/component-previews/status-bar-rich.svg)

### `status-bar/transient`

Status bar transient

![StatusBar — Status bar transient](/component-previews/status-bar-transient.svg)

### `status-bar/unicode`

Unicode StatusBar

![StatusBar — Unicode StatusBar](/component-previews/status-bar-unicode.svg)

## Usage

```rust
use ratatui_core::style::Style;
use termrock::{style::DesignSystem, widgets::{StatusBar, StatusBarState, StatusSlot}};

let system = DesignSystem::default();
let left = [StatusSlot { id: "mode", content: "NORMAL", priority: 10, min_width: 0, enabled: true, style: Style::new(), hover_style: None }];
let bar = StatusBar::new(&left, &[], &system);
let mut state = StatusBarState::<&str>::default();
state.hovered = Some("mode");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | covered |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `status-bar/basic` — Status bar
- `status-bar/minimal` — Status bar minimal
- `status-bar/narrow` — Narrow status bar
- `status-bar/rich` — Status bar rich
- `status-bar/transient` — Status bar transient
- `status-bar/unicode` — Unicode StatusBar
