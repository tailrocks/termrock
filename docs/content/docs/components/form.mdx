---
title: Form
description: 'A responsive form layout with stable focus, validation, and hit geometry.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

## Live terminal (Ghostty-class)

Click the surface and use **↑↓** (or click) to drive the same TermRock paint path
exported from the lookbook. Truecolor cells, monospace metrics, Ghostty-style chrome.

<TerminalPreview story="form/responsive" interactive caption="Interactive TermRock frame pack — not a static mock." />


### `form/responsive`

Responsive form

![Form — Responsive form](/component-previews/form-responsive.svg)

### `form/compact`

Form compact

![Form — Form compact](/component-previews/form-compact.svg)

### `form/narrow`

Narrow form

![Form — Narrow form](/component-previews/form-narrow.svg)

### `form/unicode`

Unicode Form

![Form — Unicode Form](/component-previews/form-unicode.svg)

### `form/validation`

Form validation

![Form — Form validation](/component-previews/form-validation.svg)

## Usage

```rust
use ratatui_core::text::Line;
use termrock::{style::DesignSystem, input::{KeyCode, KeyEvent, KeyModifiers}, widgets::{Form, FormField, FormSection, FormState}};

let system = DesignSystem::default();
let fields = [FormField::new("name", Line::from("Name"), Line::from("Ada")).required(true)];
let sections = [FormSection { title: Line::from("Profile"), fields: &fields }];
let form = Form::new(&sections, &system);
let mut state = FormState::new(Some("name"));
let outcome = state.handle_key(&sections, KeyEvent::new(KeyCode::Tab, KeyModifiers::NONE));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `form/compact` — Form compact
- `form/narrow` — Narrow form
- `form/responsive` — Responsive form
- `form/unicode` — Unicode Form
- `form/validation` — Form validation
