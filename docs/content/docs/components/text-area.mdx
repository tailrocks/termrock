---
title: TextArea
description: 'A multi-line grapheme-safe editor with normalized paste and a two-axis viewport.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `text-area/basic`

Text area

![TextArea — Text area](/component-previews/text-area-basic.svg)

### `text-area/empty`

Empty text area

![TextArea — Empty text area](/component-previews/text-area-empty.svg)

### `text-area/narrow`

Narrow text area

![TextArea — Narrow text area](/component-previews/text-area-narrow.svg)

### `text-area/scrolled`

Scrolled text area

![TextArea — Scrolled text area](/component-previews/text-area-scrolled.svg)

### `text-area/unicode`

Unicode text area

![TextArea — Unicode text area](/component-previews/text-area-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::{TextArea, TextAreaState, TextCursor}};

let theme = Theme::default();
let area = TextArea::new(&theme).title("Notes").placeholder("Write…");
let mut state = TextAreaState::new("first line
second line");
state.set_focused(true);
let valid = state.set_cursor(TextCursor { line: 1, byte: 0 });
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `text-area/basic` — Text area
- `text-area/empty` — Empty text area
- `text-area/narrow` — Narrow text area
- `text-area/scrolled` — Scrolled text area
- `text-area/unicode` — Unicode text area
