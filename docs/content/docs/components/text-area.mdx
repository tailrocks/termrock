---
title: TextArea
description: 'A multi-line grapheme-safe editor with selection, undo/redo, soft wrap, line numbers, and host clipboard/external-editor hooks.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `text-area/basic`

Text area

![TextArea — Text area](/component-previews/text-area-basic.svg)

### `text-area/empty`

Empty text area

![TextArea — Empty text area](/component-previews/text-area-empty.svg)

### `text-area/line-numbers`

Text area line numbers

![TextArea — Text area line numbers](/component-previews/text-area-line-numbers.svg)

### `text-area/narrow`

Narrow text area

![TextArea — Narrow text area](/component-previews/text-area-narrow.svg)

### `text-area/review`

Text area review

![TextArea — Text area review](/component-previews/text-area-review.svg)

### `text-area/scrolled`

Scrolled text area

![TextArea — Scrolled text area](/component-previews/text-area-scrolled.svg)

### `text-area/soft-wrap`

Text area soft wrap

![TextArea — Text area soft wrap](/component-previews/text-area-soft-wrap.svg)

### `text-area/unicode`

Unicode text area

![TextArea — Unicode text area](/component-previews/text-area-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{TextArea, TextAreaState, TextCursor, TextWrap};

let system = DesignSystem::default();
let area = TextArea::new(&system)
    .title("Notes")
    .placeholder("Write…")
    .line_numbers(true)
    .soft_wrap();
let mut state = TextAreaState::new("first line\nsecond line");
state.set_accepts_input(true);
state.set_wrap(TextWrap::Soft);
let valid = state.set_cursor(TextCursor { line: 1, byte: 0 });
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `text-area/basic` — Text area
- `text-area/empty` — Empty text area
- `text-area/line-numbers` — Text area line numbers
- `text-area/narrow` — Narrow text area
- `text-area/review` — Text area review
- `text-area/scrolled` — Scrolled text area
- `text-area/soft-wrap` — Text area soft wrap
- `text-area/unicode` — Unicode text area
