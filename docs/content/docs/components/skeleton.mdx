---
title: Skeleton
description: 'Low-noise structural placeholders (lines, rows, cards, tables, custom) when final layout is known; static by default, optional Full-motion pulse; ASCII/tiny-safe.'
component: Skeleton
demo: skeleton/basic
interaction: timed-state
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/skeleton.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="skeleton/basic" interactive caption="Skeleton · Skeleton lines — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Skeleton, SkeletonRecipe};

let system = DesignSystem::default();
let skeleton = Skeleton::new(4, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `skeleton/basic` | Skeleton lines | **Primary (live preview)** |
| `skeleton/ascii` | Skeleton ASCII | ASCII / low capability |
| `skeleton/card` | Skeleton card | card |
| `skeleton/narrow` | Narrow Skeleton | Narrow terminal |
| `skeleton/table` | Skeleton table | table |
| `skeleton/tiny` | Skeleton tiny | Tiny viewport |
| `skeleton/unicode` | Unicode Skeleton | Unicode / wide glyphs |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `skeleton/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/skeleton.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/skeleton.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `skeleton/basic`
