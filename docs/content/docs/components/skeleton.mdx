---
title: Skeleton
description: 'Low-noise structural placeholders (lines, rows, cards, tables, custom) when final layout is known; static by default, optional Full-motion pulse; ASCII/tiny-safe.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="skeleton/basic" interactive caption="Skeleton · Skeleton lines — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Skeleton, SkeletonRecipe};

let system = DesignSystem::default();
let skeleton = Skeleton::new(4, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `skeleton/basic` | Skeleton lines | **Primary (live preview)** |
| `skeleton/ascii` | Skeleton ASCII | ASCII / low capability |
| `skeleton/card` | Skeleton card | card |
| `skeleton/narrow` | Narrow Skeleton | Narrow terminal |
| `skeleton/table` | Skeleton table | table |
| `skeleton/tiny` | Skeleton tiny | Tiny viewport |
| `skeleton/unicode` | Unicode Skeleton | Unicode / wide glyphs |
