---
title: Drawer
description: 'Edge-mounted secondary surface for inspectors, task rails, filters, details — left/right/top/bottom, modal/non-modal, resizable depth, focus trap, opener restore, nested overlays, compact handle, fullscreen promotion; preserves host selection/scroll.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="drawer/basic" interactive caption="Drawer · Drawer right — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    Drawer, DrawerState, DrawerEdge, DrawerModality, open_drawer_overlay,
};

let system = DesignSystem::default();
let mut state = DrawerState::new(); // or ::sheet() / ::non_modal()
state.set_edge(DrawerEdge::Right);
let _ = state.open_on_stack(&mut stack, bounds, Some("main"));
// Host keeps list selection/scroll of underlying view.
Drawer::new("Inspector", &system)
    .footer(Some("esc · [ ] resize"))
    .paint(entry.rect, buf, &mut state);
// Host paints domain content into state.body_area().
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `drawer/basic` | Drawer right | **Primary (live preview)** |
| `drawer/left` | Drawer left | left |
| `drawer/narrow` | Narrow Drawer | Narrow terminal |
| `drawer/non-modal` | Drawer non-modal | non modal |
| `drawer/unicode` | Unicode Drawer | Unicode / wide glyphs |
