---
title: Drawer
description: 'Edge-mounted secondary surface for inspectors, task rails, filters, details — left/right/top/bottom, modal/non-modal, resizable depth, focus trap, opener restore, nested overlays, compact handle, fullscreen promotion; preserves host selection/scroll.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `drawer/basic`

Drawer right

![Drawer — Drawer right](/component-previews/drawer-basic.svg)

### `drawer/left`

Drawer left

![Drawer — Drawer left](/component-previews/drawer-left.svg)

### `drawer/narrow`

Narrow Drawer

![Drawer — Narrow Drawer](/component-previews/drawer-narrow.svg)

### `drawer/non-modal`

Drawer non-modal

![Drawer — Drawer non-modal](/component-previews/drawer-non-modal.svg)

### `drawer/unicode`

Unicode Drawer

![Drawer — Unicode Drawer](/component-previews/drawer-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    Drawer, DrawerState, DrawerEdge, DrawerModality, open_drawer_overlay,
};

let system = DesignSystem::default();
let mut state = DrawerState::new(); // or ::sheet() / ::non_modal()
state.set_edge(DrawerEdge::Right);
let _ = state.open_on_stack(&mut stack, bounds, Some("main"));
// Host keeps list selection/scroll of underlying view.
Drawer::new("Inspector", &system)
    .footer(Some("esc · [ ] resize"))
    .paint(entry.rect, buf, &mut state);
// Host paints domain content into state.body_area().
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `drawer/basic` — Drawer right
- `drawer/left` — Drawer left
- `drawer/narrow` — Narrow Drawer
- `drawer/non-modal` — Drawer non-modal
- `drawer/unicode` — Unicode Drawer
