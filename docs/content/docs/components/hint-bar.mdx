---
title: HintBar
description: 'A wrapping row of prioritized keyboard hints with semantic styling.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `hint-bar/wrapped`

Hint bar

![HintBar — Hint bar](/component-previews/hint-bar-wrapped.svg)

### `hint-bar/narrow`

Narrow HintBar

![HintBar — Narrow HintBar](/component-previews/hint-bar-narrow.svg)

### `hint-bar/unicode`

Unicode HintBar

![HintBar — Unicode HintBar](/component-previews/hint-bar-unicode.svg)

## Usage

```rust
use ratatui_core::{buffer::Buffer, layout::Rect, widgets::Widget};
use termrock::{style::DesignSystem, widgets::{Hint, HintBar}};

let system = DesignSystem::default();
let hints = [Hint { chord: "Enter", label: "open", priority: 0, visible: true }];
let bar = HintBar::new(&hints, &system).separator(" · ");
let area = Rect::new(0, 0, 40, 2);
bar.render(area, &mut Buffer::empty(area));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `hint-bar/narrow` — Narrow HintBar
- `hint-bar/unicode` — Unicode HintBar
- `hint-bar/wrapped` — Hint bar
