---
title: DesignInspector
description: 'Studio debug strip for focus, layer, density, and color capability.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `design-inspector/basic`

Design inspector

![DesignInspector — Design inspector](/component-previews/design-inspector-basic.svg)

### `design-inspector/narrow`

Narrow DesignInspector

![DesignInspector — Narrow DesignInspector](/component-previews/design-inspector-narrow.svg)

### `design-inspector/unicode`

Unicode DesignInspector

![DesignInspector — Unicode DesignInspector](/component-previews/design-inspector-unicode.svg)

## Usage

```rust
use termrock::{Theme, style::ColorCapability, widgets::{DesignInspector, DesignInspectorFrame}};

let theme = Theme::default();
let frame = DesignInspectorFrame {
    focused: Some("list"),
    layer: Some("root"),
    capability: ColorCapability::Truecolor,
    density: "comfortable",
    layers: &[],
    recipes: &[],
    selection_chrome: "gutter",
};
let _inspector = DesignInspector::new(frame, &theme);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `design-inspector/basic` — Design inspector
- `design-inspector/narrow` — Narrow DesignInspector
- `design-inspector/unicode` — Unicode DesignInspector
