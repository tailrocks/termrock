---
title: KeyboardHelp
description: 'Contextual generated keyboard help from live keymaps, zones, overlays, and semantic actions — footer or searchable modal, never stale hardcoded shortcuts.'
component: KeyboardHelp
demo: keyboard-help/footer
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/keyboard_help.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="keyboard-help/footer" interactive caption="KeyboardHelp · KeyboardHelp footer — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{KeyboardHelp, KeyboardHelpState, example_help_entries};

let system = DesignSystem::default();
let entries = example_help_entries(&system);
let mut state = KeyboardHelpState::new();
KeyboardHelp::new(&entries, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `keyboard-help/footer` | KeyboardHelp footer | **Primary (live preview)** |
| `keyboard-help/ascii` | KeyboardHelp ASCII | ASCII / low capability |
| `keyboard-help/modal` | KeyboardHelp modal | modal |
| `keyboard-help/search` | KeyboardHelp search | Search / filter |
| `keyboard-help/tiny` | KeyboardHelp tiny | Tiny viewport |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `keyboard-help/footer`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/keyboard_help.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/keyboard_help.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `keyboard-help/footer`
