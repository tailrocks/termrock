---
title: KeyboardHelp
description: 'Contextual generated keyboard help from live keymaps, zones, overlays, and semantic actions — footer or searchable modal, never stale hardcoded shortcuts.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `keyboard-help/footer`

KeyboardHelp footer

![KeyboardHelp — KeyboardHelp footer](/component-previews/keyboard-help-footer.svg)

### `keyboard-help/ascii`

KeyboardHelp ASCII

![KeyboardHelp — KeyboardHelp ASCII](/component-previews/keyboard-help-ascii.svg)

### `keyboard-help/modal`

KeyboardHelp modal

![KeyboardHelp — KeyboardHelp modal](/component-previews/keyboard-help-modal.svg)

### `keyboard-help/search`

KeyboardHelp search

![KeyboardHelp — KeyboardHelp search](/component-previews/keyboard-help-search.svg)

### `keyboard-help/tiny`

KeyboardHelp tiny

![KeyboardHelp — KeyboardHelp tiny](/component-previews/keyboard-help-tiny.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{KeyboardHelp, KeyboardHelpState, example_help_entries};

let system = DesignSystem::default();
let entries = example_help_entries(&system);
let mut state = KeyboardHelpState::new();
KeyboardHelp::new(&entries, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `keyboard-help/ascii` — KeyboardHelp ASCII
- `keyboard-help/footer` — KeyboardHelp footer
- `keyboard-help/modal` — KeyboardHelp modal
- `keyboard-help/search` — KeyboardHelp search
- `keyboard-help/tiny` — KeyboardHelp tiny
