---
title: ModeRibbon
description: 'Product-neutral agent mode strip with selection outcomes.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![ModeRibbon — Mode ribbon](/component-previews/mode-ribbon-basic.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{ModeRibbon, WorkbenchMode}};

let tokens = DesignTokens::default();
let modes = [WorkbenchMode { id: "plan", label: "Plan", active: true, enabled: true }];
let ribbon = ModeRibbon::new(&modes, &tokens);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | not-applicable |
| Narrow terminal | not-applicable |

## Stories

- `mode-ribbon/basic` — Mode ribbon
