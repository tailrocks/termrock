---
title: ModeRibbon
description: 'Product-neutral agent mode strip with selection outcomes.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `mode-ribbon/basic`

Mode ribbon

![ModeRibbon — Mode ribbon](/component-previews/mode-ribbon-basic.svg)

### `mode-ribbon/narrow`

Narrow ModeRibbon

![ModeRibbon — Narrow ModeRibbon](/component-previews/mode-ribbon-narrow.svg)

### `mode-ribbon/unicode`

Unicode ModeRibbon

![ModeRibbon — Unicode ModeRibbon](/component-previews/mode-ribbon-unicode.svg)

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{ModeRibbon, WorkbenchMode}};

let system = DesignSystem::default();
let modes = [WorkbenchMode { id: "plan", label: "Plan", active: true, enabled: true }];
let ribbon = ModeRibbon::new(&modes, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `mode-ribbon/basic` — Mode ribbon
- `mode-ribbon/narrow` — Narrow ModeRibbon
- `mode-ribbon/unicode` — Unicode ModeRibbon
