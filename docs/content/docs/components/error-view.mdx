---
title: ErrorView / ErrorState
description: 'Structured recoverable failure — summary, details (collapsed), recovery, retry safety.'
---

## Previews

### `error-view/basic` / `error-state/network`

Network failure with recovery

![ErrorState — network](/component-previews/error-view-basic.svg)

### `error-state/validation`

Validation (work preserved)

![ErrorState — validation](/component-previews/error-state-validation.svg)

### `error-state/permission`

Permission denied

![ErrorState — permission](/component-previews/error-state-permission.svg)

### `error-state/details`

Technical details expanded

![ErrorState — details](/component-previews/error-state-details.svg)

### `error-state/inline`

Inline recipe

![ErrorState — inline](/component-previews/error-state-inline.svg)

### `error-state/dialog`

Dialog recipe

![ErrorState — dialog](/component-previews/error-state-dialog.svg)

### `error-state/fullscreen`

Full-screen crash recovery

![ErrorState — fullscreen](/component-previews/error-state-fullscreen.svg)

### `error-view/narrow`

Narrow ErrorView

![ErrorView — narrow](/component-previews/error-view-narrow.svg)

### `error-view/unicode`

Unicode ErrorView

![ErrorView — unicode](/component-previews/error-view-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    ErrorState, ErrorKind, Recovery, RecoveryAction, RetrySafety,
    ErrorStateState, example_error_network,
};

let system = DesignSystem::default();
let err = ErrorState::new("Request failed", &system)
    .kind(ErrorKind::Network)
    .explanation("Could not reach the API")
    .technical("timeout after 30s")
    .source("jobs-service")
    .recovery(
        Recovery::none()
            .with_retry(RecoveryAction::with_shortcut("Retry", "r"))
            .with_retry_safety(RetrySafety::Safe)
            .with_copy_diagnostics(true)
            .with_work_preserved(true, Some("Draft retained")),
    );

let mut state = ErrorStateState::new(); // technical collapsed
err.paint_with_state(area, buf, &mut state);
// ErrorView is a type alias for ErrorState
```

## Rules

1. Prefer ErrorState for hard failures; EmptyState for zero-data.
2. Technical details stay collapsed until toggled (`d` / focus ToggleDetails).
3. State retry safety (Safe / Unsafe / Unknown); paint the cue when retry exists.
4. Preserve user work when possible and say so.
5. Retry is dominant and never destructive-by-default.
6. Host owns clipboard on `CopyDiagnostics` via `diagnostics_text()`.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered (Tab, Enter, r/c/d, recovery) |
| Mouse | covered (action bands, details) |
| Focus | covered (when recovery/details present) |
| Non-color cues | covered (glyph + labels + safety line) |
| Unicode | covered |
| Narrow terminal | covered (inline contraction) |

## Stories

- `error-view/basic`, `narrow`, `unicode`
- `error-state/network`, `validation`, `permission`, `details`, `inline`, `dialog`, `fullscreen`
