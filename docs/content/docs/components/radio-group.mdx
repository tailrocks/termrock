---
title: RadioGroup
description: 'Single-choice group — roving focus, FollowFocus policy, descriptions.'
component: RadioGroup
demo: radio-group/basic
interaction: selection-navigation
actions:
  - "↑↓ choose"
  - "typeahead"
  - "Enter/Space select"
  - "click option"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "Editing changes the visible Unicode-safe draft and reports the public typed edit outcome."
source: crates/termrock/src/widgets/controls.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="radio-group/basic" interactive caption="RadioGroup · RadioGroup basic — live shared Rust state" />

## Try it

- `↑↓ choose`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `typeahead`: Focus the editable field, then type or paste Unicode text; the draft remains visible.
- `Enter/Space select`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click option`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `radio-group/badges` | RadioGroup badges | paint-only |
| `radio-group/basic` | RadioGroup basic | interactive |
| `radio-group/disabled` | RadioGroup disabled option | paint-only |
| `radio-group/horizontal` | RadioGroup horizontal | paint-only |
| `radio-group/narrow` | Narrow RadioGroup | paint-only |
| `radio-group/unicode` | Unicode RadioGroup | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `radio-group/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn radio_group_basic_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let options = [
        RadioOption::new("plan", "Plan").description("Read-only analysis"),
        RadioOption::new("build", "Build").description("Apply edits with approval"),
        RadioOption::new("ask", "Ask").description("Questions only"),
    ];
    let mut state = RadioState::new(Some("build"));
    state.set_surface_focused(true);
    let _ = RadioGroup::new(&options, system)
        .legend("Workbench mode")
        .paint(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `radio-group/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

| Story | Title | Focus |
|---|---|---|
| `radio-group/basic` | RadioGroup basic | **Primary (live preview)** |
| `radio-group/horizontal` | RadioGroup horizontal | horizontal |
| `radio-group/disabled` | RadioGroup disabled option | disabled |
| `radio-group/badges` | RadioGroup badges | badges |
| `radio-group/narrow` | Narrow RadioGroup | narrow |
| `radio-group/unicode` | Unicode RadioGroup | unicode |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/controls.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/controls.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `radio-group/basic`

## Seen in applications

<SeenInApplications component="RadioGroup" />
