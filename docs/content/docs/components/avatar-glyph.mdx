---
title: AvatarGlyph
description: "Two-cell initials avatar."
component: AvatarGlyph
demo: avatar-glyph/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/identity.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="avatar-glyph/basic" interactive caption="AvatarGlyph · AvatarGlyph initials — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `avatar-glyph/basic` | AvatarGlyph initials | paint-only |
| `avatar-glyph/compact` | AvatarGlyph compact | paint-only |
| `avatar-glyph/narrow` | Narrow AvatarGlyph | paint-only |
| `avatar-glyph/no-color` | AvatarGlyph no-color | paint-only |
| `avatar-glyph/presence` | AvatarGlyph presence | paint-only |
| `avatar-glyph/unicode` | Unicode AvatarGlyph | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `avatar-glyph/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn avatar_glyph_basic_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let chunks = Layout::vertical([Constraint::Length(1); 3]).split(area);
    let _ = AvatarGlyph::new("Ada Lovelace", system)
        .size(AvatarSize::Normal)
        .paint(chunks[0], frame.buffer_mut());
    let _ = AvatarGlyph::new("termrock", system)
        .role(IdentityRole::Agent)
        .size(AvatarSize::Normal)
        .paint(chunks[1], frame.buffer_mut());
    let _ = AvatarGlyph::new("svc", system)
        .role(IdentityRole::Service)
        .role_glyph()
        .size(AvatarSize::Normal)
        .paint(chunks[2], frame.buffer_mut());
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `avatar-glyph/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `avatar-glyph/basic` | AvatarGlyph initials | **Primary (live preview)** |
| `avatar-glyph/compact` | AvatarGlyph compact | Compact density |
| `avatar-glyph/narrow` | Narrow AvatarGlyph | Narrow terminal |
| `avatar-glyph/no-color` | AvatarGlyph no-color | no color |
| `avatar-glyph/presence` | AvatarGlyph presence | presence |
| `avatar-glyph/unicode` | Unicode AvatarGlyph | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/identity.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/identity.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `avatar-glyph/basic`

## Seen in applications

<SeenInApplications component="AvatarGlyph" />
