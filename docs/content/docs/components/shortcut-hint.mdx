---
title: ShortcutHint
description: "Footer form derived from a Keymap binding."
component: ShortcutHint
demo: shortcut-hint/footer
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/kbd.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="shortcut-hint/footer" interactive caption="ShortcutHint · ShortcutHint footer — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `shortcut-hint/footer` | ShortcutHint footer | paint-only |
| `shortcut-hint/inline` | ShortcutHint inline docs | paint-only |
| `shortcut-hint/narrow` | ShortcutHint narrow | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `shortcut-hint/footer`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn shortcut_hint_footer_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::input::KeyCode;
    use termrock::keymap::{KeyBinding, KeyChord, Keymap, Visibility};
    use termrock::widgets::ShortcutHint;
    #[derive(Clone, Copy, PartialEq)]
    enum A {
        Save,
        Quit,
    }
    let map = Keymap::from_owned(vec![
        KeyBinding::owned(
            vec![KeyChord::ctrl(KeyCode::Char('s'))],
            A::Save,
            Some("Save".into()),
            Visibility::Shown,
            None,
        ),
        KeyBinding::owned(
            vec![KeyChord::ctrl(KeyCode::Char('q'))],
            A::Quit,
            Some("Quit".into()),
            Visibility::Shown,
            None,
        ),
    ]);
    let chunks = Layout::vertical([Constraint::Length(1), Constraint::Length(1)]).split(area);
    if let Some(h) = ShortcutHint::for_action(&map, A::Save, system) {
        h.footer().paint(chunks[0], frame.buffer_mut());
    }
    if let Some(h) = ShortcutHint::for_action(&map, A::Quit, system) {
        h.footer().paint(chunks[1], frame.buffer_mut());
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `shortcut-hint/footer`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `shortcut-hint/footer` | ShortcutHint footer | **Primary (live preview)** |
| `shortcut-hint/inline` | ShortcutHint inline docs | inline |
| `shortcut-hint/narrow` | ShortcutHint narrow | Narrow terminal |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/kbd.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/kbd.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `shortcut-hint/footer`

## Seen in applications

<SeenInApplications component="ShortcutHint" />
