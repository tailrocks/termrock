---
title: TerminalCellGrid
description: 'A clipped, stale-safe projection from emulator-owned terminal cells into a Ratatui buffer.'
component: TerminalCellGrid
demo: terminal-cell-grid/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/terminal_cell_grid.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="terminal-cell-grid/basic" interactive caption="TerminalCellGrid · TerminalCellGrid basic — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

`TerminalCellGrid` is stateless and passive. A host implements
`TerminalCellSource` for its snapshot or viewport type and returns neutral
`TerminalCell` values. Rendering emits no effects or outcomes.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `terminal-cell-grid/basic` | TerminalCellGrid basic | paint-only |
| `terminal-cell-grid/narrow` | TerminalCellGrid narrow | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `terminal-cell-grid/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn terminal_cell_grid_story(frame: &mut Frame<'_>, area: Rect, _system: &DesignSystem) {
    struct StoryTerminalCells<'a> {
        cells: &'a [TerminalCell<'a>],
        columns: u16,
    }
    impl TerminalCellSource for StoryTerminalCells<'_> {
        fn size(&self) -> (u16, u16) {
            let rows = (self.cells.len() as u16).div_ceil(self.columns.max(1));
            (rows, self.columns)
        }
        fn cell(&self, row: u16, column: u16) -> Option<TerminalCell<'_>> {
            self.cells
                .get(usize::from(row) * usize::from(self.columns) + usize::from(column))
                .copied()
        }
    }
    let green = Style::new().fg(Color::Green);
    let cells = [
        TerminalCell::new("$", green.bold()),
        TerminalCell::new(" ", Style::new()),
        TerminalCell::new("c", Style::new().fg(Color::White)),
        TerminalCell::new("a", Style::new().fg(Color::White)),
        TerminalCell::new("r", Style::new().fg(Color::White)),
        TerminalCell::new("g", Style::new().fg(Color::White)),
        TerminalCell::new("o", Style::new().fg(Color::White)),
        TerminalCell::new(" ", Style::new()),
        TerminalCell::new("✓", green),
        TerminalCell::new(" ", Style::new()),
        TerminalCell::new("界", Style::new().fg(Color::Yellow)).diff(
            ratatui::buffer::CellDiffOption::ForcedWidth(NonZeroU16::new(2).unwrap()),
        ),
        TerminalCell::new(" ", Style::new()),
        TerminalCell::new("r", Style::new().fg(Color::Green)),
        TerminalCell::new("e", Style::new().fg(Color::Green)),
        TerminalCell::new("a", Style::new().fg(Color::Green)),
        TerminalCell::new("d", Style::new().fg(Color::Green)),
    ];
    let source = StoryTerminalCells {
        cells: &cells,
        columns: 8,
    };
    frame.render_widget(TerminalCellGrid::new(&source), area);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not retain or clone an emulator grid inside the widget; borrow one frame.
- Do not return whole strings as one cell; return one terminal grapheme per cell.
- Represent a wide glyph with one forced-width lead plus ordinary blank tail cells.
- Do not use `Skip` to preserve prior-frame content; projection is complete each frame.

## Test recipe

Implement a two-row `TerminalCellSource`, render it into a larger pre-filled
buffer, and assert exact symbols, colors, modifiers, forced width, clipping,
and cleared cells beyond the source bounds.

## Stories

| Story | Title | Focus |
|---|---|---|
| `terminal-cell-grid/basic` | TerminalCellGrid basic | **Primary (live preview)** |
| `terminal-cell-grid/narrow` | TerminalCellGrid narrow | Narrow clipping |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/terminal_cell_grid.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/terminal_cell_grid.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `terminal-cell-grid/basic`

## Seen in applications

<SeenInApplications component="TerminalCellGrid" />
