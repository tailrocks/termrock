---
title: ThinkingBlock
description: 'Collapsible thinking/reasoning chrome.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="thinking-block/basic" interactive caption="ThinkingBlock · Thinking block — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::ThinkingBlock};

let system = DesignSystem::default();
let block = ThinkingBlock::new("Planning", &system).expanded(true).body("Details");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `thinking-block/basic` | Thinking block | **Primary (live preview)** |
| `thinking-block/narrow` | Narrow ThinkingBlock | Narrow terminal |
| `thinking-block/unicode` | Unicode ThinkingBlock | Unicode / wide glyphs |
