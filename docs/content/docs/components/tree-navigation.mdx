---
title: TreeNavigation
description: 'Hierarchical route navigation with expansion, lazy children, typeahead, active ancestors, and route distinct from focus. Distinct from data Tree.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `tree-navigation/project`

TreeNavigation project

![TreeNavigation — TreeNavigation project](/component-previews/tree-navigation-project.svg)

### `tree-navigation/docs`

TreeNavigation docs

![TreeNavigation — TreeNavigation docs](/component-previews/tree-navigation-docs.svg)

### `tree-navigation/narrow`

TreeNavigation narrow

![TreeNavigation — TreeNavigation narrow](/component-previews/tree-navigation-narrow.svg)

### `tree-navigation/schema`

TreeNavigation schema

![TreeNavigation — TreeNavigation schema](/component-previews/tree-navigation-schema.svg)

### `tree-navigation/settings`

TreeNavigation settings

![TreeNavigation — TreeNavigation settings](/component-previews/tree-navigation-settings.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{TreeNavigation, TreeNavigationState, example_project_tree};

let system = DesignSystem::default();
let nodes = example_project_tree();
let mut state = TreeNavigationState::new(Some("main"));
state.set_focused(true);
state.reconcile_route(&nodes);
let _ = TreeNavigation::new(&nodes, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `tree-navigation/docs` — TreeNavigation docs
- `tree-navigation/narrow` — TreeNavigation narrow
- `tree-navigation/project` — TreeNavigation project
- `tree-navigation/schema` — TreeNavigation schema
- `tree-navigation/settings` — TreeNavigation settings
