---
title: TreeNavigation
description: 'Hierarchical route navigation with expansion, lazy children, typeahead, active ancestors, and route distinct from focus. Distinct from data Tree.'
component: TreeNavigation
demo: tree-navigation/project
interaction: selection-navigation
actions:
  - "↑↓ select"
  - "←→ collapse/expand"
  - "Enter activate"
  - "/ filter"
  - "click row"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/tree_navigation.rs
---


## Live terminal (Ghostty-class)

This preview mounts one persistent Rust component instance. Use the listed keyboard and pointer actions; each accepted action updates the same state and reports its typed outcome.

<TerminalPreview story="tree-navigation/project" interactive caption="TreeNavigation · TreeNavigation project — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{TreeNavigation, TreeNavigationState, example_project_tree};

let system = DesignSystem::default();
let nodes = example_project_tree();
let mut state = TreeNavigationState::new(Some("main"));
state.set_focused(true);
state.reconcile_route(&nodes);
let _ = TreeNavigation::new(&nodes, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `tree-navigation/project` | TreeNavigation project | **Primary (live preview)** |
| `tree-navigation/docs` | TreeNavigation docs | docs |
| `tree-navigation/narrow` | TreeNavigation narrow | Narrow terminal |
| `tree-navigation/schema` | TreeNavigation schema | schema |
| `tree-navigation/settings` | TreeNavigation settings | settings |


## Try it

- Use the actions shown beside the terminal; mouse, keyboard, paste, drag, and resize are forwarded only when the component supports them.
- Confirm the visual state and typed outcome, then use **Reset** to mount a fresh instance.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `tree-navigation/project`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/tree_navigation.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/tree_navigation.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `tree-navigation/project`

## Migrated handbook guidance

# TreeNavigation

**Purpose.** Project explorers, schema browsers, settings trees, documentation
outlines. **Not** the multi-select data [`Tree`](/docs/components/tree) widget.


## Route ≠ focus

- `route()` — active destination (survives filter when still in projection)
- `focus()` — keyboard cursor
- Enter on routeable leaf → `RouteChanged`

## Arrows

- **←** collapse or parent  
- **→** expand / lazy load / first child  
- **↑/↓** move focus  

## Lazy children

`LazyLoadRequested { id, generation }` → host loads → `apply_lazy_result` + new
projection → `reconcile_route` keeps selection when possible.

## Examples

`example_project_tree` · `example_schema_tree` · `example_settings_tree` ·
`example_docs_tree`

## Related

Tree · Sidebar · NavigationList · migration `0154`
