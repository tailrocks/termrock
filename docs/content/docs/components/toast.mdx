---
title: Toast
description: 'Transient notifications with priority, queue/dedup/replace, timeout pause, progress/undo kinds, and missed-item archive for NotificationCenter — never steals keyboard focus.'
component: Toast
demo: toast/success
interaction: timed-state
actions:
  - "Enter show"
  - "click Show toast"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/toast.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="toast/success" interactive caption="Toast · Toast success — live shared Rust state" />

## Try it

- `Enter show`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click Show toast`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | covered |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `toast/kinds` | Toast kinds | paint-only |
| `toast/narrow` | Narrow toast | paint-only |
| `toast/persistent` | Toast persistent | paint-only |
| `toast/stack` | Toast stack | paint-only |
| `toast/success` | Toast success | interactive |
| `toast/unicode` | Unicode Toast | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `toast/success`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn toast(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    frame.render_widget(
        Toast::new(system, "Updated", Severity::Success).anchor(Anchor::TopRight),
        area,
    );
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `toast/success`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `toast/success` | Toast success | **Primary (live preview)** |
| `toast/kinds` | Toast kinds | kinds |
| `toast/narrow` | Narrow toast | Narrow terminal |
| `toast/persistent` | Toast persistent | persistent |
| `toast/stack` | Toast stack | stack |
| `toast/unicode` | Unicode Toast | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/toast.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/toast.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `toast/success`

## Seen in applications

<SeenInApplications component="Toast" />
