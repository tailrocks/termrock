---
title: FilePicker
description: 'A host-driven file/directory browser with breadcrumbs, multi-select, path entry, and optional preview — no filesystem I/O in TermRock.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `file-picker/unix`

FilePicker Unix

![FilePicker — FilePicker Unix](/component-previews/file-picker-unix.svg)

### `file-picker/windows`

FilePicker Windows

![FilePicker — FilePicker Windows](/component-previews/file-picker-windows.svg)

### `file-picker/ssh`

FilePicker SSH

![FilePicker — FilePicker SSH](/component-previews/file-picker-ssh.svg)

### `file-picker/no-preview`

FilePicker no preview

![FilePicker — FilePicker no preview](/component-previews/file-picker-no-preview.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    FileEntry, FilePicker, FilePickerMode, FilePickerState, FilePreview,
};

let system = DesignSystem::default();
let mut state = FilePickerState::new("/home/u")
    .with_mode(FilePickerMode::OpenFile)
    .with_preview(true);
state.set_focused(true);
// ListRequested { path, generation } → host list → apply_listing(generation, …)
let _ = FilePicker::new(&system).title("Open file");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `file-picker/unix` — FilePicker Unix
- `file-picker/windows` — FilePicker Windows
- `file-picker/ssh` — FilePicker SSH
- `file-picker/no-preview` — FilePicker no preview
