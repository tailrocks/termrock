---
title: Badge
description: 'Non-interactive status badge with semantic role paint.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="badge/basic" interactive caption="Badge · Badge variants — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::Badge};

let system = DesignSystem::default();
let badge = Badge::new("NEW", &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | partial |
| Mouse | partial |
| Focus | partial |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `badge/basic` | Badge variants | **Primary (live preview)** |
| `badge/count` | Badge counts | count |
| `badge/narrow` | Narrow Badge | Narrow terminal |
| `badge/settings` | Badge settings | settings |
| `badge/table` | Badge in table context | table |
| `badge/task` | Badge task status | task |
| `badge/unicode` | Unicode Badge | Unicode / wide glyphs |
