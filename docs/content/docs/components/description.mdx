---
title: Description
description: "Help, error, warning, meta descriptions."
component: Description
demo: description/kinds
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/label.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="description/kinds" interactive caption="Description · Description kinds — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

This component has one canonical catalog setup. Resize the live host to exercise its responsive contract; **Reset** remounts the same configuration.

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `description/kinds`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn description_kinds_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::Description;
    let chunks = Layout::vertical([
        Constraint::Length(1),
        Constraint::Length(1),
        Constraint::Length(1),
        Constraint::Length(1),
    ])
    .split(area);
    let _ = Description::<()>::new("Help: use a stable hostname", system)
        .paint(chunks[0], frame.buffer_mut());
    let _ = Description::<()>::error("Error: value is required", system)
        .paint(chunks[1], frame.buffer_mut());
    let _ = Description::<()>::warning("Warning: deprecated flag", system)
        .paint(chunks[2], frame.buffer_mut());
    let _ = Description::<()>::meta("Meta: last synced 2m ago", system)
        .paint(chunks[3], frame.buffer_mut());
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `description/kinds`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `description/kinds` | Description kinds | **Primary (live preview)** |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/label.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/label.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `description/kinds`

## Seen in applications

<SeenInApplications component="Description" />
