---
title: Section
description: "Quiet editorial header + description + body."
component: Section
demo: section/quiet
interaction: activation
actions:
  - "Enter/Space toggle"
  - "← collapse"
  - "→ expand"
  - "click header"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"← collapse\" action updates visible mounted state and reports its typed outcome."
  - "The \"→ expand\" action updates visible mounted state and reports its typed outcome."
source: crates/termrock/src/widgets/section.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="section/quiet" interactive caption="Section · Section quiet — live shared Rust state" />

## Try it

- `Enter/Space toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `← collapse`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `→ expand`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `click header`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `section/actions` | Section header actions | paint-only |
| `section/collapsible` | Section collapsible | paint-only |
| `section/emphasized` | Section emphasized | paint-only |
| `section/narrow` | Section narrow | paint-only |
| `section/nested` | Section nested | paint-only |
| `section/quiet` | Section quiet | interactive |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `section/quiet`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn section_quiet_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::Section;
    let body = Section::new("Appearance", system)
        .description("Theme and density")
        .quiet()
        .paint(area, frame.buffer_mut(), None);
    if body.width > 2 && body.height > 0 {
        frame.buffer_mut().set_stringn(
            body.x,
            body.y,
            "Theme: phosphor",
            usize::from(body.width),
            system.style(Role::Text),
        );
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `section/quiet`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `section/quiet` | Section quiet | **Primary (live preview)** |
| `section/actions` | Section header actions | actions |
| `section/collapsible` | Section collapsible | collapsible |
| `section/emphasized` | Section emphasized | emphasized |
| `section/narrow` | Section narrow | Narrow terminal |
| `section/nested` | Section nested | nested |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/section.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/section.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `section/quiet`

## Seen in applications

<SeenInApplications component="Section" />
