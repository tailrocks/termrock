---
title: LoadingView
description: 'Centered loading label with a caller-ticked spinner frame.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `loading-view/basic`

Loading view

![LoadingView — Loading view](/component-previews/loading-view-basic.svg)

### `loading-view/narrow`

Narrow LoadingView

![LoadingView — Narrow LoadingView](/component-previews/loading-view-narrow.svg)

### `loading-view/unicode`

Unicode LoadingView

![LoadingView — Unicode LoadingView](/component-previews/loading-view-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::LoadingView};

let theme = Theme::default();
let loading = LoadingView::new("Loading…", "⠋", &theme);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `loading-view/basic` — Loading view
- `loading-view/narrow` — Narrow LoadingView
- `loading-view/unicode` — Unicode LoadingView
