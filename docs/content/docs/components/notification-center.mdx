---
title: NotificationCenter
description: 'Persistent notification history: unread, filter/group, timestamps, actions, progress, source, dismiss/clear-all; drawer or full-page; host-owned persistence; ingests ToastArchive without duplicating models.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `notification-center/drawer`

NotificationCenter drawer

![NotificationCenter — NotificationCenter drawer](/component-previews/notification-center-drawer.svg)

### `notification-center/empty`

NotificationCenter empty

![NotificationCenter — NotificationCenter empty](/component-previews/notification-center-empty.svg)

### `notification-center/filtered`

NotificationCenter unread filter

![NotificationCenter — NotificationCenter unread filter](/component-previews/notification-center-filtered.svg)

### `notification-center/full-page`

NotificationCenter full page

![NotificationCenter — NotificationCenter full page](/component-previews/notification-center-full-page.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    NotificationCenter, NotificationCenterState, NotificationRecipe, example_notifications,
};

let system = DesignSystem::default();
let mut state = NotificationCenterState::new();
state.replace_items(example_notifications(now_secs)); // or ingest_from_toast_queue
state.set_recipe(NotificationRecipe::Drawer);
let _ = state.open();
NotificationCenter::new(&system).paint(area, buf, &mut state);
// Host persists: state.items().to_vec()
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `notification-center/drawer` — NotificationCenter drawer
- `notification-center/empty` — NotificationCenter empty
- `notification-center/filtered` — NotificationCenter unread filter
- `notification-center/full-page` — NotificationCenter full page
