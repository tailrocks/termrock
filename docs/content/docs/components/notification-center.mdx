---
title: NotificationCenter
description: 'Persistent notification history: unread, filter/group, timestamps, actions, progress, source, dismiss/clear-all; drawer or full-page; host-owned persistence; ingests ToastArchive without duplicating models.'
component: NotificationCenter
demo: notification-center/drawer
interaction: selection-navigation
actions:
  - "↑↓ select"
  - "Enter open"
  - "Space read/unread"
  - "f filter"
  - "Esc close"
  - "click item"
expectedOutcomes:
  - "Navigation moves visible focus or selection and preserves it for the next event."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "Editing changes the visible Unicode-safe draft and reports the public typed edit outcome."
  - "Cancel or close removes the transient state without performing a host-owned effect."
source: crates/termrock/src/widgets/notification_center.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="notification-center/drawer" interactive caption="NotificationCenter · NotificationCenter drawer — live shared Rust state" />

## Try it

- `↑↓ select`: Move through the real focus/selection model; the next action uses the new target.
- `Enter open`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Space read/unread`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `f filter`: Focus the editable field, then type or paste Unicode text; the draft remains visible.
- `Esc close`: Open the transient state first, then cancel it; focus and the underlying state are preserved.
- `click item`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `notification-center/drawer` | NotificationCenter drawer | interactive |
| `notification-center/empty` | NotificationCenter empty | paint-only |
| `notification-center/filtered` | NotificationCenter unread filter | paint-only |
| `notification-center/full-page` | NotificationCenter full page | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `notification-center/drawer`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn notification_center_drawer_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let mut state = NotificationCenterState::new();
    state.replace_items(example_notifications(1_700_000_000));
    state.set_recipe(NotificationRecipe::Drawer);
    let _ = state.open();
    state.set_focused(true);
    NotificationCenter::new(system).paint(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `notification-center/drawer`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `notification-center/drawer` | NotificationCenter drawer | **Primary (live preview)** |
| `notification-center/empty` | NotificationCenter empty | Empty state |
| `notification-center/filtered` | NotificationCenter unread filter | filtered |
| `notification-center/full-page` | NotificationCenter full page | full page |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/notification_center.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/notification_center.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `notification-center/drawer`

## Seen in applications

<SeenInApplications component="NotificationCenter" />
