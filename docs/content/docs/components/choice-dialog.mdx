---
title: ChoiceDialog
description: 'Modal choice prompt on the canonical Dialog engine — stable actions, loading gate, Esc dismiss / confirm-only policy via dialog_mut().'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `choice-dialog/basic`

Choice dialog

![ChoiceDialog — Choice dialog](/component-previews/choice-dialog-basic.svg)

### `choice-dialog/narrow`

Narrow ChoiceDialog

![ChoiceDialog — Narrow ChoiceDialog](/component-previews/choice-dialog-narrow.svg)

### `choice-dialog/unicode`

Unicode ChoiceDialog

![ChoiceDialog — Unicode ChoiceDialog](/component-previews/choice-dialog-unicode.svg)

## Usage

```rust
use ratatui_core::text::Text;
use termrock::input::{KeyCode, KeyEvent, KeyModifiers};
use termrock::style::DesignSystem;
use termrock::widgets::{Action, ChoiceDialog, ChoiceDialogState, Dialog};

let system = DesignSystem::default();
let actions = [Action { id: "accept", label: "Accept", enabled: true, style: None }];
let dialog = ChoiceDialog::new(Dialog::new("Confirm", Text::from("Continue?"), &system), &actions);
let mut state = ChoiceDialogState::new(Some("accept"));
let outcome = state.handle_key(&actions, KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `choice-dialog/basic` — Choice dialog
- `choice-dialog/narrow` — Narrow ChoiceDialog
- `choice-dialog/unicode` — Unicode ChoiceDialog
