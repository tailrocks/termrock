---
title: AlertDialog
description: 'High-risk confirmation distinct from Dialog: exact scope, consequences, reversibility, target, safer alternatives; typed confirmation, justified countdown, safe initial focus, non-dismissable critical state.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `alert-dialog/delete`

AlertDialog delete

![AlertDialog — AlertDialog delete](/component-previews/alert-dialog-delete.svg)

### `alert-dialog/egress`

AlertDialog data egress

![AlertDialog — AlertDialog data egress](/component-previews/alert-dialog-egress.svg)

### `alert-dialog/locked`

AlertDialog locked critical

![AlertDialog — AlertDialog locked critical](/component-previews/alert-dialog-locked.svg)

### `alert-dialog/overwrite`

AlertDialog overwrite

![AlertDialog — AlertDialog overwrite](/component-previews/alert-dialog-overwrite.svg)

### `alert-dialog/terminate`

AlertDialog terminate

![AlertDialog — AlertDialog terminate](/component-previews/alert-dialog-terminate.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    AlertDialog, AlertDialogState, AlertKind, AlertScope, AlertConfirmGates,
    open_alert_dialog_widget_overlay,
};

let system = DesignSystem::default();
let mut state = AlertDialogState::new(
    AlertKind::Delete,
    AlertScope::example_delete(),
    "delete",
    "keep",
);
state.set_gates(AlertConfirmGates::typed("prod-db.customers"));
// open: state.open_on_stack(&mut stack, bounds, Some("trigger"));
// Enter on safe focus cancels; move to confirm only after gates pass.
AlertDialog::new(&system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `alert-dialog/delete` — AlertDialog delete
- `alert-dialog/egress` — AlertDialog data egress
- `alert-dialog/locked` — AlertDialog locked critical
- `alert-dialog/overwrite` — AlertDialog overwrite
- `alert-dialog/terminate` — AlertDialog terminate
