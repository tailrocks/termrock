---
title: SegmentedMeter
description: 'Single-row proportional meter for stacked shares.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `segmented-meter/basic`

Segmented meter

![SegmentedMeter — Segmented meter](/component-previews/segmented-meter-basic.svg)

### `segmented-meter/narrow`

Narrow SegmentedMeter

![SegmentedMeter — Narrow SegmentedMeter](/component-previews/segmented-meter-narrow.svg)

### `segmented-meter/unicode`

Unicode SegmentedMeter

![SegmentedMeter — Unicode SegmentedMeter](/component-previews/segmented-meter-unicode.svg)

## Usage

```rust
use termrock::{style::DesignSystem, style::Role, widgets::{MeterSegment, SegmentedMeter}};

let system = DesignSystem::default();
let segments = [MeterSegment { label: "used", weight: 1.0, role: Role::Success }];
let meter = SegmentedMeter::new(&segments, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `segmented-meter/basic` — Segmented meter
- `segmented-meter/narrow` — Narrow SegmentedMeter
- `segmented-meter/unicode` — Unicode SegmentedMeter
