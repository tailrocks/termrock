---
title: AccentRail
description: 'A semantic one-column block rail with deterministic wave motion, reduced-motion fallback, and collapsed-block glyph.'
component: AccentRail
demo: accent-rail/actors
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/accent_rail.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="accent-rail/actors" interactive caption="AccentRail · Accent rail actors — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

This component has one canonical catalog setup. Resize the live host to exercise its responsive contract; **Reset** remounts the same configuration.

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `accent-rail/actors`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn accent_rail_actors_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::AccentRail;
    let blocks = Layout::vertical([
        Constraint::Length(2),
        Constraint::Length(3),
        Constraint::Length(2),
        Constraint::Length(2),
    ])
    .spacing(1)
    .split(area);
    let cases = [
        (Role::ActorUser, false, false, "USER  Request received"),
        (
            Role::ActorAssistant,
            true,
            false,
            "ASSISTANT  Working with deterministic wave",
        ),
        (Role::ActorTool, false, false, "TOOL  Build complete"),
        (Role::TextMuted, false, true, "COLLAPSED  3 hidden events"),
    ];
    for (index, (role, active, collapsed, label)) in cases.into_iter().enumerate() {
        let content = AccentRail::new(system, role)
            .active(active)
            .collapsed(collapsed)
            .tick(11)
            .paint(blocks[index], frame.buffer_mut());
        frame.buffer_mut().set_stringn(
            content.x,
            content.y,
            label,
            usize::from(content.width),
            system.style(if index == 1 {
                Role::TextStrong
            } else {
                Role::Text
            }),
        );
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `accent-rail/actors`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `accent-rail/actors` | Accent rail actors | **Primary (live preview)** |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/accent_rail.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/accent_rail.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `accent-rail/actors`

## Seen in applications

<SeenInApplications component="AccentRail" />
