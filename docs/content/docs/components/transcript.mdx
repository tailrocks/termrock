---
title: Transcript
description: Variable-height streaming transcript with stable visual anchors.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![Transcript — Transcript](/component-previews/transcript-basic.svg)

## Usage

```rust
use termrock::{Theme, widgets::{Transcript, TranscriptBlock, TranscriptKind, TranscriptState}};

let theme = Theme::default();
let lines = ["hello", "world"];
let blocks = [TranscriptBlock::new("b1", TranscriptKind::User, &lines)];
let mut state: TranscriptState<&str> = TranscriptState::new();
let view = Transcript::new(&blocks, &theme);
let _ = (&mut state, view);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `transcript/basic` — Transcript
- `transcript/narrow` — Transcript narrow
- `transcript/unicode` — Transcript unicode
