---
title: FullscreenViewer
description: 'Promotion chrome compact→detail→fullscreen with frozen SourceContext (selection, scroll anchor, focus, breadcrumbs). Host paints CodeBlock/Diff/logs/objects/tasks/media into body slot; nested Esc peels stack first.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `fullscreen-viewer/basic`

FullscreenViewer code

![FullscreenViewer — FullscreenViewer code](/component-previews/fullscreen-viewer-basic.svg)

### `fullscreen-viewer/diff`

FullscreenViewer diff

![FullscreenViewer — FullscreenViewer diff](/component-previews/fullscreen-viewer-diff.svg)

### `fullscreen-viewer/log`

FullscreenViewer log

![FullscreenViewer — FullscreenViewer log](/component-previews/fullscreen-viewer-log.svg)

### `fullscreen-viewer/narrow`

FullscreenViewer narrow

![FullscreenViewer — FullscreenViewer narrow](/component-previews/fullscreen-viewer-narrow.svg)

### `fullscreen-viewer/unicode`

FullscreenViewer unicode

![FullscreenViewer — FullscreenViewer unicode](/component-previews/fullscreen-viewer-unicode.svg)

### `fullscreen-viewer/zoom-badge`

SemanticZoom badge

![FullscreenViewer — SemanticZoom badge](/component-previews/fullscreen-viewer-zoom-badge.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    Action, FullscreenViewer, FullscreenViewerState, SourceContext, ScrollAnchor,
    ViewerContentKind, open_fullscreen_viewer_overlay,
};

let system = DesignSystem::default();
let actions = [Action { id: "copy", label: "Copy", enabled: true, style: None }];
let mut state = FullscreenViewerState::new();
state.zoom_mut().set_content_kind(ViewerContentKind::Code);
let ctx = SourceContext::new("main.rs")
    .scroll(ScrollAnchor::at(42, 0))
    .path(["repo", "src", "main.rs"]);
let _ = state.enter_fullscreen(ctx, "main.rs");
let _ = state.open_on_stack(&mut stack, bounds, Some("list"));
FullscreenViewer::new(&system, &actions).paint(entry.rect, buf, &mut state);
// Host: CodeBlock::…paint(state.body_area(), buf, &mut code_state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `fullscreen-viewer/basic` — FullscreenViewer code
- `fullscreen-viewer/diff` — FullscreenViewer diff
- `fullscreen-viewer/log` — FullscreenViewer log
- `fullscreen-viewer/narrow` — FullscreenViewer narrow
- `fullscreen-viewer/unicode` — FullscreenViewer unicode
- `fullscreen-viewer/zoom-badge` — SemanticZoom badge
