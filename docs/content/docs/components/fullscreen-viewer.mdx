---
title: FullscreenViewer
description: 'Promotion chrome compact→detail→fullscreen with frozen SourceContext (selection, scroll anchor, focus, breadcrumbs). Host paints CodeBlock/Diff/logs/objects/tasks/media into body slot; nested Esc peels stack first.'
component: FullscreenViewer
demo: fullscreen-viewer/basic
interaction: disclosure-overlay
actions:
  - "Enter open"
  - "click trigger"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/fullscreen_viewer.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="fullscreen-viewer/basic" interactive caption="FullscreenViewer · FullscreenViewer code — live shared Rust state" />

## Try it

- `Enter open`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click trigger`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `fullscreen-viewer/basic` | FullscreenViewer code | interactive |
| `fullscreen-viewer/diff` | FullscreenViewer diff | paint-only |
| `fullscreen-viewer/log` | FullscreenViewer log | paint-only |
| `fullscreen-viewer/narrow` | FullscreenViewer narrow | paint-only |
| `fullscreen-viewer/unicode` | FullscreenViewer unicode | paint-only |
| `fullscreen-viewer/zoom-badge` | SemanticZoom badge | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `fullscreen-viewer/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn fullscreen_viewer_code_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let actions = [
        Action {
            id: "copy",
            label: "Copy",
            enabled: true,
            style: None,
        },
        Action {
            id: "raw",
            label: "Raw",
            enabled: true,
            style: None,
        },
    ];
    let mut state = FullscreenViewerState::new();
    state.zoom_mut().set_content_kind(ViewerContentKind::Code);
    let _ = state.enter_fullscreen(fv_source("main.rs", &["repo", "src", "main.rs"]), "main.rs");
    FullscreenViewer::new(system, &actions).paint(area, frame.buffer_mut(), &mut state);
    paint_viewer_body(frame, state.body_area(), system, "fn main() { /* … */ }");
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `fullscreen-viewer/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `fullscreen-viewer/basic` | FullscreenViewer code | **Primary (live preview)** |
| `fullscreen-viewer/diff` | FullscreenViewer diff | diff |
| `fullscreen-viewer/log` | FullscreenViewer log | log |
| `fullscreen-viewer/narrow` | FullscreenViewer narrow | Narrow terminal |
| `fullscreen-viewer/unicode` | FullscreenViewer unicode | Unicode / wide glyphs |
| `fullscreen-viewer/zoom-badge` | SemanticZoom badge | zoom badge |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/fullscreen_viewer.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/fullscreen_viewer.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `fullscreen-viewer/basic`

## Seen in applications

<SeenInApplications component="FullscreenViewer" />
