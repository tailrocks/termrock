---
title: Button
description: 'Activation primitive. Enter/Space or pointer activate once; disabled/loading never activate.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="button/activation" interactive caption="Button · Button primary — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{Button, ButtonState}};

let system = DesignSystem::default();
let mut state = ButtonState::new();
let button = Button::new("Save", &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `button/activation` | Button primary | **Primary (live preview)** |
| `button/destructive` | Button destructive | destructive |
| `button/dialog` | Button dialog actions | dialog |
| `button/disabled` | Disabled button | Disabled |
| `button/form` | Button form full-width | form |
| `button/inline` | Button inline link | inline |
| `button/loading` | Loading button | Loading state |
| `button/narrow` | Narrow Button | Narrow terminal |
| `button/no-color` | Button no-color | no color |
| `button/pending` | Button pending confirm | pending |
| `button/toolbar` | Button toolbar row | toolbar |
| `button/unicode` | Unicode Button | Unicode / wide glyphs |
| `button/variants` | Button variants | variants |
