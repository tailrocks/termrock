---
title: Button
description: 'Activation primitive. Enter/Space or pointer activate once; disabled/loading never activate.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![Button — Button](/component-previews/button-activation.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{Button, ButtonState}};

let tokens = DesignTokens::default();
let mut state = ButtonState::new();
let button = Button::new("Save", &tokens);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | not-applicable |
| Narrow terminal | not-applicable |

## Stories

- `button/activation` — Button
