---
title: ResizablePanelGroup
description: "Sidebar | main | inspector with resize handles."
component: ResizablePanelGroup
demo: resizable-panel-group/workbench
interaction: drag-continuous-value
actions:
  - "←→ resize focused handle"
  - "drag divider"
  - "Home/End collapse"
expectedOutcomes:
  - "Dragging changes the visible continuous value or geometry and emits its typed change outcome."
  - "Navigation moves visible focus or selection and preserves it for the next event."
source: crates/termrock/src/widgets/resizable_panel_group.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="resizable-panel-group/workbench" interactive caption="ResizablePanelGroup · ResizablePanelGroup workbench — live shared Rust state" />

## Try it

- `←→ resize focused handle`: Press, drag, and release on the visible handle; geometry/value follows the pointer.
- `drag divider`: Press, drag, and release on the visible handle; geometry/value follows the pointer.
- `Home/End collapse`: Move through the real focus/selection model; the next action uses the new target.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

`ResizablePanelGroupState` owns sizes, collapsed state, focused/hovered handle, drag, and layout geometry. Pane content and persistence remain host-owned.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `resizable-panel-group/dashboard` | ResizablePanelGroup dashboard | paint-only |
| `resizable-panel-group/drawers` | ResizablePanelGroup drawers | paint-only |
| `resizable-panel-group/workbench` | ResizablePanelGroup workbench | interactive |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `resizable-panel-group/workbench`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn resizable_workbench_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::{
        Panel, ResizablePanelGroup, ResizablePanelGroupState, three_pane_panels,
    };
    let panels = three_pane_panels();
    let group = ResizablePanelGroup::new(&panels, system).workbench();
    let mut state = ResizablePanelGroupState::new();
    let layout = group.layout(area, &mut state);
    group.paint_handles(area, frame.buffer_mut(), &mut state);
    for p in &layout.panels {
        if p.drawer || p.collapsed || p.area.width < 3 || p.area.height < 2 {
            continue;
        }
        let _ = Panel::new(system)
            .title(p.id.0.as_str())
            .paint(p.area, frame.buffer_mut(), None);
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `resizable-panel-group/workbench`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `resizable-panel-group/workbench` | ResizablePanelGroup workbench | **Primary (live preview)** |
| `resizable-panel-group/dashboard` | ResizablePanelGroup dashboard | dashboard |
| `resizable-panel-group/drawers` | ResizablePanelGroup drawers | drawers |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/resizable_panel_group.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/resizable_panel_group.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `resizable-panel-group/workbench`

## Seen in applications

<SeenInApplications component="ResizablePanelGroup" />
