---
title: ResizablePanelGroup
description: 'ResizablePanelGroup widget.'
component: ResizablePanelGroup
demo: resizable-panel-group/workbench
interaction: drag-continuous-value
actions:
  - "←→ resize focused handle"
  - "drag divider"
  - "Home/End collapse"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/resizable_panel_group.rs
---


## Live terminal (Ghostty-class)

Resize the focused divider with arrows or drag it directly. Home/End exercises collapse policy on adjacent panes.

<TerminalPreview story="resizable-panel-group/workbench" interactive caption="ResizablePanelGroup · ResizablePanelGroup workbench — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::ResizablePanelGroup;

// See handbook / lookbook story resizable-panel-group/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `resizable-panel-group/workbench` | ResizablePanelGroup workbench | **Primary (live preview)** |
| `resizable-panel-group/dashboard` | ResizablePanelGroup dashboard | dashboard |
| `resizable-panel-group/drawers` | ResizablePanelGroup drawers | drawers |


## Try it

- `←→` resizes the focused handle within panel constraints.
- Drag a divider for direct manipulation; Home/End collapses an eligible neighbor.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

`ResizablePanelGroupState` owns sizes, collapsed state, focused/hovered handle, drag, and layout geometry. Pane content and persistence remain host-owned.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `resizable-panel-group/workbench`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/resizable_panel_group.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/resizable_panel_group.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `resizable-panel-group/workbench`
