---
title: FieldRow
description: 'A density-aware composed field row with shared label columns, semantic selection chrome, masked and unset values, markers, and annotations.'
component: FieldRow
demo: field-row/states
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/field_row.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="field-row/states" interactive caption="FieldRow · Field row states — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `field-row/in-app` | In application | interactive |
| `field-row/states` | Field row states | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `field-row/states`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn field_row_states_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::{FieldRow, FieldRowValue};
    let rows = Layout::vertical([Constraint::Length(1); 5]).split(area);
    let labels = ["Workspace", "API token", "Endpoint", "Mode", "Owner"];
    let label_cols = FieldRow::label_cols_for(labels.into_iter());
    FieldRow::new(system, labels[0], FieldRowValue::Plain("termrock"))
        .label_cols(label_cols)
        .marker("◆")
        .paint(rows[0], frame.buffer_mut());
    FieldRow::new(system, labels[1], FieldRowValue::Masked { len: 8 })
        .label_cols(label_cols)
        .annotation("stored locally")
        .annotation_italic(true)
        .paint(rows[1], frame.buffer_mut());
    FieldRow::new(system, labels[2], FieldRowValue::Unset { hint: "required" })
        .label_cols(label_cols)
        .required(true)
        .paint(rows[2], frame.buffer_mut());
    FieldRow::new(system, labels[3], FieldRowValue::Plain("Focused"))
        .label_cols(label_cols)
        .selected(true)
        .paint(rows[3], frame.buffer_mut());
    FieldRow::new(system, labels[4], FieldRowValue::Plain("Tailrocks"))
        .label_cols(label_cols)
        .hovered(true)
        .paint(rows[4], frame.buffer_mut());
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `field-row/states`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `field-row/states` | Field row states | **Primary (live preview)** |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/field_row.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/field_row.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `field-row/states`

## Seen in applications

<SeenInApplications component="FieldRow" />
