---
title: Offline / ReconnectingState
description: 'Connectivity for remote sessions — banner, full recovery, StatusBar and NotificationCenter projection.'
component: Offline / ReconnectingState
demo: connectivity/banner
interaction: disclosure-overlay
actions:
  - "R retry"
  - "O work offline"
  - "Q view queue"
  - "Esc dismiss"
  - "click retry"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/mod.rs
---
## Live terminal (Ghostty-class)

This preview mounts one persistent Rust component instance. Use the listed keyboard and pointer actions; each accepted action updates the same state and reports its typed outcome.

<TerminalPreview story="connectivity/banner" interactive caption="Offline / ReconnectingState · shared Rust demo" />

## Usage

```rust
use termrock::widgets::{
    ReconnectingState, OfflineBanner, OfflineSurface, ConnectivityPresentation,
    StatusSlot, StatusBar,
};

let mut conn = ReconnectingState::new("agent://prod-1");
conn.begin_reconnect(3);
conn.set_now_secs(now);
conn.set_last_success_secs(Some(last_ok));

// StatusBar
let label = conn.status_bar_content();
let right = [conn.status_slot_template("c", &label)];

// Banner
OfflineBanner::new(&conn, &system).paint(banner_row, buf);

// Full recovery
conn.set_presentation(ConnectivityPresentation::Full);
OfflineSurface::new(&system).paint(area, buf, &mut conn);

// Notifications
center.ingest_item(conn.to_notification_item("conn-1"));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered (r/a/o/q, Tab, Esc, Enter) |
| Mouse | covered (banner click, action bands) |
| Focus | covered (full surface actions) |
| Non-color cues | covered (glyph + labels) |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

| Story | Title | Focus |
|---|---|---|
| `connectivity/banner` | Offline banner | **Primary (live preview)** |
| `connectivity/reconnecting` | Reconnecting full | reconnecting |
| `connectivity/auth` | Auth required | auth |
| `connectivity/unavailable` | Server unavailable | unavailable |
| `connectivity/status-bar` | Connectivity StatusBar | status bar |
| `connectivity/notification` | Connectivity notification | notification |


## Try it

- Use the actions shown beside the terminal; mouse, keyboard, paste, drag, and resize are forwarded only when the component supports them.
- Confirm the visual state and typed outcome, then use **Reset** to mount a fresh instance.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `connectivity/banner`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `connectivity/banner`
