---
title: Offline / ReconnectingState
description: 'Connectivity for remote sessions — banner, full recovery, StatusBar and NotificationCenter projection.'
component: Offline / ReconnectingState
demo: connectivity/banner
interaction: disclosure-overlay
actions:
  - "R retry"
  - "O work offline"
  - "Q view queue"
  - "Esc dismiss"
  - "click retry"
expectedOutcomes:
  - "The \"R retry\" action updates visible mounted state and reports its typed outcome."
  - "The \"O work offline\" action updates visible mounted state and reports its typed outcome."
  - "The \"Q view queue\" action updates visible mounted state and reports its typed outcome."
  - "Cancel or close removes the transient state without performing a host-owned effect."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/mod.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="connectivity/banner" interactive caption="Offline / ReconnectingState · Offline banner — live shared Rust state" />

## Try it

- `R retry`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `O work offline`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Q view queue`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Esc dismiss`: Open the transient state first, then cancel it; focus and the underlying state are preserved.
- `click retry`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

This component has one canonical catalog setup. Resize the live host to exercise its responsive contract; **Reset** remounts the same configuration.

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `connectivity/banner`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn connectivity_banner_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let s = example_reconnecting_agent();
    OfflineBanner::new(&s, system).paint(area, frame.buffer_mut());
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `connectivity/banner`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

| Story | Title | Focus |
|---|---|---|
| `connectivity/banner` | Offline banner | **Primary (live preview)** |
| `connectivity/reconnecting` | Reconnecting full | reconnecting |
| `connectivity/auth` | Auth required | auth |
| `connectivity/unavailable` | Server unavailable | unavailable |
| `connectivity/status-bar` | Connectivity StatusBar | status bar |
| `connectivity/notification` | Connectivity notification | notification |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `connectivity/banner`
