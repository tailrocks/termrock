---
title: SeparatorLine
description: 'Horizontal or vertical separator rule using border role paint.'
component: SeparatorLine
demo: separator/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/separator.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="separator/basic" interactive caption="SeparatorLine · Separator quiet — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `separator/narrow` | Narrow SeparatorLine | paint-only |
| `separator/unicode` | Unicode SeparatorLine | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `separator/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn separator_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::Separator;
    Separator::horizontal(system)
        .quiet()
        .paint(area, frame.buffer_mut());
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `separator/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

| Story | Title | Focus |
|---|---|---|
| `separator/basic` | Separator quiet | **Primary (live preview)** |
| `separator/narrow` | Narrow SeparatorLine | narrow |
| `separator/unicode` | Unicode SeparatorLine | unicode |
| `separator/strong` | Separator strong | strong |
| `separator/labeled` | Separator labeled | labeled |
| `separator/section-break` | Separator section break | section break |
| `separator/vertical` | Separator vertical | vertical |
| `separator/focus-zone` | Separator focus zone | focus zone |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/separator.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/separator.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `separator/basic`

## Seen in applications

<SeenInApplications component="SeparatorLine" />
