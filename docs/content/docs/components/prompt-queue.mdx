---
title: PromptQueue
description: 'Editable queue of prompts waiting behind busy agent work.'
component: PromptQueue
demo: prompt-queue/compact
interaction: activation
actions:
  - "↑↓ select"
  - "Enter edit"
  - "D remove"
  - "Esc cancel"
expectedOutcomes:
  - "Navigation moves visible focus or selection and preserves it for the next event."
  - "Editing changes the visible Unicode-safe draft and reports the public typed edit outcome."
  - "The \"D remove\" action updates visible mounted state and reports its typed outcome."
  - "Cancel or close removes the transient state without performing a host-owned effect."
source: crates/termrock/src/widgets/mod.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="prompt-queue/compact" interactive caption="PromptQueue · PromptQueue compact — live shared Rust state" />

## Try it

- `↑↓ select`: Move through the real focus/selection model; the next action uses the new target.
- `Enter edit`: Focus the editable field, then type or paste Unicode text; the draft remains visible.
- `D remove`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Esc cancel`: Open the transient state first, then cancel it; focus and the underlying state are preserved.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted Rust demo owns deterministic sample state. The component/pattern owns its reusable paint and interaction contracts; the consuming host owns domain policy and every external effect.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | not-applicable in primary demo |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `prompt-queue/compact` | PromptQueue compact | interactive |
| `prompt-queue/expanded` | PromptQueue expanded | paint-only |
| `prompt-queue/failed` | PromptQueue failed held | paint-only |
| `prompt-queue/narrow` | Narrow PromptQueue | paint-only |
| `prompt-queue/unicode` | Unicode PromptQueue | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `prompt-queue/compact`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn prompt_queue_compact_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::patterns::{
        AgentBusyState, PromptQueue, PromptQueuePresentation, PromptQueueState,
        example_prompt_queue,
    };
    let mut state = PromptQueueState::new();
    state.set_items(example_prompt_queue());
    state.set_agent(AgentBusyState::Busy);
    state.presentation = PromptQueuePresentation::Compact;
    state.focused = true;
    frame.render_stateful_widget(&PromptQueue::new(system), area, &mut state);
}
```

## Migrated handbook guidance

# PromptQueue

Queue of user messages behind active agent work. **Host owns** persistence and
drain policy. Failed/cancelled entries are **held** (KD-29 — no library
auto-drain).


## When to use / when not

| Use PromptQueue | Prefer |
|-----------------|--------|
| Show/manage queued prompts while busy | — |
| Draft editing | [PromptComposer](/docs/components/prompt-composer) |
| Overlay focus FIFO | OverlayStack (not user prompts) |

## Presentation

| Mode | Role |
|------|------|
| Compact | One-line summary for composer status |
| Expanded | Reorder, edit, delete, send, interrupt |

## States

Queued · Sending · Blocked · Failed · Cancelled · Sent

Attachments and mentions keep **ids + labels** across edit/reorder.

## Keys (expanded)

j/k · J/K reorder · Enter send · i interrupt+send · e edit · d delete · Esc compact

## Related

PromptComposer · migration `0231`

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `prompt-queue/compact`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

| Story | Title | Focus |
|---|---|---|
| `prompt-queue/compact` | PromptQueue compact | **Primary (live preview)** |
| `prompt-queue/expanded` | PromptQueue expanded | expanded |
| `prompt-queue/failed` | PromptQueue failed held | failed |
| `prompt-queue/narrow` | Narrow PromptQueue | narrow |
| `prompt-queue/unicode` | Unicode PromptQueue | unicode |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `prompt-queue/compact`

## Seen in applications

<SeenInApplications component="PromptQueue" />
