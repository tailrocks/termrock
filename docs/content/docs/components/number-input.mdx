---
title: NumberInput
description: 'A numeric field with draft text separate from committed value, min/max/step, units, and steppers.'
component: NumberInput
demo: number-input/basic
interaction: editor-form
actions:
  - "type a number"
  - "↑↓ step"
  - "click +/-"
  - "wheel over field"
  - "Enter submit"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/number_input.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="number-input/basic" interactive caption="NumberInput · Number input — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{NumberConstraints, NumberInput, NumberInputState, NumberKind};

let system = DesignSystem::default();
let mut state = NumberInputState::new()
    .with_kind(NumberKind::decimal2())
    .with_constraints(NumberConstraints::bounded(0.0, 100.0, 0.5))
    .with_value(12.5);
state.set_focused(true);
let _ = NumberInput::new("Opacity", &system).unit("%");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `number-input/basic` | Number input | **Primary (live preview)** |
| `number-input/decimal` | Number decimal | decimal |
| `number-input/invalid` | Number invalid | invalid |
| `number-input/narrow` | Number narrow | Narrow terminal |


## Try it

- `type a number`: routes through the component public interaction API.
- `↑↓ step`: routes through the component public interaction API.
- `click +/-`: routes through the component public interaction API.
- `wheel over field`: routes through the component public interaction API.
- `Enter submit`: routes through the component public interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `number-input/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/number_input.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/number_input.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `number-input/basic`

## Migrated handbook guidance

# NumberInput

**Purpose.** Integer or decimal entry for forms and settings: min/max/step,
units, empty/intermediate invalid states, steppers, and safe overflow.


## Draft vs committed

| | Draft (`draft_text`) | Committed (`value`) |
|--|----------------------|---------------------|
| Typing | live | unchanged |
| Enter / blur / step | reformatted | clamped + snapped |
| Esc | restored | unchanged |

Storage uses locale-independent `f64` with `.` as the only decimal separator.

## Compose with Slider

```rust
let bounds = SliderBounds::new(0.0, 100.0, 1.0);
state.set_from_slider(bounds, slider.value());
slider.set_value(state.to_slider_value(bounds));
```

## Example

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{NumberConstraints, NumberInput, NumberInputState, NumberKind};

let system = DesignSystem::default();
let mut state = NumberInputState::new()
    .with_kind(NumberKind::decimal2())
    .with_constraints(NumberConstraints::bounded(0.0, 100.0, 0.5))
    .with_value(12.5);
state.set_focused(true);
let _ = NumberInput::new("Opacity", &system).unit("%").ascii(true);
```

## Related

TextInput · Slider · Field · migration `0141`
