---
title: NumberInput
description: 'A numeric field with draft text separate from committed value, min/max/step, units, and steppers.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `number-input/basic`

Number input

![NumberInput — Number input](/component-previews/number-input-basic.svg)

### `number-input/decimal`

Number decimal

![NumberInput — Number decimal](/component-previews/number-input-decimal.svg)

### `number-input/invalid`

Number invalid

![NumberInput — Number invalid](/component-previews/number-input-invalid.svg)

### `number-input/narrow`

Number narrow

![NumberInput — Number narrow](/component-previews/number-input-narrow.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{NumberConstraints, NumberInput, NumberInputState, NumberKind};

let system = DesignSystem::default();
let mut state = NumberInputState::new()
    .with_kind(NumberKind::decimal2())
    .with_constraints(NumberConstraints::bounded(0.0, 100.0, 0.5))
    .with_value(12.5);
state.set_focused(true);
let _ = NumberInput::new("Opacity", &system).unit("%");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `number-input/basic` — Number input
- `number-input/decimal` — Number decimal
- `number-input/invalid` — Number invalid
- `number-input/narrow` — Number narrow
