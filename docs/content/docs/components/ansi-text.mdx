---
title: AnsiText
description: 'Safe ANSI parse and paint — SGR, CR/BS, OSC-8, streaming, no-color.'
component: AnsiText
demo: ansi-text/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/mod.rs
---
## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="ansi-text/basic" interactive caption="AnsiText · shared Rust demo" />

## Usage

```rust
use termrock::widgets::{
    AnsiStream, AnsiText, AnsiTextState, AnsiParseOptions, lines_for_log, parse_lines,
};

// Streaming tool / PTY output
let mut stream = AnsiStream::new(AnsiParseOptions::for_system(&system))
    .with_max_lines(10_000);
stream.feed(bytes);
stream.drain_into_lines(|line| log_pane.append(line));

// Paint pre-parsed lines
let lines = parse_lines(src, &AnsiParseOptions::for_system(&system));
let mut state = AnsiTextState::new();
AnsiText::lines(&lines, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

| Story | Title | Focus |
|---|---|---|
| `ansi-text/basic` | AnsiText SGR | **Primary (live preview)** |
| `ansi-text/no-color` | AnsiText no-color | no color |
| `ansi-text/cr-bs` | AnsiText CR/BS | cr bs |
| `ansi-text/hyperlink` | AnsiText OSC-8 | hyperlink |
| `ansi-text/narrow` | Narrow AnsiText | narrow |
| `ansi-text/unicode` | Unicode AnsiText | unicode |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `ansi-text/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `ansi-text/basic`
