---
title: DateTimePicker
description: 'Civil date, time, and range picker with text entry, calendar/time-list browse, min/max, and non-color day states. Prefer TextInput for rare ISO paste.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `date-time-picker/date`

DateTimePicker date

![DateTimePicker — DateTimePicker date](/component-previews/date-time-picker-date.svg)

### `date-time-picker/narrow`

DateTimePicker narrow

![DateTimePicker — DateTimePicker narrow](/component-previews/date-time-picker-narrow.svg)

### `date-time-picker/range`

DateTimePicker range

![DateTimePicker — DateTimePicker range](/component-previews/date-time-picker-range.svg)

### `date-time-picker/time`

DateTimePicker time

![DateTimePicker — DateTimePicker time](/component-previews/date-time-picker-time.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    CivilDate, DateTimePicker, DateTimePickerKind, DateTimePickerState,
};

let system = DesignSystem::default();
let mut state = DateTimePickerState::new(DateTimePickerKind::Date)
    .with_timezone_label("UTC");
state.set_today(CivilDate::new(2026, 8, 10).unwrap());
state.set_focused(true);
let _ = DateTimePicker::new(&system).label("Due");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `date-time-picker/date` — DateTimePicker date
- `date-time-picker/narrow` — DateTimePicker narrow
- `date-time-picker/range` — DateTimePicker range
- `date-time-picker/time` — DateTimePicker time
