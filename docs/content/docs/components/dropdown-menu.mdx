---
title: DropdownMenu
description: 'Anchored command menu with nested items, checkbox/radio, shortcuts, typeahead, OverlayStack cascade, and CommandPalette promotion when deep or oversized.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `dropdown-menu/basic`

DropdownMenu basic

![DropdownMenu — DropdownMenu basic](/component-previews/dropdown-menu-basic.svg)

### `dropdown-menu/kinds`

DropdownMenu item kinds

![DropdownMenu — DropdownMenu item kinds](/component-previews/dropdown-menu-kinds.svg)

### `dropdown-menu/nested`

DropdownMenu nested

![DropdownMenu — DropdownMenu nested](/component-previews/dropdown-menu-nested.svg)

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::style::DesignSystem;
use termrock::widgets::{
    DropdownMenu, DropdownMenuState, MenuNode, open_dropdown_menu_overlay, measure_menu_panel,
};

let system = DesignSystem::default();
let nodes = vec![
    MenuNode::command("open", "Open").shortcut("C-o"),
    MenuNode::submenu("more", "More", vec![MenuNode::command("a", "A")]),
];
let mut state = DropdownMenuState::new();
let bounds = Rect::new(0, 0, 80, 24);
let _ = state.open_from_keyboard(&nodes, bounds);
// stack: open_dropdown_menu_overlay(...); paint: DropdownMenu::new(&nodes, &system).paint(...)
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `dropdown-menu/basic` — DropdownMenu basic
- `dropdown-menu/kinds` — DropdownMenu item kinds
- `dropdown-menu/nested` — DropdownMenu nested
