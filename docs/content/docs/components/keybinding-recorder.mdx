---
title: KeybindingRecorder
description: 'A settings control that captures and validates keybindings with escape law, conflicts, reserved chords, and protocol limits.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `keybinding-recorder/idle`

KeybindingRecorder idle

![KeybindingRecorder — KeybindingRecorder idle](/component-previews/keybinding-recorder-idle.svg)

### `keybinding-recorder/recording`

KeybindingRecorder recording

![KeybindingRecorder — KeybindingRecorder recording](/component-previews/keybinding-recorder-recording.svg)

### `keybinding-recorder/conflict`

KeybindingRecorder conflict

![KeybindingRecorder — KeybindingRecorder conflict](/component-previews/keybinding-recorder-conflict.svg)

### `keybinding-recorder/reserved`

KeybindingRecorder reserved

![KeybindingRecorder — KeybindingRecorder reserved](/component-previews/keybinding-recorder-reserved.svg)

## Usage

```rust
use termrock::input::KeyCode;
use termrock::keymap::KeyChord;
use termrock::style::DesignSystem;
use termrock::widgets::{KeybindingRecorder, KeybindingRecorderState};

let system = DesignSystem::default();
let mut state = KeybindingRecorderState::new("app.save", "Save")
    .with_chords([KeyChord::ctrl(KeyCode::Char('s'))]);
state.set_focused(true);
// Enter → record; Esc cancels; Enter commits sequence
let _ = KeybindingRecorder::new(&system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `keybinding-recorder/idle` — KeybindingRecorder idle
- `keybinding-recorder/recording` — KeybindingRecorder recording
- `keybinding-recorder/conflict` — KeybindingRecorder conflict
- `keybinding-recorder/reserved` — KeybindingRecorder reserved
