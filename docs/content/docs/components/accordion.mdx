---
title: Accordion
description: "Multi-open section recipe with roving cursor."
component: Accordion
demo: accordion/section
interaction: disclosure-overlay
actions:
  - "↑↓ choose"
  - "←→ collapse/expand"
  - "Enter/Space toggle"
  - "click heading"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"←→ collapse/expand\" action updates visible mounted state and reports its typed outcome."
source: crates/termrock/src/widgets/accordion.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="accordion/section" interactive caption="Accordion · Accordion section — live shared Rust state" />

## Try it

- `↑↓ choose`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `←→ collapse/expand`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Enter/Space toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click heading`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `accordion/faq` | Accordion FAQ | paint-only |
| `accordion/logs` | Accordion logs | paint-only |
| `accordion/narrow` | Accordion narrow | paint-only |
| `accordion/scroll-body` | Accordion scroll body | paint-only |
| `accordion/section` | Accordion section | interactive |
| `accordion/settings` | Accordion settings | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `accordion/section`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn accordion_section_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use ratatui::widgets::Widget as _;
    use termrock::widgets::{Accordion, AccordionItem, AccordionState};
    let items = [
        AccordionItem::new("gen", "General").content_height(2),
        AccordionItem::new("net", "Network").content_height(2),
        AccordionItem::new("adv", "Advanced").content_height(2),
    ];
    let mut state = AccordionState::new().initially_open(["gen", "adv"]);
    state.set_surface_focused(true);
    state.set_cursor(Some("gen"));
    let parts = Accordion::section(&items, system).paint(area, frame.buffer_mut(), &mut state);
    for (id, body) in [
        ("gen", "Theme · density"),
        ("net", "Proxy · DNS"),
        ("adv", "Experimental flags"),
    ] {
        if let Some(r) = parts.content_of(&id)
            && r.height > 0
        {
            Paragraph::new(body)
                .style(system.style(Role::TextMuted))
                .render(r, frame.buffer_mut());
        }
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `accordion/section`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `accordion/section` | Accordion section | **Primary (live preview)** |
| `accordion/faq` | Accordion FAQ | faq |
| `accordion/logs` | Accordion logs | logs |
| `accordion/narrow` | Accordion narrow | Narrow terminal |
| `accordion/scroll-body` | Accordion scroll body | scroll body |
| `accordion/settings` | Accordion settings | settings |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/accordion.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/accordion.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `accordion/section`

## Seen in applications

<SeenInApplications component="Accordion" />
