---
title: Pagination
description: 'Page navigation for remote datasets with prev/next, page numbers, unknown totals, page size, loading, jump entry, and narrow contraction. Not scroll virtualization.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

### `pagination/full`

Pagination full

![Pagination — Pagination full](/component-previews/pagination-full.svg)

### `pagination/unknown`

Pagination unknown total

![Pagination — Pagination unknown total](/component-previews/pagination-unknown.svg)

### `pagination/loading`

Pagination loading

![Pagination — Pagination loading](/component-previews/pagination-loading.svg)

### `pagination/minimal`

Pagination minimal

![Pagination — Pagination minimal](/component-previews/pagination-minimal.svg)

### `pagination/jump`

Pagination jump

![Pagination — Pagination jump](/component-previews/pagination-jump.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{PageTotal, Pagination, PaginationState};

let system = DesignSystem::default();
let mut state = PaginationState::new(1, 25, PageTotal::Known(1000));
state.set_focused(true);
// PageRequested { request } → host.fetch(request.offset(), request.limit())
let _ = Pagination::new(&system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `pagination/full` — Pagination full
- `pagination/unknown` — Pagination unknown total
- `pagination/loading` — Pagination loading
- `pagination/minimal` — Pagination minimal
- `pagination/jump` — Pagination jump
