---
title: Popover
description: 'Anchored interactive surface for settings, filters, pickers, and details — OverlayStack placement, non-modal default with explicit modal policy, presentation contraction, and header/body/footer slots without forcing Panel.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `popover/basic`

Popover basic

![Popover — Popover basic](/component-previews/popover-basic.svg)

### `popover/modal`

Popover modal

![Popover — Popover modal](/component-previews/popover-modal.svg)

### `popover/narrow`

Narrow Popover

![Popover — Narrow Popover](/component-previews/popover-narrow.svg)

### `popover/slots`

Popover slots

![Popover — Popover slots](/component-previews/popover-slots.svg)

### `popover/unicode`

Unicode Popover

![Popover — Unicode Popover](/component-previews/popover-unicode.svg)

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::interaction::{OverlaySize, OverlayStack};
use termrock::style::DesignSystem;
use termrock::widgets::{
    Popover, PopoverState, open_popover_overlay, POPOVER_OVERLAY_ID,
};

let system = DesignSystem::default();
let mut stack = OverlayStack::<&str>::new();
let bounds = Rect::new(0, 0, 80, 24);
let anchor = Rect::new(10, 5, 8, 1);
let size = OverlaySize::menu(28, 10);
let mut state = PopoverState::new();
let _ = state.open_on_stack(&mut stack, bounds, anchor, size, Some("trigger"));
// Paint: Popover::new("Settings", &system).paint(entry.rect, buf, &mut state);
// Host fills state.slots().body. Dismiss via stack → opener focus restored.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `popover/basic` — Popover basic
- `popover/modal` — Popover modal
- `popover/narrow` — Narrow Popover
- `popover/slots` — Popover slots
- `popover/unicode` — Unicode Popover
