---
title: ContextMenu
description: 'Pointer/context-key command menu at origin with nested cascade, same item model as DropdownMenu, OverlayKind::ContextMenu placement.'
component: ContextMenu
demo: context-menu/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/mod.rs
---
## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="context-menu/basic" interactive caption="ContextMenu · shared Rust demo" />

## Usage

```rust
use termrock::widgets::{
    ContextMenu, DropdownMenuState, MenuNode, open_context_menu_overlay, measure_menu_panel,
};

let nodes = vec![
    MenuNode::command("copy", "Copy").shortcut("C-c"),
    MenuNode::command("delete", "Delete").destructive(true),
];
let mut state = DropdownMenuState::context();
let _ = state.open_from_context_pointer(&nodes, bounds);
// open_context_menu_overlay(&mut stack, bounds, origin, size, opener);
// ContextMenu::new(&nodes, &system).paint(rect, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered (right-click open host-driven; outside dismiss) |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

| Story | Title | Focus |
|---|---|---|
| `context-menu/basic` | ContextMenu basic | **Primary (live preview)** |
| `context-menu/nested` | ContextMenu nested | nested |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `context-menu/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `context-menu/basic`
