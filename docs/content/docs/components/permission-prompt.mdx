---
title: PermissionPrompt
description: 'Fail-safe permission/trust surface with default-deny focus.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="permission-prompt/basic" interactive caption="PermissionPrompt · Permission prompt — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{PermissionPrompt, PermissionPromptState, PermissionRequest}};

let system = DesignSystem::default();
let prompt = PermissionPrompt::new(&system);
let mut state = PermissionPromptState::new();
state.enqueue(PermissionRequest::new("r1", "bash", "workspace"));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `permission-prompt/basic` | Permission prompt | **Primary (live preview)** |
| `permission-prompt/destructive-nested` | Permission destructive nested | destructive nested |
| `permission-prompt/egress` | Permission data egress | egress |
| `permission-prompt/low-read` | Permission low-risk read | low read |
| `permission-prompt/narrow` | Narrow PermissionPrompt | Narrow terminal |
| `permission-prompt/unicode` | Unicode PermissionPrompt | Unicode / wide glyphs |
