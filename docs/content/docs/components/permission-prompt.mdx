---
title: PermissionPrompt
description: 'Fail-safe permission/trust surface with default-deny focus.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `permission-prompt/basic`

Permission prompt

![PermissionPrompt — Permission prompt](/component-previews/permission-prompt-basic.svg)

### `permission-prompt/narrow`

Narrow PermissionPrompt

![PermissionPrompt — Narrow PermissionPrompt](/component-previews/permission-prompt-narrow.svg)

### `permission-prompt/unicode`

Unicode PermissionPrompt

![PermissionPrompt — Unicode PermissionPrompt](/component-previews/permission-prompt-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::{PermissionPrompt, PermissionPromptState, PermissionRequest}};

let theme = Theme::default();
let prompt = PermissionPrompt::new(&theme);
let mut state = PermissionPromptState::new();
state.enqueue(PermissionRequest::new("r1", "bash", "workspace"));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `permission-prompt/basic` — Permission prompt
- `permission-prompt/narrow` — Narrow PermissionPrompt
- `permission-prompt/unicode` — Unicode PermissionPrompt
