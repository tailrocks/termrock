---
title: PermissionPrompt
description: 'Fail-safe permission/trust surface with default-deny focus.'
component: PermissionPrompt
demo: permission-prompt/basic
interaction: disclosure-overlay
actions:
  - "←→ choose decision"
  - "↑↓ choose scope"
  - "Enter decide"
  - "D details"
  - "E edit"
  - "click action"
  - "Esc deny/cancel"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"D details\" action updates visible mounted state and reports its typed outcome."
  - "Editing changes the visible Unicode-safe draft and reports the public typed edit outcome."
  - "Cancel or close removes the transient state without performing a host-owned effect."
source: crates/termrock/src/widgets/permission.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="permission-prompt/basic" interactive caption="PermissionPrompt · Permission prompt — live shared Rust state" />

## Try it

- `←→ choose decision`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `↑↓ choose scope`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter decide`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `D details`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `E edit`: Focus the editable field, then type or paste Unicode text; the draft remains visible.
- `click action`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Esc deny/cancel`: Open the transient state first, then cancel it; focus and the underlying state are preserved.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `permission-prompt/basic` | Permission prompt | interactive |
| `permission-prompt/destructive-nested` | Permission destructive nested | paint-only |
| `permission-prompt/egress` | Permission data egress | paint-only |
| `permission-prompt/in-app` | In application | interactive |
| `permission-prompt/low-read` | Permission low-risk read | paint-only |
| `permission-prompt/narrow` | Narrow PermissionPrompt | paint-only |
| `permission-prompt/unicode` | Unicode PermissionPrompt | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `permission-prompt/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn permission_prompt_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let prompt = PermissionPrompt::new(system);
    let mut state = PermissionPromptState::new();
    let req = PermissionRequest::new("r1", "bash", "workspace")
        .risk(PermissionRisk::High)
        .action_kind(PermissionActionKind::Shell)
        .command("rm -rf build/")
        .provenance(PermissionProvenance::main_agent("a", "agent"));
    state.enqueue(req);
    frame.render_stateful_widget(&prompt, area, &mut state);
}
```

## Migrated handbook guidance

# PermissionPrompt

Trust chrome keeps a muted focus-visible frame while a semantic severity rail
carries risk; approval choices use the shared action-chip vocabulary.

Not a generic Allow dialog. Checklist paint: **who** (leaf initiator), **via**
provenance, **op** + target, **run @** location, **access/dest**, **reversible**,
risk banners, expected result, grant scope once/session/project/always.


## When to use / when not

| Use PermissionPrompt | Prefer |
|----------------------|--------|
| Shell / file / network / MCP gates | — |
| Product settings toggles | Form / Switch |
| Agent prompt input | [PromptComposer](/docs/components/prompt-composer) |

## Checklist fields

Initiator · provenance · action kind · target · location · accessed data ·
destination · expected · reversible · risk · scope · prior grant · command/pattern

## Focus law

| Concern | Owner |
|---------|--------|
| Overlay trap (High/Critical) | OverlayStack AlertDialog |
| Surface input | Host `set_accepts_input` when overlay top |
| Action **cursor** | `action_cursor` (default **Deny**) |
| Scope cursor | `scope` via `[` `]` → Allow once…always |

Scene surface focus ≠ decision cursor.

## Outcomes

- `Ignored`
- `ActionCursorMoved` / `ScopeChanged`
- `DetailsToggled` / `EditStarted` / `EditChanged` / `EditCancelled`
- `Decided { request_id, generation, action, scope, edited }`
- `Cancelled` / `QueueChanged` / `StaleIgnored`

## Intents / keys

`default_permission_intent` — arrows/Tab, Enter, Esc, `d` details.
**No `y` grant.** `n` deny · `e`/`p` edit · `[`/`]` scope.

## Safety

- Default cursor **Deny** for all risks
- Enter default → Deny, never Allow
- Esc → cancel without grant + advance queue
- Stale generation → `StaleIgnored` (no grant)
- Concurrent queue FIFO + monotonic generations

## Related

PromptComposer · OverlayStack · migrations `0077`, **`0226`**

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `permission-prompt/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `permission-prompt/basic` | Permission prompt | **Primary (live preview)** |
| `permission-prompt/destructive-nested` | Permission destructive nested | destructive nested |
| `permission-prompt/egress` | Permission data egress | egress |
| `permission-prompt/low-read` | Permission low-risk read | low read |
| `permission-prompt/narrow` | Narrow PermissionPrompt | Narrow terminal |
| `permission-prompt/unicode` | Unicode PermissionPrompt | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/permission.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/permission.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `permission-prompt/basic`

## Seen in applications

<SeenInApplications component="PermissionPrompt" />
