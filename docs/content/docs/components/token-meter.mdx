---
title: TokenMeter
description: 'Token or cost usage meter with threshold roles.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `token-meter/basic`

Token meter

![TokenMeter — Token meter](/component-previews/token-meter-basic.svg)

### `token-meter/narrow`

Narrow TokenMeter

![TokenMeter — Narrow TokenMeter](/component-previews/token-meter-narrow.svg)

### `token-meter/unicode`

Unicode TokenMeter

![TokenMeter — Unicode TokenMeter](/component-previews/token-meter-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::TokenMeter};

let theme = Theme::default();
let meter = TokenMeter::new(128_000, 200_000, &theme);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `token-meter/basic` — Token meter
- `token-meter/narrow` — Narrow TokenMeter
- `token-meter/unicode` — Unicode TokenMeter
