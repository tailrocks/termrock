---
title: Table
description: 'A stable-ID columnar data view with deterministic widths, sorting requests, and visible-window rendering.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `table/basic`

Data table

![Table — Data table](/component-previews/table-basic.svg)

### `table/disabled`

Disabled table row

![Table — Disabled table row](/component-previews/table-disabled.svg)

### `table/empty`

Empty table

![Table — Empty table](/component-previews/table-empty.svg)

### `table/narrow`

Narrow table

![Table — Narrow table](/component-previews/table-narrow.svg)

### `table/sorted`

Sorted table

![Table — Sorted table](/component-previews/table-sorted.svg)

### `table/unicode`

Unicode table

![Table — Unicode table](/component-previews/table-unicode.svg)

## Usage

```rust
use std::num::NonZeroU16;
use ratatui_core::text::Line;
use termrock::{style::DesignTokens, Theme, input::{KeyCode, KeyEvent, KeyModifiers}, widgets::{CellAlignment, Column, ColumnWidth, Table, TableRow, TableState}};

let theme = Theme::default();
let columns = [Column { id: "name", title: Line::from("Name"), width: ColumnWidth::Fill(NonZeroU16::new(1).unwrap()), alignment: CellAlignment::Left, sortable: true, sort: None }];
let cells = [Line::from("termrock")];
let rows = [TableRow { id: "termrock", cells: &cells, leading: None, badge: None, enabled: true, emphasis: false, style: None }];
let tokens = DesignTokens::default();
let table = Table::new(&columns, &rows, &tokens);
let mut state = TableState::<&str, &str>::new(Some("termrock"));
state.set_focused(true);
let outcome = state.handle_key(&rows, KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `table/basic` — Data table
- `table/disabled` — Disabled table row
- `table/empty` — Empty table
- `table/narrow` — Narrow table
- `table/sorted` — Sorted table
- `table/unicode` — Unicode table
