---
title: Table
description: 'A stable-ID columnar data view with deterministic widths, sorting requests, and visible-window rendering.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="table/basic" interactive caption="Table · Data table — focus surface; use keys/click for states" />

## Usage

```rust
use std::num::NonZeroU16;
use ratatui_core::text::Line;
use termrock::{style::DesignSystem, input::{KeyCode, KeyEvent, KeyModifiers}, widgets::{CellAlignment, Column, ColumnWidth, Table, TableRow, TableState}};

let system = DesignSystem::default();
let columns = [Column { id: "name", title: Line::from("Name"), width: ColumnWidth::Fill(NonZeroU16::new(1).unwrap()), alignment: CellAlignment::Left, sortable: true, sort: None }];
let cells = [Line::from("termrock")];
let rows = [TableRow { id: "termrock", cells: &cells, leading: None, badge: None, enabled: true, emphasis: false, style: None }];
let system = DesignSystem::default();
let table = Table::new(&columns, &rows, &system);
let mut state = TableState::<&str, &str>::new(Some("termrock"));
state.set_focused(true);
let outcome = state.handle_key(&rows, KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `table/basic` | Data table | **Primary (live preview)** |
| `table/bordered` | Bordered table | bordered |
| `table/compact` | Compact table | Compact density |
| `table/disabled` | Disabled table row | Disabled |
| `table/empty` | Empty table | Empty state |
| `table/error` | Error table | Error |
| `table/loading` | Loading table | Loading state |
| `table/narrow` | Narrow table | Narrow terminal |
| `table/priority` | Priority columns | priority |
| `table/sorted` | Sorted table | sorted |
| `table/striped` | Striped table | striped |
| `table/unicode` | Unicode table | Unicode / wide glyphs |
