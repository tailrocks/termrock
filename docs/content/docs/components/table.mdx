---
title: Table
description: 'A stable-ID columnar data view with deterministic widths, sorting requests, and visible-window rendering.'
component: Table
demo: table/basic
interaction: selection-navigation
actions:
  - "arrow keys"
  - "Enter activate"
  - "click"
expectedOutcomes:
  - "Navigation moves visible focus or selection and preserves it for the next event."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/table.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="table/basic" interactive caption="Table · Data table — live shared Rust state" />

## Try it

- `arrow keys`: Move through the real focus/selection model; the next action uses the new target.
- `Enter activate`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `table/basic` | Data table | interactive |
| `table/bordered` | Bordered table | paint-only |
| `table/compact` | Compact table | paint-only |
| `table/disabled` | Disabled table row | paint-only |
| `table/empty` | Empty table | paint-only |
| `table/error` | Error table | paint-only |
| `table/loading` | Loading table | paint-only |
| `table/narrow` | Narrow table | paint-only |
| `table/priority` | Priority columns | paint-only |
| `table/sorted` | Sorted table | paint-only |
| `table/striped` | Striped table | paint-only |
| `table/unicode` | Unicode table | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `table/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn table_basic(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    render_table(frame, area, system, TableVariant::Basic);
}
```

## Migrated handbook guidance

# Table

**Purpose.** Present moderate-size relational data as a readable grid: headers,
alignment, widths, truncation, row selection, optional cell focus, and empty /
loading / error body states. TermRock owns layout, chrome, and interaction
geometry; the host owns rows, sort execution, wording, and effects.

For interactive 1M-row kits (cursor, filter execution, virtual windows), use
[DataTable](/docs/components/data-table).


## When to use / when not

| Use Table | Prefer |
|-----------|--------|
| Process lists, settings matrices, modest query results | — |
| Static or host-sorted projections | — |
| Huge virtual grids / filter kits | [DataTable](/docs/components/data-table) |
| Single-column navigation | [List](/docs/components/list) |
| Hierarchies | Tree |

## Installation

```toml
termrock = { git = "https://github.com/tailrocks/termrock.git", rev = "PIN" }
```

## Source files

`termrock::widgets::{Table, TableRow, TableState, Column, TableRecipe, TableBodyState}`

## Basic example

```rust
use ratatui_core::text::Line;
use termrock::{
    style::DesignSystem,
    widgets::{Column, ColumnWidth, Table, TableRow, TableState},
};

let system = DesignSystem::default();
let columns = [
    Column::new("name", "Name", ColumnWidth::Min(12)).priority(100),
    Column::new("cpu", "CPU", ColumnWidth::Fixed(6))
        .alignment(termrock::widgets::CellAlignment::Right)
        .priority(80),
];
let cells = [
    [Line::from("termrock"), Line::from("12%")],
    [Line::from("cargo"), Line::from("4%")],
];
let rows = [
    TableRow::new("r1", &cells[0]),
    TableRow::new("r2", &cells[1]),
];
let table = Table::new(&columns, &rows, &system)
    .empty_message(Line::from("No processes"));
let mut state = TableState::new(Some("r1"));
let _ = (table, &mut state);
```

## Recipes

| Recipe | Chrome |
|--------|--------|
| `Quiet` (default) | Gutter selection, no grid lines |
| `Bordered` | `│` between columns + rule under header |
| `Striped` | Alternate-row muted text |
| `Compact` | Gap 1 |

## Body states

`TableBodyState::Ready` (default), `Loading`, `Error` — sticky header remains;
body shows host `loading_message` / `error_message` / `empty_message`.

## Responsive columns

Set `Column::priority` (higher kept longer). Under width pressure, lowest
priority columns drop first; equal priority still collapses rightmost.

Horizontal overflow after layout uses `TableState::h_offset` / `scroll_horizontal`.
Left/Right keys move cell focus when a row is selected, else scroll horizontally.

## Anatomy

`gutter` · **sticky header** · body rows · each cell: alignment + overflow  
Selection: design-system `SelectionChrome` (gutter glyph / tint / fill)

## Related

- [DataTable](/docs/components/data-table) — interactive virtual kit
- [List](/docs/components/list) — single-column collections
- Migration `0188-v0.13.0-table-presentation`

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `table/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `table/basic` | Data table | **Primary (live preview)** |
| `table/bordered` | Bordered table | bordered |
| `table/compact` | Compact table | Compact density |
| `table/disabled` | Disabled table row | Disabled |
| `table/empty` | Empty table | Empty state |
| `table/error` | Error table | Error |
| `table/loading` | Loading table | Loading state |
| `table/narrow` | Narrow table | Narrow terminal |
| `table/priority` | Priority columns | priority |
| `table/sorted` | Sorted table | sorted |
| `table/striped` | Striped table | striped |
| `table/unicode` | Unicode table | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/table.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/table.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `table/basic`

## Seen in applications

<SeenInApplications component="Table" />
