---
title: QuickOpen
description: 'High-performance multi-provider fuzzy resource opener with async streaming, previews, query syntax, JumpMode and fullscreen integration.'
component: QuickOpen
demo: quick-open/basic
interaction: editor-form
actions:
  - "type to filter"
  - "↑↓ select"
  - "Enter open"
  - "Ctrl+N/P provider"
  - "click result"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/quick_open.rs
---


## Live terminal (Ghostty-class)

This preview mounts one persistent Rust component instance. Use the listed keyboard and pointer actions; each accepted action updates the same state and reports its typed outcome.

<TerminalPreview story="quick-open/basic" interactive caption="QuickOpen · QuickOpen files — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{QuickOpen, QuickOpenState, example_quick_open_files, example_quick_open_providers};

let system = DesignSystem::default();
let providers = example_quick_open_providers();
let items = example_quick_open_files();
let mut state = QuickOpenState::new();
state.set_focused(true);
let _ = state.apply_results(0, &items, true, None);
QuickOpen::new(&providers, &items, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `quick-open/basic` | QuickOpen files | **Primary (live preview)** |
| `quick-open/ascii` | QuickOpen ASCII | ASCII / low capability |
| `quick-open/empty` | QuickOpen empty | Empty state |
| `quick-open/fuzzy` | QuickOpen fuzzy | fuzzy |
| `quick-open/loading` | QuickOpen loading | Loading state |
| `quick-open/narrow` | QuickOpen narrow | Narrow terminal |
| `quick-open/symbols` | QuickOpen symbols | symbols |


## Try it

- Use the actions shown beside the terminal; mouse, keyboard, paste, drag, and resize are forwarded only when the component supports them.
- Confirm the visual state and typed outcome, then use **Reset** to mount a fresh instance.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `quick-open/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/quick_open.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/quick_open.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `quick-open/basic`
