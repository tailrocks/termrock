---
title: ErrorState
description: 'Canonical structured recoverable failure surface (see ErrorView). Recovery bundle, detail disclosure, recipes.'
component: ErrorState
demo: error-state/network
interaction: disclosure-overlay
actions:
  - "R retry"
  - "D details"
  - "C copy diagnostics"
  - "Tab choose action"
  - "Enter activate"
  - "click action"
expectedOutcomes:
  - "The \"R retry\" action updates visible mounted state and reports its typed outcome."
  - "The \"D details\" action updates visible mounted state and reports its typed outcome."
  - "The \"C copy diagnostics\" action updates visible mounted state and reports its typed outcome."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/error_state.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="error-state/network" interactive caption="ErrorState · ErrorState network — live shared Rust state" />

## Try it

- `R retry`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `D details`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `C copy diagnostics`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Tab choose action`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter activate`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click action`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `error-state/details` | ErrorState technical details | paint-only |
| `error-state/dialog` | ErrorState dialog | paint-only |
| `error-state/fullscreen` | ErrorState full-screen | paint-only |
| `error-state/inline` | ErrorState inline | paint-only |
| `error-state/network` | ErrorState network | interactive |
| `error-state/permission` | ErrorState permission | paint-only |
| `error-state/validation` | ErrorState validation | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `error-state/network`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn error_state_network_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    example_error_network(system).paint(area, frame.buffer_mut());
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `error-state/network`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `error-state/network` | ErrorState network | **Primary (live preview)** |
| `error-state/details` | ErrorState technical details | details |
| `error-state/dialog` | ErrorState dialog | dialog |
| `error-state/fullscreen` | ErrorState full-screen | fullscreen |
| `error-state/inline` | ErrorState inline | inline |
| `error-state/permission` | ErrorState permission | permission |
| `error-state/validation` | ErrorState validation | validation |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/error_state.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/error_state.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `error-state/network`
