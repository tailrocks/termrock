---
title: ErrorState
description: 'Canonical structured recoverable failure surface (see ErrorView). Recovery bundle, detail disclosure, recipes.'
component: ErrorState
demo: error-state/network
interaction: disclosure-overlay
actions:
  - "R retry"
  - "D details"
  - "C copy diagnostics"
  - "Tab choose action"
  - "Enter activate"
  - "click action"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/error_state.rs
---


## Live terminal (Ghostty-class)

This preview mounts one persistent Rust component instance. Use the listed keyboard and pointer actions; each accepted action updates the same state and reports its typed outcome.

<TerminalPreview story="error-state/network" interactive caption="ErrorState · ErrorState network — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::{ErrorState, example_error_network};
example_error_network(&system).paint(area, buf);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `error-state/network` | ErrorState network | **Primary (live preview)** |
| `error-state/details` | ErrorState technical details | details |
| `error-state/dialog` | ErrorState dialog | dialog |
| `error-state/fullscreen` | ErrorState full-screen | fullscreen |
| `error-state/inline` | ErrorState inline | inline |
| `error-state/permission` | ErrorState permission | permission |
| `error-state/validation` | ErrorState validation | validation |


## Try it

- Use the actions shown beside the terminal; mouse, keyboard, paste, drag, and resize are forwarded only when the component supports them.
- Confirm the visual state and typed outcome, then use **Reset** to mount a fresh instance.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `error-state/network`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/error_state.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/error_state.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `error-state/network`
