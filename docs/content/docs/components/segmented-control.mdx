---
title: SegmentedControl
description: 'SegmentedControl widget.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `segmented-control/basic`

SegmentedControl basic

![SegmentedControl — SegmentedControl basic](/component-previews/segmented-control-basic.svg)

### `segmented-control/collapsed`

SegmentedControl collapsed

![SegmentedControl — SegmentedControl collapsed](/component-previews/segmented-control-collapsed.svg)

### `segmented-control/icons`

SegmentedControl icons

![SegmentedControl — SegmentedControl icons](/component-previews/segmented-control-icons.svg)

### `segmented-control/narrow`

Narrow SegmentedControl

![SegmentedControl — Narrow SegmentedControl](/component-previews/segmented-control-narrow.svg)

### `segmented-control/overflow`

SegmentedControl overflow

![SegmentedControl — SegmentedControl overflow](/component-previews/segmented-control-overflow.svg)

### `segmented-control/unicode`

Unicode SegmentedControl

![SegmentedControl — Unicode SegmentedControl](/component-previews/segmented-control-unicode.svg)

## Usage

```rust
use termrock::widgets::SegmentedControl;

// See handbook / lookbook story segmented-control/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `segmented-control/basic` — SegmentedControl basic
- `segmented-control/collapsed` — SegmentedControl collapsed
- `segmented-control/icons` — SegmentedControl icons
- `segmented-control/narrow` — Narrow SegmentedControl
- `segmented-control/overflow` — SegmentedControl overflow
- `segmented-control/unicode` — Unicode SegmentedControl
