---
title: SegmentedControl
description: 'SegmentedControl widget.'
component: SegmentedControl
demo: segmented-control/basic
interaction: selection-navigation
actions:
  - "←→ change view"
  - "Home/End"
  - "click segment"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/segmented_control.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="segmented-control/basic" interactive caption="SegmentedControl · SegmentedControl basic — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::SegmentedControl;

// See handbook / lookbook story segmented-control/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `segmented-control/basic` | SegmentedControl basic | **Primary (live preview)** |
| `segmented-control/collapsed` | SegmentedControl collapsed | collapsed |
| `segmented-control/icons` | SegmentedControl icons | icons |
| `segmented-control/narrow` | Narrow SegmentedControl | Narrow terminal |
| `segmented-control/overflow` | SegmentedControl overflow | Overflow |
| `segmented-control/unicode` | Unicode SegmentedControl | Unicode / wide glyphs |


## Try it

- `←→ change view`: routes through the component public interaction API.
- `Home/End`: routes through the component public interaction API.
- `click segment`: routes through the component public interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `segmented-control/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/segmented_control.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/segmented_control.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `segmented-control/basic`
