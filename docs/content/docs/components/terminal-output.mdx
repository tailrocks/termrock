---
title: TerminalOutput
description: 'TerminalOutput widget.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `terminal-output/running`

TerminalOutput running

![TerminalOutput — TerminalOutput running](/component-previews/terminal-output-running.svg)

### `terminal-output/ascii`

TerminalOutput ASCII

![TerminalOutput — TerminalOutput ASCII](/component-previews/terminal-output-ascii.svg)

### `terminal-output/compact`

TerminalOutput compact

![TerminalOutput — TerminalOutput compact](/component-previews/terminal-output-compact.svg)

### `terminal-output/empty`

TerminalOutput empty

![TerminalOutput — TerminalOutput empty](/component-previews/terminal-output-empty.svg)

### `terminal-output/env`

TerminalOutput env

![TerminalOutput — TerminalOutput env](/component-previews/terminal-output-env.svg)

### `terminal-output/failed`

TerminalOutput failed

![TerminalOutput — TerminalOutput failed](/component-previews/terminal-output-failed.svg)

### `terminal-output/narrow`

TerminalOutput narrow

![TerminalOutput — TerminalOutput narrow](/component-previews/terminal-output-narrow.svg)

### `terminal-output/pinned`

TerminalOutput pinned

![TerminalOutput — TerminalOutput pinned](/component-previews/terminal-output-pinned.svg)

## Usage

```rust
use termrock::widgets::TerminalOutput;

// See handbook / lookbook story terminal-output/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `terminal-output/ascii` — TerminalOutput ASCII
- `terminal-output/compact` — TerminalOutput compact
- `terminal-output/empty` — TerminalOutput empty
- `terminal-output/env` — TerminalOutput env
- `terminal-output/failed` — TerminalOutput failed
- `terminal-output/narrow` — TerminalOutput narrow
- `terminal-output/pinned` — TerminalOutput pinned
- `terminal-output/running` — TerminalOutput running
