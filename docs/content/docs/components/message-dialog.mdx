---
title: MessageDialog
description: 'A message dialog composed with optional scrollable detail rows.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `message-dialog/details`

Detailed message dialog

![MessageDialog — Detailed message dialog](/component-previews/message-dialog-details.svg)

### `message-dialog/narrow`

Narrow MessageDialog

![MessageDialog — Narrow MessageDialog](/component-previews/message-dialog-narrow.svg)

### `message-dialog/unicode`

Unicode MessageDialog

![MessageDialog — Unicode MessageDialog](/component-previews/message-dialog-unicode.svg)

## Usage

```rust
use ratatui_core::text::Text;
use termrock::{style::DesignSystem, widgets::{DetailCapability, DetailRow, DetailTableState, Dialog, MessageDialog}};

let system = DesignSystem::default();
let details = [DetailRow { id: "stage", label: "Stage", value: "Build", href: None, capability: DetailCapability::None, emphasis: false, style: None }];
let dialog = MessageDialog::new(Dialog::new("Failure", Text::from("Build failed"), &system), &details, &system).wrap(true);
let mut state = DetailTableState::<&str>::default();
let outcome = state.select_next(&details);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | caller-owned |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `message-dialog/details` — Detailed message dialog
- `message-dialog/narrow` — Narrow MessageDialog
- `message-dialog/unicode` — Unicode MessageDialog
