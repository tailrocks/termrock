---
title: DetailTable
description: A selectable key/value table with stable rows and typed activation capabilities.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![DetailTable — Detail table](/component-previews/detail-table-basic.svg)

## Usage

```rust
use termrock::{Theme, widgets::{DetailCapability, DetailRow, DetailTable, DetailTableState}};

let theme = Theme::default();
let rows = [DetailRow { id: "url", label: "URL", value: "https://example.com", href: Some("https://example.com"), capability: DetailCapability::Link, emphasis: false, style: None }];
let table = DetailTable::new(&rows, &theme).wrap(true);
let mut state = DetailTableState::<&str>::default();
let outcome = state.select_next(&rows);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `detail-table/basic` — Detail table
- `detail-table/unicode` — Unicode detail table
