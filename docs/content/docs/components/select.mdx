---
title: Select
description: 'A single-choice select with CollectionState navigation, value≠highlight, recipes, and popover/fullscreen.'
component: Select
demo: select/basic
interaction: disclosure-overlay
actions:
  - "Enter/click open"
  - "↑↓ highlight"
  - "type to filter"
  - "Enter select"
  - "Esc close"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/select.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="select/basic" interactive caption="Select · Select — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Select, SelectOption, SelectState};

let system = DesignSystem::default();
let options = [
  SelectOption::option("apple", "Apple"),
  SelectOption::option("banana", "Banana"),
];
let mut state = SelectState::new().with_value("apple");
state.set_focused(true);
let _ = Select::new(&options, &system).label("Fruit");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `select/basic` | Select | **Primary (live preview)** |
| `select/compact` | Select compact | Compact density |
| `select/open` | Select open | Open / expanded |
| `select/search` | Select search | Search / filter |


## Try it

- `Enter/click open`: routes through the component public interaction API.
- `↑↓ highlight`: routes through the component public interaction API.
- `type to filter`: routes through the component public interaction API.
- `Enter select`: routes through the component public interaction API.
- `Esc close`: routes through the component public interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `select/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/select.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/select.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `select/basic`

## Migrated handbook guidance

# Select

**Purpose.** Compact form/toolbar control that opens a navigable option list.


## Value vs highlight

| | Meaning |
|--|---------|
| `value()` | Committed selection |
| `highlight()` | Open-list keyboard cursor |

Esc closes without committing. Enter commits highlight.

## Host overlay

```rust
match state.handle_key(key, &options, bounds) {
    SelectOutcome::Opened { presentation } => match presentation {
        SelectPresentation::Popover => open_popover_overlay(...),
        SelectPresentation::Fullscreen => { /* fullscreen panel */ }
        _ => {}
    },
    SelectOutcome::ValueChanged { id } => host.set(id),
    _ => {}
}
```

## Recipes

- **Inline** — default field
- **Form** — label row
- **Compact** — toolbar

## Related

RadioGroup · Picker · CollectionState · Popover · migration `0145`
