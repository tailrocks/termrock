---
title: Select
description: 'A single-choice select with CollectionState navigation, value≠highlight, recipes, and popover/fullscreen.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `select/basic`

Select

![Select — Select](/component-previews/select-basic.svg)

### `select/compact`

Select compact

![Select — Select compact](/component-previews/select-compact.svg)

### `select/open`

Select open

![Select — Select open](/component-previews/select-open.svg)

### `select/search`

Select search

![Select — Select search](/component-previews/select-search.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Select, SelectOption, SelectState};

let system = DesignSystem::default();
let options = [
  SelectOption::option("apple", "Apple"),
  SelectOption::option("banana", "Banana"),
];
let mut state = SelectState::new().with_value("apple");
state.set_focused(true);
let _ = Select::new(&options, &system).label("Fruit");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `select/basic` — Select
- `select/compact` — Select compact
- `select/open` — Select open
- `select/search` — Select search
