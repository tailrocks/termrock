---
title: ActionBar
description: 'A horizontal group of stable, caller-owned actions with painted hit regions.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="action-bar/basic" interactive caption="ActionBar · Action bar — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{Action, ActionBar, ActionBarState}};

let system = DesignSystem::default();
let actions = [Action { id: "save", label: "Save", enabled: true, style: None }];
let bar = ActionBar::new(&actions, &system);
let state = ActionBarState { focused: Some("save"), ..ActionBarState::default() };
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `action-bar/basic` | Action bar | **Primary (live preview)** |
| `action-bar/narrow` | Narrow ActionBar | Narrow terminal |
| `action-bar/unicode` | Unicode ActionBar | Unicode / wide glyphs |
