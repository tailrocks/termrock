---
title: ActionBar
description: 'A horizontal group of stable, caller-owned actions with painted hit regions.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `action-bar/basic`

Action bar

![ActionBar — Action bar](/component-previews/action-bar-basic.svg)

### `action-bar/narrow`

Narrow ActionBar

![ActionBar — Narrow ActionBar](/component-previews/action-bar-narrow.svg)

### `action-bar/unicode`

Unicode ActionBar

![ActionBar — Unicode ActionBar](/component-previews/action-bar-unicode.svg)

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{Action, ActionBar, ActionBarState}};

let system = DesignSystem::default();
let actions = [Action { id: "save", label: "Save", enabled: true, style: None }];
let bar = ActionBar::new(&actions, &system);
let state = ActionBarState { focused: Some("save"), ..ActionBarState::default() };
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `action-bar/basic` — Action bar
- `action-bar/narrow` — Narrow ActionBar
- `action-bar/unicode` — Unicode ActionBar
