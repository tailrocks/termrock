---
title: DiagnosticView
description: 'DiagnosticView widget.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="diagnostic/list" interactive caption="DiagnosticView · Diagnostic list — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::widgets::DiagnosticView;

// See handbook / lookbook story diagnostic-view/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `diagnostic/list` | Diagnostic list | **Primary (live preview)** |
| `diagnostic/ascii` | Diagnostic ASCII | ASCII / low capability |
| `diagnostic/empty` | Diagnostic empty | Empty state |
| `diagnostic/full` | Diagnostic code frame | full |
| `diagnostic/inline` | Diagnostic inline | inline |
| `diagnostic/narrow` | Diagnostic narrow | Narrow terminal |
