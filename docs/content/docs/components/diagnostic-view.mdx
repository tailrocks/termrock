---
title: DiagnosticView
description: 'DiagnosticView widget.'
component: DiagnosticView
demo: diagnostic/list
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/diagnostic.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="diagnostic/list" interactive caption="DiagnosticView · Diagnostic list — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::DiagnosticView;

// See handbook / lookbook story diagnostic-view/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `diagnostic/list` | Diagnostic list | **Primary (live preview)** |
| `diagnostic/ascii` | Diagnostic ASCII | ASCII / low capability |
| `diagnostic/empty` | Diagnostic empty | Empty state |
| `diagnostic/full` | Diagnostic code frame | full |
| `diagnostic/inline` | Diagnostic inline | inline |
| `diagnostic/narrow` | Diagnostic narrow | Narrow terminal |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `diagnostic/list`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/diagnostic.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/diagnostic.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `diagnostic/list`
