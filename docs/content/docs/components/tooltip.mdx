---
title: Tooltip
description: 'Delayed non-focus-stealing contextual help with pointer/focus triggers, plain/shortcut/rich variants, reduced-motion, and essential-elsewhere policy.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `tooltip/plain`

Tooltip plain

![Tooltip — Tooltip plain](/component-previews/tooltip-plain.svg)

### `tooltip/ascii`

Tooltip ASCII

![Tooltip — Tooltip ASCII](/component-previews/tooltip-ascii.svg)

### `tooltip/rich`

Tooltip rich

![Tooltip — Tooltip rich](/component-previews/tooltip-rich.svg)

### `tooltip/shortcut`

Tooltip shortcut

![Tooltip — Tooltip shortcut](/component-previews/tooltip-shortcut.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Tooltip, TooltipContent, TooltipState};

let system = DesignSystem::default();
let mut state = TooltipState::new();
// host: state.set_pointer_over / set_focus_within + advance(tick, motion)
Tooltip::content(
    TooltipContent::plain("Help").essential_elsewhere(true),
    &system,
).paint(area, buf, &state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `tooltip/ascii` — Tooltip ASCII
- `tooltip/plain` — Tooltip plain
- `tooltip/rich` — Tooltip rich
- `tooltip/shortcut` — Tooltip shortcut
