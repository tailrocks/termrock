---
title: Tooltip
description: 'Delayed non-focus-stealing contextual help with pointer/focus triggers, plain/shortcut/rich variants, reduced-motion, and essential-elsewhere policy.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="tooltip/plain" interactive caption="Tooltip · Tooltip plain — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Tooltip, TooltipContent, TooltipState};

let system = DesignSystem::default();
let mut state = TooltipState::new();
// host: state.set_pointer_over / set_focus_within + advance(tick, motion)
Tooltip::content(
    TooltipContent::plain("Help").essential_elsewhere(true),
    &system,
).paint(area, buf, &state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `tooltip/plain` | Tooltip plain | **Primary (live preview)** |
| `tooltip/ascii` | Tooltip ASCII | ASCII / low capability |
| `tooltip/rich` | Tooltip rich | rich |
| `tooltip/shortcut` | Tooltip shortcut | shortcut |
