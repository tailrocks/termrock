---
title: ThemePicker
description: A live theme preset list; selection changes drive caller re-render.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![ThemePicker — Theme picker](/component-previews/theme-picker-basic.svg)

## Usage

```rust
use termrock::{Theme, widgets::{BUILTIN_THEME_PRESETS, ThemePicker, ThemePickerState, theme_from_preset_id}};

let theme = Theme::default();
let picker = ThemePicker::new(BUILTIN_THEME_PRESETS, &theme);
let mut state = ThemePickerState::new(0);
let preview = theme_from_preset_id("slate");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | not-applicable |
| Narrow terminal | not-applicable |

## Stories

- `theme-picker/basic` — Theme picker
