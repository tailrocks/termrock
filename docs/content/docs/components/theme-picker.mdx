---
title: ThemePicker
description: 'A live theme preset list; selection changes drive caller re-render.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `theme-picker/basic`

Theme picker

![ThemePicker — Theme picker](/component-previews/theme-picker-basic.svg)

### `theme-picker/narrow`

Narrow ThemePicker

![ThemePicker — Narrow ThemePicker](/component-previews/theme-picker-narrow.svg)

### `theme-picker/unicode`

Unicode ThemePicker

![ThemePicker — Unicode ThemePicker](/component-previews/theme-picker-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::{BUILTIN_THEME_PRESETS, ThemePicker, ThemePickerState, theme_from_preset_id}};

let theme = Theme::default();
let picker = ThemePicker::new(BUILTIN_THEME_PRESETS, &theme);
let mut state = ThemePickerState::new(0);
let preview = theme_from_preset_id("slate");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `theme-picker/basic` — Theme picker
- `theme-picker/narrow` — Narrow ThemePicker
- `theme-picker/unicode` — Unicode ThemePicker
