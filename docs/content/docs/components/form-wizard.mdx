---
title: FormWizard
description: 'Multi-step form wizard chrome; domain fields and validation stay consumer-owned.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `blocks/form-wizard`

FormWizard

![FormWizard — FormWizard](/component-previews/blocks-form-wizard.svg)

### `blocks/narrow`

Narrow FormWizard

![FormWizard — Narrow FormWizard](/component-previews/blocks-narrow.svg)

### `blocks/unicode`

Unicode FormWizard

![FormWizard — Unicode FormWizard](/component-previews/blocks-unicode.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{FormWizard, FormWizardState}};

let tokens = DesignTokens::default();
let mut state = FormWizardState::new(3);
let wizard = FormWizard::new(&tokens, "Setup");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | not-applicable |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `blocks/form-wizard` — FormWizard
- `blocks/narrow` — Narrow FormWizard
- `blocks/unicode` — Unicode FormWizard
