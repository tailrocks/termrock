---
title: FormWizard
description: 'Multi-step form wizard chrome with stepper, validation gates, review, failure/retry, and resume. Domain fields stay consumer-owned.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `blocks/form-wizard`

FormWizard

![FormWizard — FormWizard](/component-previews/blocks-form-wizard.svg)

### `form-wizard/review`

FormWizard review

![FormWizard — FormWizard review](/component-previews/form-wizard-review.svg)

### `form-wizard/failure`

FormWizard failure

![FormWizard — FormWizard failure](/component-previews/form-wizard-failure.svg)

### `form-wizard/resume`

FormWizard resume

![FormWizard — FormWizard resume](/component-previews/form-wizard-resume.svg)

### `blocks/narrow`

Narrow FormWizard

![FormWizard — Narrow FormWizard](/component-previews/blocks-narrow.svg)

### `blocks/unicode`

Unicode FormWizard

![FormWizard — Unicode FormWizard](/component-previews/blocks-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{FormWizard, FormWizardState, WizardStep};

let system = DesignSystem::default();
let mut state = FormWizardState::with_steps([
    WizardStep::new("account", "Account"),
    WizardStep::new("region", "Region").optional(true),
    WizardStep::new("confirm", "Confirm"),
]);
state.set_focused(true);
let _ = FormWizard::new(&system).title("Setup");
// paint wizard; then host fields into state.body_area()
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `blocks/form-wizard` — FormWizard
- `form-wizard/review` — FormWizard review
- `form-wizard/failure` — FormWizard failure
- `form-wizard/resume` — FormWizard resume
- `blocks/narrow` — Narrow FormWizard
- `blocks/unicode` — Unicode FormWizard
