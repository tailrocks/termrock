---
title: CheckpointTimeline
description: 'Rewindable session history — browse/preview (draft preserved), restore/rewind with safe confirm and boundary warnings.'
component: CheckpointTimeline
demo: checkpoint-timeline/basic
interaction: selection-navigation
actions:
  - "↑↓ select"
  - "Enter preview/confirm"
  - "R restore"
  - "W rewind"
  - "C compare"
  - "F follow"
  - "click row"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/checkpoint_timeline.rs
---


## Live terminal (Ghostty-class)

This preview mounts one persistent Rust component instance. Use the listed keyboard and pointer actions; each accepted action updates the same state and reports its typed outcome.

<TerminalPreview story="checkpoint-timeline/basic" interactive caption="CheckpointTimeline · CheckpointTimeline — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::{
    example_checkpoints, CheckpointTimeline, CheckpointTimelineState,
    CheckpointTimelineOutcome,
};

let mut state = CheckpointTimelineState::new();
state.set_checkpoints(example_checkpoints());
CheckpointTimeline::new(&system).paint(area, buf, &mut state);
match state.handle_key(key) {
    CheckpointTimelineOutcome::RestoreRequested { id } => { let _ = id; }
    CheckpointTimelineOutcome::Cancelled => {}
    _ => {}
}
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `checkpoint-timeline/basic` | CheckpointTimeline | **Primary (live preview)** |
| `checkpoint-timeline/boundaries` | CheckpointTimeline boundaries | boundaries |
| `checkpoint-timeline/confirm` | CheckpointTimeline confirm | confirm |
| `checkpoint-timeline/narrow` | Narrow CheckpointTimeline | Narrow terminal |
| `checkpoint-timeline/preview` | CheckpointTimeline preview | preview |
| `checkpoint-timeline/unicode` | Unicode CheckpointTimeline | Unicode / wide glyphs |


## Try it

- Use the actions shown beside the terminal; mouse, keyboard, paste, drag, and resize are forwarded only when the component supports them.
- Confirm the visual state and typed outcome, then use **Reset** to mount a fresh instance.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `checkpoint-timeline/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/checkpoint_timeline.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/checkpoint_timeline.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `checkpoint-timeline/basic`

## Migrated handbook guidance

# CheckpointTimeline

Session checkpoint browser. Outcomes are **requests only**; host owns
persistence and restore I/O. **Never** clears PromptComposer draft.


## When to use / when not

| Use CheckpointTimeline | Prefer |
|------------------------|--------|
| Agent session restore / rewind | — |
| General deploy/CI chronology | [Timeline](/docs/components/timeline) |
| Field value history | [HistoryPicker](/docs/components/history-picker) (if present) |
| Patch compare | [DiffReview](/docs/components/diff-review) via CompareRequested |

## Modes

| Mode | Mutates session? |
|------|------------------|
| Browse | No |
| Preview | No (draft preserved) |
| Confirm | Request only — host executes |

Confirm defaults focus to **Cancel**.

## Boundaries

Soft · Dirty workspace · External effects · Irreversible (blocked)

## Keys

j/k · Enter preview · r restore · w rewind · c compare · Esc · confirm ←/→ Enter

## Related

Timeline · DiffReview · migration `0229`
