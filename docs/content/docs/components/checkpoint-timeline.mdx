---
title: CheckpointTimeline
description: 'Rewindable session history — browse/preview (draft preserved), restore/rewind with safe confirm and boundary warnings.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `checkpoint-timeline/basic`

CheckpointTimeline

![CheckpointTimeline — CheckpointTimeline](/component-previews/checkpoint-timeline-basic.svg)

### `checkpoint-timeline/boundaries`

CheckpointTimeline boundaries

![CheckpointTimeline — CheckpointTimeline boundaries](/component-previews/checkpoint-timeline-boundaries.svg)

### `checkpoint-timeline/confirm`

CheckpointTimeline confirm

![CheckpointTimeline — CheckpointTimeline confirm](/component-previews/checkpoint-timeline-confirm.svg)

### `checkpoint-timeline/narrow`

Narrow CheckpointTimeline

![CheckpointTimeline — Narrow CheckpointTimeline](/component-previews/checkpoint-timeline-narrow.svg)

### `checkpoint-timeline/preview`

CheckpointTimeline preview

![CheckpointTimeline — CheckpointTimeline preview](/component-previews/checkpoint-timeline-preview.svg)

### `checkpoint-timeline/unicode`

Unicode CheckpointTimeline

![CheckpointTimeline — Unicode CheckpointTimeline](/component-previews/checkpoint-timeline-unicode.svg)

## Usage

```rust
use termrock::widgets::{
    example_checkpoints, CheckpointTimeline, CheckpointTimelineState,
    CheckpointTimelineOutcome,
};

let mut state = CheckpointTimelineState::new();
state.set_checkpoints(example_checkpoints());
CheckpointTimeline::new(&system).paint(area, buf, &mut state);
match state.handle_key(key) {
    CheckpointTimelineOutcome::RestoreRequested { id } => { let _ = id; }
    CheckpointTimelineOutcome::Cancelled => {}
    _ => {}
}
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `checkpoint-timeline/basic` — CheckpointTimeline
- `checkpoint-timeline/boundaries` — CheckpointTimeline boundaries
- `checkpoint-timeline/confirm` — CheckpointTimeline confirm
- `checkpoint-timeline/narrow` — Narrow CheckpointTimeline
- `checkpoint-timeline/preview` — CheckpointTimeline preview
- `checkpoint-timeline/unicode` — Unicode CheckpointTimeline
