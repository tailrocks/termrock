---
title: CheckpointTimeline
description: 'Rewindable session history with safe restore/rewind confirms.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `checkpoint-timeline/basic`

CheckpointTimeline

![CheckpointTimeline — basic](/component-previews/checkpoint-timeline-basic.svg)

### `checkpoint-timeline/preview`

CheckpointTimeline preview

![CheckpointTimeline — preview](/component-previews/checkpoint-timeline-preview.svg)

### `checkpoint-timeline/confirm`

CheckpointTimeline confirm

![CheckpointTimeline — confirm](/component-previews/checkpoint-timeline-confirm.svg)

### `checkpoint-timeline/boundaries`

CheckpointTimeline boundaries

![CheckpointTimeline — boundaries](/component-previews/checkpoint-timeline-boundaries.svg)

### `checkpoint-timeline/narrow`

Narrow CheckpointTimeline

![CheckpointTimeline — narrow](/component-previews/checkpoint-timeline-narrow.svg)

### `checkpoint-timeline/unicode`

Unicode CheckpointTimeline

![CheckpointTimeline — unicode](/component-previews/checkpoint-timeline-unicode.svg)

## Usage

```rust
use termrock::widgets::{
    example_checkpoints, CheckpointTimeline, CheckpointTimelineOutcome,
    CheckpointTimelineState,
};

let mut state = CheckpointTimelineState::new();
state.set_checkpoints(example_checkpoints());
CheckpointTimeline::new(&system).paint(area, buf, &mut state);

match state.handle_key(key) {
    CheckpointTimelineOutcome::RestoreRequested { id } => { let _ = id; }
    CheckpointTimelineOutcome::RewindRequested { id } => { let _ = id; }
    CheckpointTimelineOutcome::Cancelled => {}
    _ => {}
}
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `checkpoint-timeline/basic` — browse
- `checkpoint-timeline/preview` — preview
- `checkpoint-timeline/confirm` — confirm Cancel default
- `checkpoint-timeline/boundaries` — warnings
- `checkpoint-timeline/narrow` — narrow
- `checkpoint-timeline/unicode` — unicode
