---
title: Stepper
description: 'Progress/navigation chrome for multi-step flows with status marks, horizontal/vertical layout, policy-gated jumps, and narrow numeric/menu contraction.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `stepper/horizontal`

Stepper horizontal

![Stepper — Stepper horizontal](/component-previews/stepper-horizontal.svg)

### `stepper/ascii`

Stepper ASCII

![Stepper — Stepper ASCII](/component-previews/stepper-ascii.svg)

### `stepper/error`

Stepper error state

![Stepper — Stepper error state](/component-previews/stepper-error.svg)

### `stepper/menu`

Stepper menu

![Stepper — Stepper menu](/component-previews/stepper-menu.svg)

### `stepper/numeric`

Stepper numeric

![Stepper — Stepper numeric](/component-previews/stepper-numeric.svg)

### `stepper/vertical`

Stepper vertical

![Stepper — Stepper vertical](/component-previews/stepper-vertical.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Stepper, StepperState, StepperNavPolicy, example_onboarding_steps};

let system = DesignSystem::default();
let items = example_onboarding_steps();
let mut state = StepperState::with_len(items.len()).policy(StepperNavPolicy::Linear);
state.set_focused(true);
Stepper::new(&items, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `stepper/ascii` — Stepper ASCII
- `stepper/error` — Stepper error state
- `stepper/horizontal` — Stepper horizontal
- `stepper/menu` — Stepper menu
- `stepper/numeric` — Stepper numeric
- `stepper/vertical` — Stepper vertical
