---
title: Stepper
description: 'Progress/navigation chrome for multi-step flows with status marks, horizontal/vertical layout, policy-gated jumps, and narrow numeric/menu contraction.'
component: Stepper
demo: stepper/horizontal
interaction: selection-navigation
actions:
  - "←→ choose step"
  - "Enter activate"
  - "1–9 jump"
  - "click step"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"1–9 jump\" action updates visible mounted state and reports its typed outcome."
source: crates/termrock/src/widgets/stepper.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="stepper/horizontal" interactive caption="Stepper · Stepper horizontal — live shared Rust state" />

## Try it

- `←→ choose step`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter activate`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `1–9 jump`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `click step`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `stepper/ascii` | Stepper ASCII | paint-only |
| `stepper/error` | Stepper error state | paint-only |
| `stepper/horizontal` | Stepper horizontal | interactive |
| `stepper/menu` | Stepper menu | paint-only |
| `stepper/numeric` | Stepper numeric | paint-only |
| `stepper/vertical` | Stepper vertical | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `stepper/horizontal`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn stepper_horizontal_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let items = example_onboarding_steps();
    let mut state = StepperState::with_len(items.len()).policy(StepperNavPolicy::Linear);
    state.set_focused(true);
    state.set_status(0, StepStatus::Complete);
    state.set_current(1, items.len(), true);
    Stepper::new(&items, system).paint(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `stepper/horizontal`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `stepper/horizontal` | Stepper horizontal | **Primary (live preview)** |
| `stepper/ascii` | Stepper ASCII | ASCII / low capability |
| `stepper/error` | Stepper error state | Error |
| `stepper/menu` | Stepper menu | menu |
| `stepper/numeric` | Stepper numeric | numeric |
| `stepper/vertical` | Stepper vertical | vertical |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/stepper.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/stepper.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `stepper/horizontal`

## Seen in applications

<SeenInApplications component="Stepper" />
