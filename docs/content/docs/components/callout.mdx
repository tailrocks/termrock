---
title: Callout
description: 'Inline feedback with tone gutter rail, title/description/details/source, compact or section recipes; readable in ASCII/no-color.'
component: Callout
demo: callout/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/callout.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="callout/basic" interactive caption="Callout · Callout warning — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Callout, CalloutTone};

let system = DesignSystem::default();
let callout = Callout::new("Heads up", &system)
    .body("Non-color risk glyph present.")
    .tone(CalloutTone::Warning)
    .source("validator");
// Section: .section() · ASCII: .ascii(true)
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | caller-owned |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `callout/basic` | Callout warning | **Primary (live preview)** |
| `callout/narrow` | Narrow Callout | Narrow terminal |
| `callout/section` | Callout section | section |
| `callout/tones` | Callout tones | tones |
| `callout/unicode` | Unicode Callout | Unicode / wide glyphs |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `callout/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/callout.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/callout.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `callout/basic`
