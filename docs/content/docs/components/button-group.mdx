---
title: ButtonGroup
description: 'ButtonGroup widget.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="button-group/dialog" interactive caption="ButtonGroup · ButtonGroup dialog — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::widgets::ButtonGroup;

// See handbook / lookbook story button-group/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `button-group/dialog` | ButtonGroup dialog | **Primary (live preview)** |
| `button-group/connected` | ButtonGroup connected | connected |
| `button-group/loading` | ButtonGroup loading | Loading state |
| `button-group/narrow` | Narrow ButtonGroup | Narrow terminal |
| `button-group/overflow` | ButtonGroup overflow | Overflow |
| `button-group/unicode` | Unicode ButtonGroup | Unicode / wide glyphs |
