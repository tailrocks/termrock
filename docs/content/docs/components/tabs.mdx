---
title: Tabs
description: 'A composable tab strip with roving focus, manual/automatic activation, badges/status, close/reorder hooks, and narrow overflow/Select contraction. Panel state stays host-owned by id.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `tabs/status`

Tabs

![Tabs — Tabs](/component-previews/tabs-status.svg)

### `tabs/overflow`

Tabs overflow

![Tabs — Tabs overflow](/component-previews/tabs-overflow.svg)

### `tabs/vertical`

Tabs vertical

![Tabs — Tabs vertical](/component-previews/tabs-vertical.svg)

### `tabs/manual`

Tabs manual

![Tabs — Tabs manual](/component-previews/tabs-manual.svg)

### `tabs/closable`

Tabs closable

![Tabs — Tabs closable](/component-previews/tabs-closable.svg)

### `tabs/narrow`

Narrow tabs

![Tabs — Narrow tabs](/component-previews/tabs-narrow.svg)

### `tabs/unicode`

Unicode Tabs

![Tabs — Unicode Tabs](/component-previews/tabs-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Tab, TabStatus, Tabs, TabsState};

let system = DesignSystem::default();
let tabs = [
    Tab::new("logs", "Logs").status(TabStatus::Running).closable(true),
];
let mut state = TabsState::new().with_selected("logs");
state.set_focused(true);
let _ = Tabs::new(&tabs, &system).gap(1);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `tabs/status` — Tabs
- `tabs/overflow` — Tabs overflow
- `tabs/vertical` — Tabs vertical
- `tabs/manual` — Tabs manual
- `tabs/closable` — Tabs closable
- `tabs/narrow` — Narrow tabs
- `tabs/unicode` — Unicode Tabs
