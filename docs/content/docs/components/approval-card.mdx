---
title: ApprovalCard
description: A blocking permission card with risk tiers and semantic decisions.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![ApprovalCard — Approval card](/component-previews/approval-card-basic.svg)

## Usage

```rust
use termrock::{Theme, input::{KeyCode, KeyEvent, KeyModifiers}, widgets::{ApprovalCard, ApprovalCardState, ApprovalRisk}};

let theme = Theme::default();
let card = ApprovalCard::new("Permission", "Run cargo publish?", ApprovalRisk::High, &theme);
let mut state = ApprovalCardState::new();
let decision = state.handle_key(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | not-applicable |
| Narrow terminal | not-applicable |

## Stories

- `approval-card/basic` — Approval card
