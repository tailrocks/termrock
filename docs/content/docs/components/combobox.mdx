---
title: Combobox
description: 'An editable input with CompletionMenu suggestions, async generation gating, and separate draft/active/value.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `combobox/basic`

Combobox

![Combobox — Combobox](/component-previews/combobox-basic.svg)

### `combobox/open`

Combobox open

![Combobox — Combobox open](/component-previews/combobox-open.svg)

### `combobox/loading`

Combobox loading

![Combobox — Combobox loading](/component-previews/combobox-loading.svg)

### `autocomplete/basic`

Autocomplete

![Autocomplete — Autocomplete](/component-previews/autocomplete-basic.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Combobox, ComboboxState, CompletionCandidate};

let system = DesignSystem::default();
let mut state: ComboboxState<&str> = ComboboxState::new();
state.set_focused(true);
// DraftChanged { generation } → host fetch → apply_suggestions(generation, …)
let _ = Combobox::new(&system).label("Language");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `combobox/basic` — Combobox
- `combobox/open` — Combobox open
- `combobox/loading` — Combobox loading
- `autocomplete/basic` — Autocomplete
