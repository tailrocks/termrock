---
title: MenuBar
description: 'Desktop-style top-level menus with nested cascade, mnemonics, checked/radio rows, OverlayStack helpers, and narrow CommandPalette replacement.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `menu-bar/basic`

MenuBar basic

![MenuBar — MenuBar basic](/component-previews/menu-bar-basic.svg)

### `menu-bar/ascii`

MenuBar ASCII

![MenuBar — MenuBar ASCII](/component-previews/menu-bar-ascii.svg)

### `menu-bar/mnemonic`

MenuBar mnemonic mode

![MenuBar — MenuBar mnemonic mode](/component-previews/menu-bar-mnemonic.svg)

### `menu-bar/narrow`

MenuBar narrow palette

![MenuBar — MenuBar narrow palette](/component-previews/menu-bar-narrow.svg)

### `menu-bar/nested`

MenuBar nested dismiss

![MenuBar — MenuBar nested dismiss](/component-previews/menu-bar-nested.svg)

### `menu-bar/open`

MenuBar open cascade

![MenuBar — MenuBar open cascade](/component-previews/menu-bar-open.svg)

### `menu-bar/unicode`

MenuBar Unicode

![MenuBar — MenuBar Unicode](/component-previews/menu-bar-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{MenuBar, MenuBarState, example_app_menus};

let system = DesignSystem::default();
let menus = example_app_menus();
let mut state = MenuBarState::new();
state.set_focused(true);
let _ = state.open_menu_at(&menus, 0);
MenuBar::new(&menus, &system).paint_all(bar_area, bounds, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `menu-bar/ascii` — MenuBar ASCII
- `menu-bar/basic` — MenuBar basic
- `menu-bar/mnemonic` — MenuBar mnemonic mode
- `menu-bar/narrow` — MenuBar narrow palette
- `menu-bar/nested` — MenuBar nested dismiss
- `menu-bar/open` — MenuBar open cascade
- `menu-bar/unicode` — MenuBar Unicode
