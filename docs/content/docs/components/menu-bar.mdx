---
title: MenuBar
description: 'Desktop-style top-level menus with nested cascade, mnemonics, checked/radio rows, OverlayStack helpers, and narrow CommandPalette replacement.'
component: MenuBar
demo: menu-bar/basic
interaction: selection-navigation
actions:
  - "←→ choose menu"
  - "Enter/↓ open"
  - "↑↓ choose item"
  - "Esc close layer"
  - "click menu"
expectedOutcomes:
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "Cancel or close removes the transient state without performing a host-owned effect."
source: crates/termrock/src/widgets/menu_bar.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="menu-bar/basic" interactive caption="MenuBar · MenuBar basic — live shared Rust state" />

## Try it

- `←→ choose menu`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter/↓ open`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `↑↓ choose item`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Esc close layer`: Open the transient state first, then cancel it; focus and the underlying state are preserved.
- `click menu`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `menu-bar/ascii` | MenuBar ASCII | paint-only |
| `menu-bar/basic` | MenuBar basic | interactive |
| `menu-bar/mnemonic` | MenuBar mnemonic mode | paint-only |
| `menu-bar/narrow` | MenuBar narrow palette | paint-only |
| `menu-bar/nested` | MenuBar nested dismiss | paint-only |
| `menu-bar/open` | MenuBar open cascade | paint-only |
| `menu-bar/unicode` | MenuBar Unicode | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `menu-bar/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn menu_bar_basic_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let menus = example_app_menus();
    let mut state = MenuBarState::new();
    state.set_focused(true);
    let bar = Rect::new(area.x, area.y, area.width, 1.min(area.height));
    MenuBar::new(&menus, system).paint(bar, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `menu-bar/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `menu-bar/basic` | MenuBar basic | **Primary (live preview)** |
| `menu-bar/ascii` | MenuBar ASCII | ASCII / low capability |
| `menu-bar/mnemonic` | MenuBar mnemonic mode | mnemonic |
| `menu-bar/narrow` | MenuBar narrow palette | Narrow terminal |
| `menu-bar/nested` | MenuBar nested dismiss | nested |
| `menu-bar/open` | MenuBar open cascade | Open / expanded |
| `menu-bar/unicode` | MenuBar Unicode | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/menu_bar.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/menu_bar.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `menu-bar/basic`
