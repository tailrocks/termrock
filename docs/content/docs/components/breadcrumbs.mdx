---
title: Breadcrumbs
description: 'Location context and ancestor navigation with collapse, overflow menu, editable path mode, and a single Tab stop.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="breadcrumbs/path" interactive caption="Breadcrumbs · Breadcrumbs path — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{BreadcrumbItem, Breadcrumbs, BreadcrumbsState};

let system = DesignSystem::default();
let items = [
    BreadcrumbItem::new("h", "home"),
    BreadcrumbItem::new("s", "src").current(true),
];
let mut state = BreadcrumbsState::new();
state.set_focused(true);
let _ = Breadcrumbs::new(&items, &system).ascii(true);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `breadcrumbs/path` | Breadcrumbs path | **Primary (live preview)** |
| `breadcrumbs/collapsed` | Breadcrumbs collapsed | collapsed |
| `breadcrumbs/editable` | Breadcrumbs editable | editable |
| `breadcrumbs/schema` | Breadcrumbs schema | schema |
| `breadcrumbs/status` | Breadcrumbs status | status |
