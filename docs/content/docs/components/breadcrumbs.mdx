---
title: Breadcrumbs
description: 'Location context and ancestor navigation with collapse, overflow menu, editable path mode, and a single Tab stop.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

### `breadcrumbs/path`

Breadcrumbs path

![Breadcrumbs — Breadcrumbs path](/component-previews/breadcrumbs-path.svg)

### `breadcrumbs/collapsed`

Breadcrumbs collapsed

![Breadcrumbs — Breadcrumbs collapsed](/component-previews/breadcrumbs-collapsed.svg)

### `breadcrumbs/editable`

Breadcrumbs editable

![Breadcrumbs — Breadcrumbs editable](/component-previews/breadcrumbs-editable.svg)

### `breadcrumbs/status`

Breadcrumbs status

![Breadcrumbs — Breadcrumbs status](/component-previews/breadcrumbs-status.svg)

### `breadcrumbs/schema`

Breadcrumbs schema

![Breadcrumbs — Breadcrumbs schema](/component-previews/breadcrumbs-schema.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{BreadcrumbItem, Breadcrumbs, BreadcrumbsState};

let system = DesignSystem::default();
let items = [
    BreadcrumbItem::new("h", "home"),
    BreadcrumbItem::new("s", "src").current(true),
];
let mut state = BreadcrumbsState::new();
state.set_focused(true);
let _ = Breadcrumbs::new(&items, &system).ascii(true);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `breadcrumbs/path` — Breadcrumbs path
- `breadcrumbs/collapsed` — Breadcrumbs collapsed
- `breadcrumbs/editable` — Breadcrumbs editable
- `breadcrumbs/status` — Breadcrumbs status
- `breadcrumbs/schema` — Breadcrumbs schema
