---
title: Slider
description: 'Slider widget.'
component: Slider
demo: slider/basic
interaction: drag-continuous-value
actions:
  - '←→ adjust'
  - 'Home/End'
  - 'drag track'
  - 'wheel over slider'
expectedOutcomes:
  - 'The preview status reports the typed Rust outcome after each accepted action.'
source: crates/termrock/src/widgets/slider.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="slider/basic" interactive caption="Slider · Slider basic — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::Slider;

// See handbook / lookbook story slider/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `slider/basic` | Slider basic | **Primary (live preview)** |
| `slider/marks` | Slider marks | marks |
| `slider/narrow` | Narrow Slider | Narrow terminal |
| `slider/numeric` | Slider numeric fallback | numeric |
| `slider/unicode` | Unicode Slider | Unicode / wide glyphs |
| `slider/vertical` | Slider vertical | vertical |


## Try it

- `arrow keys`: dispatches through the public TermRock interaction API.
- `Enter activate`: dispatches through the public TermRock interaction API.
- `click`: dispatches through the public TermRock interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `slider/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/slider.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/slider.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `slider/basic`
