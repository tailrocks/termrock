---
title: Backdrop
description: 'A configurable themed fill painted behind modal content.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `backdrop/basic`

Backdrop

![Backdrop — Backdrop](/component-previews/backdrop-basic.svg)

### `backdrop/narrow`

Narrow Backdrop

![Backdrop — Narrow Backdrop](/component-previews/backdrop-narrow.svg)

### `backdrop/unicode`

Unicode Backdrop

![Backdrop — Unicode Backdrop](/component-previews/backdrop-unicode.svg)

## Usage

```rust
use ratatui_core::{buffer::Buffer, layout::Rect, style::{Color, Style}, widgets::Widget};
use termrock::widgets::Backdrop;

let backdrop = Backdrop::new()
    .symbol('░')
    .style(Style::new().bg(Color::Black));
let area = Rect::new(0, 0, 80, 24);
backdrop.render(area, &mut Buffer::empty(area));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `backdrop/basic` — Backdrop
- `backdrop/narrow` — Narrow Backdrop
- `backdrop/unicode` — Unicode Backdrop
