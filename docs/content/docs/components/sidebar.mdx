---
title: Sidebar
description: 'Primary application navigation with sections, hierarchy, badges, status, and rail/drawer collapse. Route state is distinct from keyboard focus.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `sidebar/settings`

Sidebar settings

![Sidebar — Sidebar settings](/component-previews/sidebar-settings.svg)

### `sidebar/database`

Sidebar database

![Sidebar — Sidebar database](/component-previews/sidebar-database.svg)

### `sidebar/agent`

Sidebar agent workbench

![Sidebar — Sidebar agent workbench](/component-previews/sidebar-agent.svg)

### `sidebar/rail`

Sidebar rail

![Sidebar — Sidebar rail](/component-previews/sidebar-rail.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Sidebar, SidebarState, example_settings_nav};

let system = DesignSystem::default();
let items = example_settings_nav();
let mut state = SidebarState::new(Some("profile"));
state.set_focused(true);
let _ = Sidebar::new(&items, &system).title("Settings").show_panel(true);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `sidebar/settings` — Sidebar settings
- `sidebar/database` — Sidebar database
- `sidebar/agent` — Sidebar agent workbench
- `sidebar/rail` — Sidebar rail
