---
title: CommandPalette
description: Filterable command list chrome over the picker contract.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![CommandPalette — Command palette](/component-previews/command-palette-basic.svg)

## Usage

```rust
use ratatui_core::text::Line;
use termrock::{Theme, widgets::{CommandPalette, CommandPaletteState, ListRow, RowRole}};

let theme = Theme::default();
let rows = [ListRow { id: "quit", label: Line::from("Quit"), trailing: None, enabled: true, role: RowRole::Item }];
let palette = CommandPalette::new("Commands", &rows, &theme);
let state = CommandPaletteState::new(Some("quit"));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | not-applicable |
| Narrow terminal | not-applicable |

## Stories

- `command-palette/basic` — Command palette
