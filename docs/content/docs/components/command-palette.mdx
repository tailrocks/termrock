---
title: CommandPalette
description: 'Flagship universal command surface: fuzzy search, groups, recent/contextual actions, nested pages, arguments, async generation gates, history, and fullscreen promotion.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `command-palette/basic`

Command palette

![CommandPalette — Command palette](/component-previews/command-palette-basic.svg)

### `command-palette/empty`

Command palette empty

![CommandPalette — Command palette empty](/component-previews/command-palette-empty.svg)

### `command-palette/no-result`

Command palette no result

![CommandPalette — Command palette no result](/component-previews/command-palette-no-result.svg)

### `command-palette/loading`

Command palette loading

![CommandPalette — Command palette loading](/component-previews/command-palette-loading.svg)

### `command-palette/fuzzy`

Command palette fuzzy

![CommandPalette — Command palette fuzzy](/component-previews/command-palette-fuzzy.svg)

### `command-palette/nested`

Command palette nested page

![CommandPalette — Command palette nested page](/component-previews/command-palette-nested.svg)

### `command-palette/args`

Command palette arguments

![CommandPalette — Command palette arguments](/component-previews/command-palette-args.svg)

### `command-palette/ascii`

Command palette ASCII

![CommandPalette — Command palette ascii](/component-previews/command-palette-ascii.svg)

### `command-palette/narrow`

Narrow CommandPalette

![CommandPalette — Narrow CommandPalette](/component-previews/command-palette-narrow.svg)

### `command-palette/unicode`

Unicode CommandPalette

![CommandPalette — Unicode CommandPalette](/component-previews/command-palette-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{CommandPalette, CommandPaletteState, example_command_catalog};

let system = DesignSystem::default();
let catalog = example_command_catalog();
let mut state = CommandPaletteState::new(None);
state.set_focused(true);
let visible = state.refilter(&catalog);
CommandPalette::new("Commands", &visible, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Async generation | covered |
| Nested pages | covered |
| Arguments | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered (fullscreen promote) |

## Stories

- `command-palette/args` — Command palette arguments
- `command-palette/ascii` — Command palette ASCII
- `command-palette/basic` — Command palette
- `command-palette/empty` — Command palette empty
- `command-palette/fuzzy` — Command palette fuzzy
- `command-palette/loading` — Command palette loading
- `command-palette/narrow` — Narrow CommandPalette
- `command-palette/nested` — Command palette nested page
- `command-palette/no-result` — Command palette no result
- `command-palette/unicode` — Unicode CommandPalette
