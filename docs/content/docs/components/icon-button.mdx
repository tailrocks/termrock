---
title: IconButton
description: 'Compact glyph action with mandatory accessible label and hit slop.'
component: IconButton
demo: button/icon
interaction: activation
actions:
  - "hover"
  - "click toggle"
  - "Enter/Space toggle"
expectedOutcomes:
  - "Pointer movement updates visible hover chrome without activating the control."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/primitives.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="button/icon" interactive caption="IconButton · Icon button — live shared Rust state" />

## Try it

- `hover`: Move the pointer over the target; hover chrome changes without activation.
- `click toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter/Space toggle`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `button/icon` | Icon button | interactive |
| `icon-button/destructive` | IconButton destructive | paint-only |
| `icon-button/loading` | IconButton loading | paint-only |
| `icon-button/narrow` | Narrow IconButton | paint-only |
| `icon-button/row` | IconButton data row | paint-only |
| `icon-button/toolbar` | IconButton toolbar | paint-only |
| `icon-button/unicode` | Unicode IconButton | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `button/icon`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn button_icon_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::IconButtonState;
    let mut state = IconButtonState::new();
    state.activation.set_accepts_input(true);
    termrock::widgets::IconButton::new("×", "Close dialog", system)
        .help("Close the dialog (Esc)")
        .render(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `button/icon`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

| Story | Title | Focus |
|---|---|---|
| `button/icon` | Icon button | **Primary (live preview)** |
| `icon-button/toolbar` | IconButton toolbar | toolbar |
| `icon-button/destructive` | IconButton destructive | destructive |
| `icon-button/loading` | IconButton loading | loading |
| `icon-button/row` | IconButton data row | row |
| `icon-button/narrow` | Narrow IconButton | narrow |
| `icon-button/unicode` | Unicode IconButton | unicode |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/primitives.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/primitives.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `button/icon`

## Seen in applications

<SeenInApplications component="IconButton" />
