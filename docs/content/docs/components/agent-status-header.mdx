---
title: AgentStatusHeader
description: 'Compact agent/session status with StatusBar contraction.'
component: AgentStatusHeader
demo: agent-status-header/basic
interaction: activation
actions:
  - "←→ action"
  - "Enter activate"
  - "click action"
expectedOutcomes:
  - "The \"←→ action\" action updates visible mounted state and reports its typed outcome."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/mod.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="agent-status-header/basic" interactive caption="AgentStatusHeader · AgentStatusHeader — live shared Rust state" />

## Try it

- `←→ action`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Enter activate`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `click action`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted Rust demo owns deterministic sample state. The component/pattern owns its reusable paint and interaction contracts; the consuming host owns domain policy and every external effect.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `agent-status-header/basic` | AgentStatusHeader | interactive |
| `agent-status-header/idle` | AgentStatusHeader idle | paint-only |
| `agent-status-header/in-app` | In application | interactive |
| `agent-status-header/narrow` | AgentStatusHeader narrow | paint-only |
| `agent-status-header/unicode` | Unicode AgentStatusHeader | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `agent-status-header/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn agent_status_header_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::patterns::{
        AgentStatusHeader, AgentStatusHeaderState, AgentStatusPresentation, example_agent_status,
    };
    let mut state = AgentStatusHeaderState::new();
    state.set_snapshot(example_agent_status());
    if area.width < 56 {
        state.auto_contract = true;
    } else {
        state.presentation = AgentStatusPresentation::Header;
        state.auto_contract = false;
    }
    state.focused = true;
    frame.render_stateful_widget(&AgentStatusHeader::new(system), area, &mut state);
}
```

## Migrated handbook guidance

# AgentStatusHeader

Top-level chrome for project/session, branch, mode/model, connection, work
status, context, cost/time, and action-required. **Provider-neutral.**


## When to use / when not

| Use AgentStatusHeader | Prefer |
|-----------------------|--------|
| Agent workbench header | — |
| Generic L/C/R slots | [StatusBar](/docs/components) if documented |
| Full token budget UI | [ContextMeter](/docs/interaction-notes) |

## Presentation

| Form | When |
|------|------|
| Header (1–2 rows) | Wide (≥56 cols default) |
| StatusBar | Narrow auto-contract |

## Priority

Action-required / connection issues paint before decorative cost/time.

## Quick actions

Sessions · Model · Tasks · Help (+ Acknowledge when attention needed)

## Related

StatusBar · ModeRibbon · ContextMeter · migration `0232`

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `agent-status-header/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

| Story | Title | Focus |
|---|---|---|
| `agent-status-header/basic` | AgentStatusHeader | **Primary (live preview)** |
| `agent-status-header/idle` | AgentStatusHeader idle | idle |
| `agent-status-header/narrow` | AgentStatusHeader narrow | narrow |
| `agent-status-header/unicode` | Unicode AgentStatusHeader | unicode |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/mod.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/mod.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `agent-status-header/basic`

## Seen in applications

<SeenInApplications component="AgentStatusHeader" />
