---
title: Menu
description: 'Flat single-panel menu adapter over MenuItem (prefer DropdownMenu/MenuNode for nested command menus).'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `menu/roving`

Menu

![Menu — Menu](/component-previews/menu-roving.svg)

### `menu/narrow`

Narrow Menu

![Menu — Narrow Menu](/component-previews/menu-narrow.svg)

### `menu/unicode`

Unicode Menu

![Menu — Unicode Menu](/component-previews/menu-unicode.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{Menu, MenuItem, MenuState};

let system = DesignSystem::default();
let items = [MenuItem::new("a", "Open")];
let mut state = MenuState::new();
Menu::new(&items, &system).render(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `menu/narrow` — Narrow Menu
- `menu/roving` — Menu
- `menu/unicode` — Unicode Menu
