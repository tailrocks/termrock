---
title: Menu
description: 'Menu list with roving focus, disabled skip, and Esc close outcomes.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![Menu — Menu](/component-previews/menu-roving.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{Menu, MenuItem, MenuState}};

let tokens = DesignTokens::default();
let items = [MenuItem::new("a", "Open")];
let state = MenuState::new();
let menu = Menu::new(&items, &tokens);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | not-applicable |
| Narrow terminal | not-applicable |

## Stories

- `menu/roving` — Menu
