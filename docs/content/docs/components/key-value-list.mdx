---
title: KeyValueList
description: 'Metadata list — aligned keys, stacked narrow, secrets, status, links.'
component: KeyValueList
demo: key-value-list/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/key_value_list.rs
---
## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="key-value-list/basic" interactive caption="KeyValueList · shared Rust demo" />

## Usage

```rust
use termrock::widgets::{KeyValueList, KeyValueListState, KvEntry, KvStatus};

let entries = [
    KvEntry::group_header("g", "Network"),
    KvEntry::pair("host", "Host", "localhost").copyable(),
    KvEntry::pair("tls", "TLS", "required").status(KvStatus::Warning),
];
let mut state = KeyValueListState::new();
KeyValueList::dense(&entries, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

| Story | Title | Focus |
|---|---|---|
| `key-value-list/basic` | KeyValueList reading | **Primary (live preview)** |
| `key-value-list/dense` | KeyValueList dense | dense |
| `key-value-list/stacked` | KeyValueList stacked | stacked |
| `key-value-list/secret` | KeyValueList secret | secret |
| `key-value-list/narrow` | Narrow KeyValueList | narrow |
| `key-value-list/unicode` | Unicode KeyValueList | unicode |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `key-value-list/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/key_value_list.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/key_value_list.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `key-value-list/basic`
