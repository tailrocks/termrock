---
title: KeyValueList
description: 'Metadata list — aligned keys, stacked narrow, secrets, status, links.'
component: KeyValueList
demo: key-value-list/basic
interaction: selection-navigation
actions:
  - "↑↓ select"
  - "R/Space reveal secret"
  - "C copy"
  - "Enter open link"
  - "wheel scroll"
  - "click row"
expectedOutcomes:
  - "Navigation moves visible focus or selection and preserves it for the next event."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"C copy\" action updates visible mounted state and reports its typed outcome."
  - "Scrolling changes the mounted viewport while preserving selection and surrounding page position."
source: crates/termrock/src/widgets/key_value_list.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="key-value-list/basic" interactive caption="KeyValueList · KeyValueList reading — live shared Rust state" />

## Try it

- `↑↓ select`: Move through the real focus/selection model; the next action uses the new target.
- `R/Space reveal secret`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `C copy`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Enter open link`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `wheel scroll`: Scroll over the preview; its own viewport moves while the docs page stays put.
- `click row`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `key-value-list/basic` | KeyValueList reading | interactive |
| `key-value-list/dense` | KeyValueList dense | paint-only |
| `key-value-list/narrow` | Narrow KeyValueList | paint-only |
| `key-value-list/secret` | KeyValueList secret | paint-only |
| `key-value-list/stacked` | KeyValueList stacked | paint-only |
| `key-value-list/unicode` | Unicode KeyValueList | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `key-value-list/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn key_value_list_basic_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let entries = [
        KvEntry::group_header("id", "Identity"),
        KvEntry::pair("name", "Name", "termrock")
            .copyable()
            .annotation("crate"),
        KvEntry::pair("status", "Status", "active").status(KvStatus::Success),
        KvEntry::pair("token", "Token", "super-secret")
            .secret()
            .copyable(),
        KvEntry::pair("docs", "Docs", "handbook")
            .href("https://example.invalid")
            .annotation("external"),
        KvEntry::group_header("build", "Build").depth(0),
        KvEntry::pair("target", "Target", "aarch64-apple-darwin").depth(1),
    ];
    let mut state = KeyValueListState::new();
    state.set_focused(true);
    state.cursor = Some("status");
    let _ = KeyValueList::reading(&entries, system).paint(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `key-value-list/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

| Story | Title | Focus |
|---|---|---|
| `key-value-list/basic` | KeyValueList reading | **Primary (live preview)** |
| `key-value-list/dense` | KeyValueList dense | dense |
| `key-value-list/stacked` | KeyValueList stacked | stacked |
| `key-value-list/secret` | KeyValueList secret | secret |
| `key-value-list/narrow` | Narrow KeyValueList | narrow |
| `key-value-list/unicode` | Unicode KeyValueList | unicode |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/key_value_list.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/key_value_list.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `key-value-list/basic`
