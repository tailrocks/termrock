---
title: PreviewCard
description: 'Non-essential delayed resource preview (file/command/symbol/session) with metadata, loading/error, pin-to-open, and generation-gated async — never focus-stealing when unpinned; required facts must exist outside the card.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `preview-card/file`

PreviewCard file

![PreviewCard — PreviewCard file](/component-previews/preview-card-file.svg)

### `preview-card/command`

PreviewCard command

![PreviewCard — PreviewCard command](/component-previews/preview-card-command.svg)

### `preview-card/error`

PreviewCard error

![PreviewCard — PreviewCard error](/component-previews/preview-card-error.svg)

### `preview-card/loading`

PreviewCard loading

![PreviewCard — PreviewCard loading](/component-previews/preview-card-loading.svg)

### `preview-card/pinned`

PreviewCard pinned

![PreviewCard — PreviewCard pinned](/component-previews/preview-card-pinned.svg)

### `preview-card/session`

PreviewCard session

![PreviewCard — PreviewCard session](/component-previews/preview-card-session.svg)

### `preview-card/symbol`

PreviewCard symbol

![PreviewCard — PreviewCard symbol](/component-previews/preview-card-symbol.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    PreviewCard, PreviewCardOutcome, PreviewCardState, example_file_preview,
};

let system = DesignSystem::default();
let mut state = PreviewCardState::new();
let _ = state.set_selection("main.rs");
if let PreviewCardOutcome::Loading { generation } = state.begin_fetch() {
    let _ = state.apply_ready(generation); // host async first
}
let (content, _, _) = example_file_preview();
let _ = state.tick_hover(300, true);
PreviewCard::new(content, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `preview-card/command` — PreviewCard command
- `preview-card/error` — PreviewCard error
- `preview-card/file` — PreviewCard file
- `preview-card/loading` — PreviewCard loading
- `preview-card/pinned` — PreviewCard pinned
- `preview-card/session` — PreviewCard session
- `preview-card/symbol` — PreviewCard symbol
