---
title: PreviewCard
description: 'Non-essential delayed resource preview (file/command/symbol/session) with metadata, loading/error, pin-to-open, and generation-gated async — never focus-stealing when unpinned; required facts must exist outside the card.'
component: PreviewCard
demo: preview-card/file
interaction: disclosure-overlay
actions:
  - "hover show/hide"
  - "P pin"
expectedOutcomes:
  - "Pointer movement updates visible hover chrome without activating the control."
  - "The \"P pin\" action updates visible mounted state and reports its typed outcome."
source: crates/termrock/src/widgets/preview_card.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="preview-card/file" interactive caption="PreviewCard · PreviewCard file — live shared Rust state" />

## Try it

- `hover show/hide`: Move the pointer over the target; hover chrome changes without activation.
- `P pin`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `preview-card/command` | PreviewCard command | paint-only |
| `preview-card/error` | PreviewCard error | paint-only |
| `preview-card/file` | PreviewCard file | interactive |
| `preview-card/loading` | PreviewCard loading | paint-only |
| `preview-card/pinned` | PreviewCard pinned | paint-only |
| `preview-card/session` | PreviewCard session | paint-only |
| `preview-card/symbol` | PreviewCard symbol | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `preview-card/file`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn preview_card_file_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let (content, _, _) = example_file_preview();
    let mut state = PreviewCardState::with_delay(std::time::Duration::ZERO);
    let _ = state.tick_hover(1, true);
    PreviewCard::new(content, system).paint(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `preview-card/file`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `preview-card/file` | PreviewCard file | **Primary (live preview)** |
| `preview-card/command` | PreviewCard command | command |
| `preview-card/error` | PreviewCard error | Error |
| `preview-card/loading` | PreviewCard loading | Loading state |
| `preview-card/pinned` | PreviewCard pinned | pinned |
| `preview-card/session` | PreviewCard session | session |
| `preview-card/symbol` | PreviewCard symbol | symbol |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/preview_card.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/preview_card.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `preview-card/file`

## Seen in applications

<SeenInApplications component="PreviewCard" />
