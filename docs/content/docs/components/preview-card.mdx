---
title: PreviewCard
description: 'Non-essential delayed resource preview (file/command/symbol/session) with metadata, loading/error, pin-to-open, and generation-gated async — never focus-stealing when unpinned; required facts must exist outside the card.'
component: PreviewCard
demo: preview-card/file
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/preview_card.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="preview-card/file" interactive caption="PreviewCard · PreviewCard file — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    PreviewCard, PreviewCardOutcome, PreviewCardState, example_file_preview,
};

let system = DesignSystem::default();
let mut state = PreviewCardState::new();
let _ = state.set_selection("main.rs");
if let PreviewCardOutcome::Loading { generation } = state.begin_fetch() {
    let _ = state.apply_ready(generation); // host async first
}
let (content, _, _) = example_file_preview();
let _ = state.tick_hover(300, true);
PreviewCard::new(content, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `preview-card/file` | PreviewCard file | **Primary (live preview)** |
| `preview-card/command` | PreviewCard command | command |
| `preview-card/error` | PreviewCard error | Error |
| `preview-card/loading` | PreviewCard loading | Loading state |
| `preview-card/pinned` | PreviewCard pinned | pinned |
| `preview-card/session` | PreviewCard session | session |
| `preview-card/symbol` | PreviewCard symbol | symbol |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `preview-card/file`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/preview_card.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/preview_card.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `preview-card/file`
