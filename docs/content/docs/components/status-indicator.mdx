---
title: StatusIndicator
description: 'Compact semantic status primitive (glyph + label + style) with shared SemanticStatus vocabulary for connections, tasks, agents, rows, and services; compact/labeled/elapsed variants; maps domain enums.'
component: StatusIndicator
demo: status-indicator/catalog
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/status_indicator.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="status-indicator/catalog" interactive caption="StatusIndicator · StatusIndicator catalog — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `status-indicator/ascii` | StatusIndicator ASCII | paint-only |
| `status-indicator/catalog` | StatusIndicator catalog | paint-only |
| `status-indicator/compact` | StatusIndicator compact | paint-only |
| `status-indicator/elapsed` | StatusIndicator elapsed | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `status-indicator/catalog`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn status_indicator_catalog_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let mut y = area.y;
    for (kind, label) in example_status_catalog() {
        if y >= area.bottom() {
            break;
        }
        StatusIndicator::new(kind, system)
            .label(label)
            .paint(Rect::new(area.x, y, area.width, 1), frame.buffer_mut());
        y = y.saturating_add(1);
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `status-indicator/catalog`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `status-indicator/catalog` | StatusIndicator catalog | **Primary (live preview)** |
| `status-indicator/ascii` | StatusIndicator ASCII | ASCII / low capability |
| `status-indicator/compact` | StatusIndicator compact | Compact density |
| `status-indicator/elapsed` | StatusIndicator elapsed | elapsed |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/status_indicator.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/status_indicator.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `status-indicator/catalog`

## Seen in applications

<SeenInApplications component="StatusIndicator" />
