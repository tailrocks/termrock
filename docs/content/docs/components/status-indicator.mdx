---
title: StatusIndicator
description: 'Compact semantic status primitive (glyph + label + style) with shared SemanticStatus vocabulary for connections, tasks, agents, rows, and services; compact/labeled/elapsed variants; maps domain enums.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `status-indicator/catalog`

StatusIndicator catalog

![StatusIndicator — StatusIndicator catalog](/component-previews/status-indicator-catalog.svg)

### `status-indicator/ascii`

StatusIndicator ASCII

![StatusIndicator — StatusIndicator ASCII](/component-previews/status-indicator-ascii.svg)

### `status-indicator/compact`

StatusIndicator compact

![StatusIndicator — StatusIndicator compact](/component-previews/status-indicator-compact.svg)

### `status-indicator/elapsed`

StatusIndicator elapsed

![StatusIndicator — StatusIndicator elapsed](/component-previews/status-indicator-elapsed.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{StatusIndicator, SemanticStatus, StatusIndicatorState};

let system = DesignSystem::default();
StatusIndicator::new(SemanticStatus::Running, &system)
    .label("agent")
    .elapsed_secs(42)
    .paint(area, buf);
// Map domain: SemanticStatus::from_tool_status / from_presence / from_progress_status
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `status-indicator/ascii` — StatusIndicator ASCII
- `status-indicator/catalog` — StatusIndicator catalog
- `status-indicator/compact` — StatusIndicator compact
- `status-indicator/elapsed` — StatusIndicator elapsed
