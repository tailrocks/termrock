---
title: HistoryPicker
description: 'Reusable recent-history selector with pin/delete, search, groups, preview, redaction hooks, draft preservation, and popover/fullscreen placement.'
component: HistoryPicker
demo: history-picker/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/history_picker.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="history-picker/basic" interactive caption="HistoryPicker · HistoryPicker basic — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{HistoryPicker, HistoryPickerState, example_history_entries, filter_history_entries};

let system = DesignSystem::default();
let entries = example_history_entries();
let visible = filter_history_entries(&entries, "");
let mut state = HistoryPickerState::new();
let _ = state.open(None);
HistoryPicker::new(&visible, &system).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `history-picker/basic` | HistoryPicker basic | **Primary (live preview)** |
| `history-picker/ascii` | HistoryPicker ASCII | ASCII / low capability |
| `history-picker/draft` | HistoryPicker draft preserved | draft |
| `history-picker/empty` | HistoryPicker empty | Empty state |
| `history-picker/redacted` | HistoryPicker redacted | redacted |
| `history-picker/search` | HistoryPicker search | Search / filter |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `history-picker/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/history_picker.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/history_picker.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `history-picker/basic`
