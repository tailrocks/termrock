---
title: HistoryPicker
description: 'Reusable recent-history selector with pin/delete, search, groups, preview, redaction hooks, draft preservation, and popover/fullscreen placement.'
component: HistoryPicker
demo: history-picker/basic
interaction: editor-form
actions:
  - "type to filter"
  - "↑↓ choose"
  - "Enter restore"
  - "Ctrl+P pin"
  - "Ctrl+D delete"
  - "click entry"
expectedOutcomes:
  - "Editing changes the visible Unicode-safe draft and reports the public typed edit outcome."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
  - "The \"Ctrl+P pin\" action updates visible mounted state and reports its typed outcome."
  - "The \"Ctrl+D delete\" action updates visible mounted state and reports its typed outcome."
source: crates/termrock/src/widgets/history_picker.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="history-picker/basic" interactive caption="HistoryPicker · HistoryPicker basic — live shared Rust state" />

## Try it

- `type to filter`: Focus the editable field, then type or paste Unicode text; the draft remains visible.
- `↑↓ choose`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter restore`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Ctrl+P pin`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `Ctrl+D delete`: Dispatch this action to the mounted Rust state machine and inspect its visible typed result.
- `click entry`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | covered |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `history-picker/ascii` | HistoryPicker ASCII | paint-only |
| `history-picker/basic` | HistoryPicker basic | interactive |
| `history-picker/draft` | HistoryPicker draft preserved | paint-only |
| `history-picker/empty` | HistoryPicker empty | paint-only |
| `history-picker/redacted` | HistoryPicker redacted | paint-only |
| `history-picker/search` | HistoryPicker search | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `history-picker/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn history_picker_basic(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let entries = example_history_entries();
    let visible = filter_history_entries(&entries, "");
    let mut state = HistoryPickerState::new();
    let _ = state.open(None);
    state.reconcile(&visible);
    HistoryPicker::new(&visible, system).paint(area, frame.buffer_mut(), &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `history-picker/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `history-picker/basic` | HistoryPicker basic | **Primary (live preview)** |
| `history-picker/ascii` | HistoryPicker ASCII | ASCII / low capability |
| `history-picker/draft` | HistoryPicker draft preserved | draft |
| `history-picker/empty` | HistoryPicker empty | Empty state |
| `history-picker/redacted` | HistoryPicker redacted | redacted |
| `history-picker/search` | HistoryPicker search | Search / filter |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/history_picker.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/history_picker.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `history-picker/basic`
