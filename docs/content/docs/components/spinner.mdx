---
title: Spinner
description: 'Semantic activity spinner with verb label, phases (indeterminate/waiting/queued/reconnecting), capability glyphs, reduced-motion static frames, and idle redraw when inactive/hidden.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `spinner/labeled`

Spinner labeled

![Spinner — Spinner labeled](/component-previews/spinner-labeled.svg)

### `spinner/ascii`

Spinner ASCII

![Spinner — Spinner ASCII](/component-previews/spinner-ascii.svg)

### `spinner/compact`

Spinner compact embedded

![Spinner — Spinner compact embedded](/component-previews/spinner-compact.svg)

### `spinner/phases`

Spinner phases

![Spinner — Spinner phases](/component-previews/spinner-phases.svg)

## Usage

```rust
use termrock::style::{DesignSystem, Motion};
use termrock::runtime::FrameTick;
use termrock::widgets::{Spinner, SpinnerState, ActivityPhase};

let system = DesignSystem::default();
let mut state = SpinnerState::new();
state.set_phase(ActivityPhase::Indeterminate);
// host: only tick while state.should_tick()
Spinner::labeled("Fetching packages", &system)
    .paint(area, buf, &state, tick, Motion::Full);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `spinner/ascii` — Spinner ASCII
- `spinner/compact` — Spinner compact embedded
- `spinner/labeled` — Spinner labeled
- `spinner/phases` — Spinner phases
