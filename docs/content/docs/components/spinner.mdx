---
title: Spinner
description: 'Semantic activity spinner with verb label, phases (indeterminate/waiting/queued/reconnecting), capability glyphs, reduced-motion static frames, and idle redraw when inactive/hidden.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="spinner/labeled" interactive caption="Spinner · Spinner labeled — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::style::{DesignSystem, Motion};
use termrock::runtime::FrameTick;
use termrock::widgets::{Spinner, SpinnerState, ActivityPhase};

let system = DesignSystem::default();
let mut state = SpinnerState::new();
state.set_phase(ActivityPhase::Indeterminate);
// host: only tick while state.should_tick()
Spinner::labeled("Fetching packages", &system)
    .paint(area, buf, &state, tick, Motion::Full);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `spinner/labeled` | Spinner labeled | **Primary (live preview)** |
| `motion/presence` | Motion presence kit | presence |
| `spinner/ascii` | Spinner ASCII | ASCII / low capability |
| `spinner/compact` | Spinner compact embedded | Compact density |
| `spinner/phases` | Spinner phases | phases |
