---
title: Dialog
description: 'Canonical modal surface with title, description, body, actions, close policy, focus trap, recipes, safe Enter/Esc, loading and validation.'
---

## Previews

### `dialog/message`

Message dialog

![Dialog — message](/component-previews/dialog-message.svg)

### `dialog/destructive`

Destructive dialog

![Dialog — destructive](/component-previews/dialog-destructive.svg)

### `dialog/compact`

Compact dialog

![Dialog — compact](/component-previews/dialog-compact.svg)

### `dialog/narrow`

Narrow dialog

![Dialog — narrow](/component-previews/dialog-narrow.svg)

### `dialog/unicode`

Unicode Dialog

![Dialog — unicode](/component-previews/dialog-unicode.svg)

## Usage

```rust
use ratatui_core::text::Text;
use termrock::style::DesignSystem;
use termrock::widgets::{
    Dialog, DialogRecipe, DialogSize, DialogState, open_dialog_overlay,
};

let system = DesignSystem::default();
let mut state = DialogState::<&str>::new();
let _ = state.open_on_stack(&mut stack, bounds, DialogSize::default(), Some("trigger"));
Dialog::new("Notice", Text::from("Saved"), &system)
    .description("Write completed.")
    .recipe(DialogRecipe::Normal)
    .footer_hint("esc dismiss")
    .paint(entry.rect, buf, &mut state, 0);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered (Enter/Esc policy, action roving, body scroll) |
| Mouse | covered (action hits; outside trapped by stack) |
| Focus | covered (trap on stack; zones body/actions; opener restore) |
| Non-color cues | covered (danger `!`, loading glyph, validation) |
| Unicode | covered |
| Narrow terminal | covered (fullscreen promotion) |

## Stories

- `dialog/message`, `destructive`, `compact`, `narrow`, `unicode`
