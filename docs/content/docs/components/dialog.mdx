---
title: Dialog
description: 'A framed modal surface with semantic chrome and caller-owned content.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `dialog/message`

Message dialog

![Dialog — Message dialog](/component-previews/dialog-message.svg)

### `dialog/narrow`

Narrow dialog

![Dialog — Narrow dialog](/component-previews/dialog-narrow.svg)

### `dialog/unicode`

Unicode Dialog

![Dialog — Unicode Dialog](/component-previews/dialog-unicode.svg)

## Usage

```rust
use ratatui_core::{buffer::Buffer, layout::Rect, text::Text, widgets::Widget};
use termrock::{Theme, widgets::{Dialog, PanelEmphasis}};

let theme = Theme::default();
let dialog = Dialog::new("Notice", Text::from("Saved"), &theme)
    .emphasis(PanelEmphasis::Focused);
let area = Rect::new(0, 0, 40, 8);
dialog.render(area, &mut Buffer::empty(area));
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | caller-owned |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `dialog/message` — Message dialog
- `dialog/narrow` — Narrow dialog
- `dialog/unicode` — Unicode Dialog
