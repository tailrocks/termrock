---
title: Dialog
description: 'Canonical modal surface: title, description, body, actions, close policy, focus trap via OverlayStack, initial focus, opener restoration, scrolling, loading, validation; recipes normal/compact/wide/fullscreen/destructive; safe Enter (no accidental submit from body).'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `dialog/message`

Message dialog

![Dialog — Message dialog](/component-previews/dialog-message.svg)

### `dialog/compact`

Compact dialog

![Dialog — Compact dialog](/component-previews/dialog-compact.svg)

### `dialog/destructive`

Destructive dialog

![Dialog — Destructive dialog](/component-previews/dialog-destructive.svg)

### `dialog/narrow`

Narrow dialog

![Dialog — Narrow dialog](/component-previews/dialog-narrow.svg)

### `dialog/unicode`

Unicode Dialog

![Dialog — Unicode Dialog](/component-previews/dialog-unicode.svg)

## Usage

```rust
use ratatui_core::text::Text;
use termrock::interaction::OverlayStack;
use termrock::style::DesignSystem;
use termrock::widgets::{
    Dialog, DialogRecipe, DialogSize, DialogState, open_dialog_overlay,
};

let system = DesignSystem::default();
let mut state = DialogState::<&str>::new();
state.set_recipe(DialogRecipe::Normal);
let mut stack = OverlayStack::<&str>::new();
let _ = state.open_on_stack(&mut stack, bounds, DialogSize::default(), Some("trigger"));
Dialog::new("Notice", Text::from("Saved"), &system)
    .description("Write completed.")
    .recipe(DialogRecipe::Normal)
    .footer_hint("esc dismiss")
    .paint(entry.rect, buf, &mut state, 0);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | caller-owned |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `dialog/compact` — Compact dialog
- `dialog/destructive` — Destructive dialog
- `dialog/message` — Message dialog
- `dialog/narrow` — Narrow dialog
- `dialog/unicode` — Unicode Dialog
