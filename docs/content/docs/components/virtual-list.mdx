---
title: VirtualList
description: 'High-performance list for extremely large/streaming collections: shared Virtualizer (overscan, sticky, anchors, variable extents); host projects measure window only; follow-tail; async page status; O(viewport) semantics; diagnostics.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `virtual-list/million`

VirtualList million-row

![VirtualList — VirtualList million-row](/component-previews/virtual-list-million.svg)

### `virtual-list/follow-tail`

VirtualList follow-tail

![VirtualList — VirtualList follow-tail](/component-previews/virtual-list-follow-tail.svg)

### `virtual-list/loading`

VirtualList page loading

![VirtualList — VirtualList page loading](/component-previews/virtual-list-loading.svg)

## Usage

```rust
use termrock::widgets::{VirtualList, VirtualListState, VirtualListItem, ListRow};
use ratatui_core::text::Line;

let mut state = VirtualListState::million_fixed();
let mut idx = Vec::new();
state.projection_indices(&mut idx);
let projected: Vec<_> = idx.iter().map(|&i| {
    VirtualListItem::new(i, ListRow::item(i, Line::from(format!("row {i}"))))
}).collect();
VirtualList::new(&projected, &system).show_diagnostics(true).paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `virtual-list/follow-tail` — VirtualList follow-tail
- `virtual-list/loading` — VirtualList page loading
- `virtual-list/million` — VirtualList million-row
