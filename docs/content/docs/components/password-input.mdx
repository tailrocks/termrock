---
title: PasswordInput
description: 'A secure secret-entry field that masks paint, redacts Debug/semantic output, and never embeds secrets in outcomes.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `password-input/basic`

Password input

![PasswordInput — Password input](/component-previews/password-input-basic.svg)

### `password-input/reveal`

Password reveal

![PasswordInput — Password reveal](/component-previews/password-input-reveal.svg)

### `password-input/invalid`

Password invalid

![PasswordInput — Password invalid](/component-previews/password-input-invalid.svg)

### `password-input/pending`

Password pending

![PasswordInput — Password pending](/component-previews/password-input-pending.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    ClipboardPolicy, PasswordInput, PasswordInputState, PasswordStrengthHint, RevealPolicy,
};

let system = DesignSystem::default();
let mut state = PasswordInputState::new()
    .with_reveal_policy(RevealPolicy::Explicit)
    .with_clipboard_policy(ClipboardPolicy::PasteOnly);
state.set_focused(true);
let _ = PasswordInput::new("Password", &system)
    .placeholder("••••")
    .strength(PasswordStrengthHint::Weak);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `password-input/basic` — Password input
- `password-input/reveal` — Password reveal
- `password-input/invalid` — Password invalid
- `password-input/pending` — Password pending
