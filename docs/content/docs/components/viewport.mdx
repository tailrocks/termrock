---
title: Viewport
description: 'A two-axis scrollable view over borrowed terminal lines.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `viewport/both-axes`

Scrollable viewport

![Viewport — Scrollable viewport](/component-previews/viewport-both-axes.svg)

### `viewport/narrow`

Narrow Viewport

![Viewport — Narrow Viewport](/component-previews/viewport-narrow.svg)

### `viewport/unicode`

Unicode Viewport

![Viewport — Unicode Viewport](/component-previews/viewport-unicode.svg)

## Usage

```rust
use ratatui_core::text::Line;
use termrock::{Theme, scroll::DialogScroll, widgets::Viewport};

let theme = Theme::default();
let lines = [Line::from("long output")];
let viewport = Viewport::new(&lines, &theme).title("Output");
let mut state = DialogScroll::default();
state.scroll_y = 1;
let _vertical_offset = state.scroll_y;
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `viewport/both-axes` — Scrollable viewport
- `viewport/narrow` — Narrow Viewport
- `viewport/unicode` — Unicode Viewport
