---
title: ToggleGroup
description: 'ToggleGroup widget.'
component: ToggleGroup
demo: toggle-group/format
interaction: selection-navigation
actions:
  - "←→ choose"
  - "Space/Enter toggle"
  - "click item"
expectedOutcomes:
  - "Typed widget outcomes appear in the preview status and state persists until Reset."
source: crates/termrock/src/widgets/toggle.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="toggle-group/format" interactive caption="ToggleGroup · ToggleGroup multi format — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::ToggleGroup;

// See handbook / lookbook story toggle-group/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `toggle-group/format` | ToggleGroup multi format | **Primary (live preview)** |
| `toggle-group/align` | ToggleGroup single align | align |
| `toggle-group/narrow` | Narrow ToggleGroup | Narrow terminal |
| `toggle-group/overflow` | ToggleGroup overflow | Overflow |
| `toggle-group/unicode` | Unicode ToggleGroup | Unicode / wide glyphs |


## Try it

- `←→ choose`: routes through the component public interaction API.
- `Space/Enter toggle`: routes through the component public interaction API.
- `click item`: routes through the component public interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `toggle-group/format`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/toggle.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/toggle.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `toggle-group/format`
