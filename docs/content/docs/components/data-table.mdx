---
title: DataTable
description: 'Scalable table chrome; SelectAll is request-only over projected rows.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `data-table/toolbar`

DataTable

![DataTable — DataTable](/component-previews/data-table-toolbar.svg)

### `data-table/empty`

Empty data table

![DataTable — Empty data table](/component-previews/data-table-empty.svg)

### `data-table/narrow`

Narrow DataTable

![DataTable — Narrow DataTable](/component-previews/data-table-narrow.svg)

### `data-table/unicode`

Unicode DataTable

![DataTable — Unicode DataTable](/component-previews/data-table-unicode.svg)

## Usage

```rust
use termrock::{style::DesignTokens, widgets::{ColumnModel, DataColumn, DataColumnWidth, DataTable, DataTableState}};

let tokens = DesignTokens::default();
let columns = ColumnModel::new(vec![DataColumn::new("c", "C", DataColumnWidth::Min(4))]);
let rows: [(u64, &[&str]); 0] = [];
let mut state = DataTableState::<u64, &str>::new();
let table = DataTable::new(&tokens, &columns, &rows);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `data-table/empty` — Empty data table
- `data-table/narrow` — Narrow DataTable
- `data-table/toolbar` — DataTable
- `data-table/unicode` — Unicode DataTable
