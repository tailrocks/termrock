---
title: Surface
description: 'Lowest-level visual ownership: fill, padding, border, clip, and hit geometry with canvas→destructive recipes.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="surface/ladder" interactive caption="Surface · Surface recipe ladder — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{Surface, SurfaceRecipe, SurfaceFill}};

let system = DesignSystem::default();
let content = Surface::new(&system)
    .recipe(SurfaceRecipe::Focused)
    .paint(area, buf);
// Children paint only inside content (clip contract).
// Canvas / terminal-default:
Surface::new(&system).recipe(SurfaceRecipe::Canvas).fill(SurfaceFill::TerminalDefault);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `surface/ladder` | Surface recipe ladder | **Primary (live preview)** |
| `surface/basic` | Surface raised | Baseline layout |
| `surface/focused` | Surface focused | focused |
| `surface/narrow` | Narrow Surface | Narrow terminal |
| `surface/terminal-default` | Surface terminal-default | terminal default |
| `surface/unicode` | Unicode Surface | Unicode / wide glyphs |
