---
title: Surface
description: 'Lowest-level visual ownership: fill, padding, border, clip, and hit geometry with canvas→destructive recipes.'
component: Surface
demo: surface/ladder
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/surface.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="surface/ladder" interactive caption="Surface · Surface recipe ladder — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `surface/basic` | Surface raised | paint-only |
| `surface/focused` | Surface focused | paint-only |
| `surface/ladder` | Surface recipe ladder | paint-only |
| `surface/narrow` | Narrow Surface | paint-only |
| `surface/terminal-default` | Surface terminal-default | paint-only |
| `surface/unicode` | Unicode Surface | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `surface/ladder`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn surface_ladder_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let recipes = [
        SurfaceRecipe::Canvas,
        SurfaceRecipe::Inset,
        SurfaceRecipe::Raised,
        SurfaceRecipe::Overlay,
        SurfaceRecipe::Interactive,
        SurfaceRecipe::Focused,
        SurfaceRecipe::Selected,
        SurfaceRecipe::Warning,
        SurfaceRecipe::Destructive,
    ];
    let rows = recipes.len() as u16;
    let row_h = (area.height / rows.max(1)).max(2);
    for (i, recipe) in recipes.iter().enumerate() {
        let y = area.y.saturating_add((i as u16).saturating_mul(row_h));
        if y >= area.bottom() {
            break;
        }
        let h = row_h.min(area.bottom().saturating_sub(y));
        let row = Rect::new(area.x, y, area.width, h);
        let content = Surface::new(system)
            .recipe(*recipe)
            .padding(1, 0)
            .paint(row, frame.buffer_mut());
        if content.width > 2 {
            frame.buffer_mut().set_stringn(
                content.x,
                content.y,
                recipe.id(),
                usize::from(content.width),
                system.style(Role::Text),
            );
        }
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `surface/ladder`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `surface/ladder` | Surface recipe ladder | **Primary (live preview)** |
| `surface/basic` | Surface raised | Baseline layout |
| `surface/focused` | Surface focused | focused |
| `surface/narrow` | Narrow Surface | Narrow terminal |
| `surface/terminal-default` | Surface terminal-default | terminal default |
| `surface/unicode` | Unicode Surface | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/surface.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/surface.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `surface/ladder`

## Seen in applications

<SeenInApplications component="Surface" />
