---
title: Picker
description: 'A filterable stable-ID list composition with caller-owned matching and ordering.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

## Live terminal (Ghostty-class)

Click the surface and use **↑↓** (or click) to drive the same TermRock paint path
exported from the lookbook. Truecolor cells, monospace metrics, Ghostty-style chrome.

<TerminalPreview story="picker/basic" interactive caption="Interactive TermRock frame pack — not a static mock." />


### `picker/basic`

Filterable picker

![Picker — Filterable picker](/component-previews/picker-basic.svg)

### `picker/empty`

Empty picker

![Picker — Empty picker](/component-previews/picker-empty.svg)

### `picker/narrow-unicode`

Narrow Unicode picker

![Picker — Narrow Unicode picker](/component-previews/picker-narrow-unicode.svg)

## Usage

```rust
use ratatui_core::text::Line;
use termrock::{style::DesignSystem, input::{KeyCode, KeyEvent, KeyModifiers}, widgets::{ListRow, Picker, PickerOutcome, PickerState, RowRole}};

let system = DesignSystem::default();
let candidates = [("open", "Open file"), ("logs", "Show logs")];
let project = |query: &str| candidates.iter()
    .filter(|(_, label)| label.to_lowercase().contains(&query.to_lowercase()))
    .map(|(id, label)| ListRow { id: *id, label: Line::from(*label), leading: None, secondary: None, badge: None, shortcut: None, trailing: None, role: RowRole::Item, enabled: true, loading: false })
    .collect::<Vec<_>>();
let mut state = PickerState::new(Some("open"));
let mut rows = project(state.query_text());
if matches!(state.handle_key(&rows, KeyEvent::new(KeyCode::Char('l'), KeyModifiers::NONE)), PickerOutcome::QueryChanged) {
    rows = project(state.query_text());
    state.reconcile(&rows);
}
let system = DesignSystem::default();
let picker = Picker::new(&rows, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `picker/basic` — Filterable picker
- `picker/empty` — Empty picker
- `picker/narrow-unicode` — Narrow Unicode picker
