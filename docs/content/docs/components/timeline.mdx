---
title: Timeline
description: 'Vertical activity timeline with active markers.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="timeline/basic" interactive caption="Timeline · Timeline — focus surface; use keys/click for states" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{Timeline, TimelineEvent}};

let system = DesignSystem::default();
let events = [TimelineEvent { when: "12:01", text: "Started", active: true }];
let timeline = Timeline::new(&events, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `timeline/basic` | Timeline | **Primary (live preview)** |
| `timeline/checkpoint` | Timeline checkpoint rows | checkpoint |
| `timeline/grouped` | Timeline grouped day | grouped |
| `timeline/narrow` | Narrow Timeline | Narrow terminal |
| `timeline/rail` | Timeline rail | rail |
| `timeline/streaming` | Timeline streaming | streaming |
| `timeline/unicode` | Unicode Timeline | Unicode / wide glyphs |
