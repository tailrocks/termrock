---
title: LogPane
description: 'A bounded, scrollable log buffer with freeze-on-scroll and tail following.'
component: LogPane
demo: log-pane/follow
interaction: scrolling-virtualization
actions:
  - "↑↓/PageUp/PageDown scroll"
  - "End follow tail"
  - "wheel scroll"
expectedOutcomes:
  - "Scrolling changes the mounted viewport while preserving selection and surrounding page position."
  - "Navigation moves visible focus or selection and preserves it for the next event."
source: crates/termrock/src/widgets/log_pane.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="log-pane/follow" interactive caption="LogPane · Following log pane — live shared Rust state" />

## Try it

- `↑↓/PageUp/PageDown scroll`: Scroll over the preview; its own viewport moves while the docs page stays put.
- `End follow tail`: Move through the real focus/selection model; the next action uses the new target.
- `wheel scroll`: Scroll over the preview; its own viewport moves while the docs page stays put.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `log-pane/follow` | Following log pane | interactive |
| `log-pane/narrow` | Narrow LogPane | paint-only |
| `log-pane/scrolled` | Frozen log scrollback | paint-only |
| `log-pane/unicode` | Unicode LogPane | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `log-pane/follow`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn log_pane(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    let mut state = LogPaneState::new().with_max_lines(200);
    for line in [
        "[12:04:01] resolving workspace",
        "[12:04:02] compiling termrock",
        "[12:04:03] running 205 tests",
        "[12:04:04] result: ok ✓",
    ] {
        state.append(line);
    }
    frame.render_stateful_widget(&LogPane::new(system).title("Build log"), area, &mut state);
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `log-pane/follow`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `log-pane/follow` | Following log pane | **Primary (live preview)** |
| `log-pane/narrow` | Narrow LogPane | Narrow terminal |
| `log-pane/scrolled` | Frozen log scrollback | scrolled |
| `log-pane/unicode` | Unicode LogPane | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/log_pane.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/log_pane.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `log-pane/follow`
