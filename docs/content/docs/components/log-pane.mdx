---
title: LogPane
description: A bounded, scrollable log buffer with freeze-on-scroll and tail following.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![LogPane — Following log pane](/component-previews/log-pane-follow.svg)

## Usage

```rust
use termrock::{
  Theme,
  ansi_text::line_from_ansi,
  style::Role,
  widgets::{LogPane, LogPaneState},
};

let theme = Theme::default();
let pane = LogPane::new(&theme).title("Build");
let mut state = LogPaneState::new().with_max_lines(1_000);
state.append(line_from_ansi("\u{1b}[32mready\u{1b}[0m", theme.style(Role::Text)));
state.scroll_by(-1);
state.follow();

// Unbounded retention is an explicit opt-in when the caller owns the policy.
let unbounded = LogPaneState::new().unbounded();
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | caller-owned |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `log-pane/follow` — Following log pane
- `log-pane/scrolled` — Frozen log scrollback
