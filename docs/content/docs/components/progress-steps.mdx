---
title: ProgressSteps
description: 'Pipeline/phase progress for builds and agent plans: queued→running→complete with waiting/skipped/warning/failed/retrying/cancelled; passive or interactive; narrow summary; Timeline/TaskRail projections.'
component: ProgressSteps
demo: progress-steps/pipeline
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/progress_steps.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="progress-steps/pipeline" interactive caption="ProgressSteps · ProgressSteps pipeline — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    ProgressSteps, ProgressStepsState, example_build_pipeline,
};

let system = DesignSystem::default();
let steps = example_build_pipeline();
let mut state = ProgressStepsState::new(); // or ::interactive()
ProgressSteps::new(&steps, &system)
    .title("Build")
    .paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `progress-steps/pipeline` | ProgressSteps pipeline | **Primary (live preview)** |
| `progress-steps/agent` | ProgressSteps agent plan | agent |
| `progress-steps/interactive` | ProgressSteps interactive | Interactive |
| `progress-steps/summary` | ProgressSteps narrow summary | summary |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `progress-steps/pipeline`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/progress_steps.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/progress_steps.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `progress-steps/pipeline`
