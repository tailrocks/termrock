---
title: ProgressSteps
description: 'Pipeline/phase progress for builds and agent plans: queued→running→complete with waiting/skipped/warning/failed/retrying/cancelled; passive or interactive; narrow summary; Timeline/TaskRail projections.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `progress-steps/pipeline`

ProgressSteps pipeline

![ProgressSteps — ProgressSteps pipeline](/component-previews/progress-steps-pipeline.svg)

### `progress-steps/agent`

ProgressSteps agent plan

![ProgressSteps — ProgressSteps agent plan](/component-previews/progress-steps-agent.svg)

### `progress-steps/interactive`

ProgressSteps interactive

![ProgressSteps — ProgressSteps interactive](/component-previews/progress-steps-interactive.svg)

### `progress-steps/summary`

ProgressSteps narrow summary

![ProgressSteps — ProgressSteps narrow summary](/component-previews/progress-steps-summary.svg)

## Usage

```rust
use termrock::style::DesignSystem;
use termrock::widgets::{
    ProgressSteps, ProgressStepsState, example_build_pipeline,
};

let system = DesignSystem::default();
let steps = example_build_pipeline();
let mut state = ProgressStepsState::new(); // or ::interactive()
ProgressSteps::new(&steps, &system)
    .title("Build")
    .paint(area, buf, &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `progress-steps/agent` — ProgressSteps agent plan
- `progress-steps/interactive` — ProgressSteps interactive
- `progress-steps/pipeline` — ProgressSteps pipeline
- `progress-steps/summary` — ProgressSteps narrow summary
