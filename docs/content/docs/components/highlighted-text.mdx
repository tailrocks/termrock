---
title: HighlightedText
description: "Substring matches with keep-first truncation."
component: HighlightedText
demo: highlighted-text/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/highlighted_text.rs
---

## Live terminal (Ghostty-class)

This preview is deterministic paint only. It traps no keyboard or pointer input; resize the page to inspect responsive rendering.

<TerminalPreview story="highlighted-text/basic" interactive caption="HighlightedText · HighlightedText matches — paint-only shared Rust frame" />

## Try it

- No component input. Resize the host and compare its narrow-terminal, Unicode, and non-color rendering contracts.

The docs page remains fully navigable because this demo does not invent focus, a cursor, or activation.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable in primary demo |
| Mouse | not-applicable in primary demo |
| Focus | not-applicable in primary demo |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `highlighted-text/basic` | HighlightedText matches | paint-only |
| `highlighted-text/narrow` | Narrow HighlightedText | paint-only |
| `highlighted-text/no-color` | HighlightedText no-color | paint-only |
| `highlighted-text/overlap` | HighlightedText overlaps | paint-only |
| `highlighted-text/selected` | HighlightedText selected | paint-only |
| `highlighted-text/unicode` | Unicode HighlightedText | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `highlighted-text/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn highlighted_text_basic_story(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::{MatchRanges, substring_ranges};
    let lines = [
        "src/widgets/command_palette.rs",
        "crates/termrock/src/widgets/picker.rs",
        "docs/design/component-prompt-library.md",
    ];
    let chunks = Layout::vertical([Constraint::Length(1); 3]).split(area);
    for (i, src) in lines.iter().enumerate() {
        let ranges = substring_ranges(src, "pal");
        if ranges.is_empty() {
            let ranges = substring_ranges(src, "widget");
            let _ = HighlightedText::prepared(src, ranges.as_slice(), system)
                .truncate(MatchTruncate::KeepFirstMatch)
                .paint(chunks[i], frame.buffer_mut());
        } else {
            let _ = HighlightedText::prepared(src, ranges.as_slice(), system)
                .truncate(MatchTruncate::KeepFirstMatch)
                .paint(chunks[i], frame.buffer_mut());
        }
        let _ = MatchRanges::new();
    }
}
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `highlighted-text/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `highlighted-text/basic` | HighlightedText matches | **Primary (live preview)** |
| `highlighted-text/narrow` | Narrow HighlightedText | Narrow terminal |
| `highlighted-text/no-color` | HighlightedText no-color | no color |
| `highlighted-text/overlap` | HighlightedText overlaps | overlap |
| `highlighted-text/selected` | HighlightedText selected | selected |
| `highlighted-text/unicode` | Unicode HighlightedText | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/highlighted_text.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/highlighted_text.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `highlighted-text/basic`
