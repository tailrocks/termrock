---
title: JumpOverlay
description: 'Letter-badge jump navigation over registered rectangles.'
component: JumpOverlay
demo: jump-overlay/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/jump_overlay.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="jump-overlay/basic" interactive caption="JumpOverlay · Jump overlay — persistent Rust preview; available actions appear below" />

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::{style::DesignSystem, widgets::{JumpOverlay, JumpOverlayState, JumpTarget}};

let system = DesignSystem::default();
let targets = [JumpTarget { id: "files", area: Rect::new(0, 0, 10, 1), badge: 'f' }];
let overlay = JumpOverlay::new(&targets, &system);
let mut state = JumpOverlayState::new();
state.open();
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `jump-overlay/basic` | Jump overlay | **Primary (live preview)** |
| `jump-overlay/narrow` | Narrow JumpOverlay | Narrow terminal |
| `jump-overlay/unicode` | Unicode JumpOverlay | Unicode / wide glyphs |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `jump-overlay/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/jump_overlay.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/jump_overlay.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `jump-overlay/basic`
