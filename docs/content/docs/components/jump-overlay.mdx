---
title: JumpOverlay
description: 'Letter-badge jump navigation over registered rectangles.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `jump-overlay/basic`

Jump overlay

![JumpOverlay — Jump overlay](/component-previews/jump-overlay-basic.svg)

### `jump-overlay/narrow`

Narrow JumpOverlay

![JumpOverlay — Narrow JumpOverlay](/component-previews/jump-overlay-narrow.svg)

### `jump-overlay/unicode`

Unicode JumpOverlay

![JumpOverlay — Unicode JumpOverlay](/component-previews/jump-overlay-unicode.svg)

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::{style::DesignSystem, widgets::{JumpOverlay, JumpOverlayState, JumpTarget}};

let system = DesignSystem::default();
let targets = [JumpTarget { id: "files", area: Rect::new(0, 0, 10, 1), badge: 'f' }];
let overlay = JumpOverlay::new(&targets, &system);
let mut state = JumpOverlayState::new();
state.open();
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | caller-owned |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `jump-overlay/basic` — Jump overlay
- `jump-overlay/narrow` — Narrow JumpOverlay
- `jump-overlay/unicode` — Unicode JumpOverlay
