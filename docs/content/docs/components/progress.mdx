---
title: Progress
description: 'Determinate/indeterminate progress with percentage, units, rate, ETA, phases, and status recipes for task/transfer models.'
---

## Previews

### `progress/determinate`

Progress

![Progress — determinate](/component-previews/progress-determinate.svg)

### `progress/detailed`

ProgressBar detailed

![Progress — detailed](/component-previews/progress-detailed.svg)

### `progress/multi-line`

ProgressBar multi-line

![Progress — multi-line](/component-previews/progress-multi-line.svg)

### `progress/failed`

ProgressBar failed

![Progress — failed](/component-previews/progress-failed.svg)

### `progress/narrow`

Narrow progress

![Progress — narrow](/component-previews/progress-narrow.svg)

### `progress/unicode`

Unicode progress labels

![Progress — unicode](/component-previews/progress-unicode.svg)

## Usage

```rust
use termrock::widgets::{Progress, ProgressBar, ProgressBarState, ProgressKind, ProgressRecipe};

Progress::new(ProgressKind::Determinate { fraction: 0.72 }, &system)
    .label("Indexing")
    .paint(area, buf);

let mut state = ProgressBarState::transfer(12_000_000, 30_000_000);
state.set_label("Download");
state.set_rate(Some(2_000_000.0));
state.recompute_eta();
state.set_recipe(ProgressRecipe::Detailed);
ProgressBar::paint_state(&system, area, buf, &mut state, tick, motion);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | covered (never focusable) |
| Non-color cues | covered (glyphs + %) |
| Unicode | covered |
| Narrow terminal | covered (% elided under 16 cols) |

## Stories

- `progress/determinate`, `detailed`, `multi-line`, `failed`, `narrow`, `unicode`
