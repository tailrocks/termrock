---
title: Progress
description: A deterministic determinate bar or caller-ticked indeterminate indicator.
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

![Progress — Progress](/component-previews/progress-determinate.svg)

## Usage

```rust
use ratatui_core::{buffer::Buffer, layout::Rect, widgets::Widget};
use termrock::{Theme, widgets::{Progress, ProgressKind}};

let theme = Theme::default();
let progress = Progress::new(ProgressKind::Determinate { fraction: 0.72 }, &theme)
    .label("Indexing");
let area = Rect::new(0, 0, 40, 1);
progress.render(area, &mut Buffer::empty(area));

let frames = ["|", "/", "-", "\\"];
let spinner = Progress::new(ProgressKind::Indeterminate { tick: 3 }, &theme)
    .frames(&frames)
    .label("Waiting");
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `progress/determinate` — Progress
- `progress/narrow` — Narrow progress
- `progress/unicode` — Unicode progress labels
