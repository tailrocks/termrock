---
title: SplitPane
description: 'A resizable two-pane layout with bounded ratios and collapse support.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Live terminal (Ghostty-class)

One focused interactive surface — the same TermRock paint path as the lookbook.
**States, narrow, unicode, and transitions live in this preview** (↑↓ / ←→, j/k,
click, Home/End; host resize remaps cols×rows). Other lookbook stories are listed
in the table below — not as extra screens.

<TerminalPreview story="split-pane/horizontal" interactive caption="SplitPane · Horizontal split pane — focus surface; use keys/click for states" />

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::{style::DesignSystem, widgets::{SplitDirection, SplitPane, SplitPaneState, SplitRatio}};

let system = DesignSystem::default();
let pane = SplitPane::new(SplitDirection::Horizontal, 20, 20, &system);
let mut state = SplitPaneState::new(SplitRatio::from_percent(40));
let layout = pane.layout(Rect::new(0, 0, 100, 24), &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `split-pane/horizontal` | Horizontal split pane | **Primary (live preview)** |
| `split-pane/narrow` | Narrow SplitPane | Narrow terminal |
| `split-pane/unicode` | Unicode SplitPane | Unicode / wide glyphs |
