---
title: SplitPane
description: 'A resizable two-pane layout with bounded ratios and collapse support.'
component: SplitPane
demo: split-pane/horizontal
interaction: drag-continuous-value
actions:
  - '←→ resize'
  - 'Home/End collapse'
  - 'drag divider'
expectedOutcomes:
  - 'The preview status reports the typed Rust outcome after each accepted action.'
source: crates/termrock/src/widgets/split_pane.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="split-pane/horizontal" interactive caption="SplitPane · Horizontal split pane — persistent Rust preview; available actions appear below" />

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::{style::DesignSystem, widgets::{SplitDirection, SplitPane, SplitPaneState, SplitRatio}};

let system = DesignSystem::default();
let pane = SplitPane::new(SplitDirection::Horizontal, 20, 20, &system);
let mut state = SplitPaneState::new(SplitRatio::from_percent(40));
let layout = pane.layout(Rect::new(0, 0, 100, 24), &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `split-pane/horizontal` | Horizontal split pane | **Primary (live preview)** |
| `split-pane/narrow` | Narrow SplitPane | Narrow terminal |
| `split-pane/unicode` | Unicode SplitPane | Unicode / wide glyphs |


## Try it

- `←→ resize`: dispatches through the public TermRock interaction API.
- `drag divider`: dispatches through the public TermRock interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo owns ephemeral widget state between events. The widget owns painted geometry, focus/selection mechanics, and typed outcomes. The host owns domain effects and decides what an emitted outcome means to the application.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `split-pane/horizontal`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/split_pane.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/split_pane.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `split-pane/horizontal`
