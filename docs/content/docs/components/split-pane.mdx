---
title: SplitPane
description: 'A resizable two-pane layout with bounded ratios and collapse support.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `split-pane/horizontal`

Horizontal split pane

![SplitPane — Horizontal split pane](/component-previews/split-pane-horizontal.svg)

### `split-pane/narrow`

Narrow SplitPane

![SplitPane — Narrow SplitPane](/component-previews/split-pane-narrow.svg)

### `split-pane/unicode`

Unicode SplitPane

![SplitPane — Unicode SplitPane](/component-previews/split-pane-unicode.svg)

## Usage

```rust
use ratatui_core::layout::Rect;
use termrock::{style::DesignSystem, widgets::{SplitDirection, SplitPane, SplitPaneState, SplitRatio}};

let system = DesignSystem::default();
let pane = SplitPane::new(SplitDirection::Horizontal, 20, 20, &system);
let mut state = SplitPaneState::new(SplitRatio::from_percent(40));
let layout = pane.layout(Rect::new(0, 0, 100, 24), &mut state);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `split-pane/horizontal` — Horizontal split pane
- `split-pane/narrow` — Narrow SplitPane
- `split-pane/unicode` — Unicode SplitPane
