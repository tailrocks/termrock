---
title: ImageSurface
description: 'A framed image slot with protocol labels; pixels stay caller-owned.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `image-surface/basic`

Image surface

![ImageSurface — Image surface](/component-previews/image-surface-basic.svg)

### `image-surface/narrow`

Narrow ImageSurface

![ImageSurface — Narrow ImageSurface](/component-previews/image-surface-narrow.svg)

### `image-surface/unicode`

Unicode ImageSurface

![ImageSurface — Unicode ImageSurface](/component-previews/image-surface-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::{ImageMeta, ImageProtocol, ImageSurface}};

let theme = Theme::default();
let meta = ImageMeta { label: "shot.png", pixel_width: Some(64), pixel_height: Some(64), protocol: ImageProtocol::Placeholder, pending: false, stale: false, generation: 0 };
let surface = ImageSurface::new(meta, &theme);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `image-surface/basic` — Image surface
- `image-surface/narrow` — Narrow ImageSurface
- `image-surface/unicode` — Unicode ImageSurface
