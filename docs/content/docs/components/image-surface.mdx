---
title: ImageSurface
description: 'A framed image slot with protocol labels; pixels stay caller-owned.'
component: ImageSurface
demo: image-surface/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/image_surface.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="image-surface/basic" interactive caption="ImageSurface · Image surface — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{ImageMeta, ImageProtocol, ImageSurface}};

let system = DesignSystem::default();
let meta = ImageMeta { label: "shot.png", pixel_width: Some(64), pixel_height: Some(64), protocol: ImageProtocol::Placeholder, pending: false, stale: false, generation: 0 };
let surface = ImageSurface::new(meta, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | not-applicable |
| Mouse | not-applicable |
| Focus | not-applicable |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `image-surface/basic` | Image surface | **Primary (live preview)** |
| `image-surface/narrow` | Narrow ImageSurface | Narrow terminal |
| `image-surface/unicode` | Unicode ImageSurface | Unicode / wide glyphs |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `image-surface/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/image_surface.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/image_surface.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `image-surface/basic`
