---
title: MarkdownView
description: 'Projected markdown-like blocks with semantic roles.'
component: MarkdownView
demo: markdown-view/basic
interaction: scrolling-virtualization
actions:
  - "↑↓ scroll"
  - "Tab choose link"
  - "Enter open link"
  - "wheel scroll"
  - "click link"
expectedOutcomes:
  - "Scrolling changes the mounted viewport while preserving selection and surrounding page position."
  - "Activation changes the mounted state and exposes the public typed request in the preview status."
source: crates/termrock/src/widgets/markdown.rs
---

## Live terminal (Ghostty-class)

This preview mounts one persistent Rust state machine. Focus it, use the current actions shown by the runtime, inspect the typed outcome, and use **Reset** for fresh state.

<TerminalPreview story="markdown-view/basic" interactive caption="MarkdownView · Markdown view — live shared Rust state" />

## Try it

- `↑↓ scroll`: Scroll over the preview; its own viewport moves while the docs page stays put.
- `Tab choose link`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `Enter open link`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.
- `wheel scroll`: Scroll over the preview; its own viewport moves while the docs page stays put.
- `click link`: Activate the focused or pointed target; inspect the changed cells and typed outcome below the terminal.

Every accepted action reaches the same mounted Rust value. The status line shows the latest typed outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The mounted widget owns interaction state, focus chrome, hit geometry, and typed outcomes. The host only forwards backend-neutral events and performs no external effects.

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Cursor / caret | not-applicable in primary demo |
| Narrow terminal | covered |
| Unicode | covered |
| Non-color cues | covered |
| Motion / time | not-applicable in primary demo |

## Configuration and variants

Choose **Variant** above the preview. A variant is a separate configuration and remounts fresh state; it is never presented as an interaction step.

| Demo | Configuration | Input |
|---|---|---|
| `markdown-view/basic` | Markdown view | interactive |
| `markdown-view/in-app` | In application | interactive |
| `markdown-view/narrow` | Narrow MarkdownView | paint-only |
| `markdown-view/no-color` | Markdown no-color | paint-only |
| `markdown-view/streaming` | Markdown streaming fence | paint-only |
| `markdown-view/table` | Markdown responsive table | paint-only |
| `markdown-view/unicode` | Unicode MarkdownView | paint-only |

## Usage

The Code view above and this source excerpt use the exact paint setup registered for `markdown-view/basic`. Interaction state is long-lived in the catalog interactor; the host only forwards backend-neutral events.

```rust
use ratatui::prelude::*;
use termrock::{input::*, interaction::*, runtime::*, style::*, widgets::*};

// Exact paint setup used by the shared catalog story.
fn markdown_view(frame: &mut Frame<'_>, area: Rect, system: &DesignSystem) {
    use termrock::widgets::project_markdown;
    let src = "\
# Plan

Implement the **markdown** surface with [links](https://example.invalid).

- [ ] tasks remain open
- [x] done items check

1. First ordered
2. Second ordered

> Quote with hanging indent.

```rust
```

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `markdown-view/basic`, dispatch each listed action, and assert the typed outcome, changed Ratatui cells, persistent follow-up state, pointer hit geometry, and Reset behavior.

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `markdown-view/basic` | Markdown view | **Primary (live preview)** |
| `markdown-view/narrow` | Narrow MarkdownView | Narrow terminal |
| `markdown-view/no-color` | Markdown no-color | no color |
| `markdown-view/streaming` | Markdown streaming fence | streaming |
| `markdown-view/table` | Markdown responsive table | table |
| `markdown-view/unicode` | Unicode MarkdownView | Unicode / wide glyphs |

## Source and related material

- Rust source: [`crates/termrock/src/widgets/markdown.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/markdown.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `markdown-view/basic`

## Seen in applications

<SeenInApplications component="MarkdownView" />
