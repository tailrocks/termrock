---
title: MarkdownView
description: 'Projected markdown-like blocks with semantic roles.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `markdown-view/basic`

Markdown view

![MarkdownView — Markdown view](/component-previews/markdown-view-basic.svg)

### `markdown-view/narrow`

Narrow MarkdownView

![MarkdownView — Narrow MarkdownView](/component-previews/markdown-view-narrow.svg)

### `markdown-view/no-color`

Markdown no-color

![MarkdownView — Markdown no-color](/component-previews/markdown-view-no-color.svg)

### `markdown-view/streaming`

Markdown streaming fence

![MarkdownView — Markdown streaming fence](/component-previews/markdown-view-streaming.svg)

### `markdown-view/table`

Markdown responsive table

![MarkdownView — Markdown responsive table](/component-previews/markdown-view-table.svg)

### `markdown-view/unicode`

Unicode MarkdownView

![MarkdownView — Unicode MarkdownView](/component-previews/markdown-view-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::{MarkdownBlock, MarkdownBlockKind, MarkdownView}};

let theme = Theme::default();
let blocks = [MarkdownBlock { kind: MarkdownBlockKind::Heading, text: "Plan" }];
let view = MarkdownView::new(&blocks, &theme);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `markdown-view/basic` — Markdown view
- `markdown-view/narrow` — Narrow MarkdownView
- `markdown-view/no-color` — Markdown no-color
- `markdown-view/streaming` — Markdown streaming fence
- `markdown-view/table` — Markdown responsive table
- `markdown-view/unicode` — Unicode MarkdownView
