---
title: MarkdownView
description: 'Projected markdown-like blocks with semantic roles.'
component: MarkdownView
demo: markdown-view/basic
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/markdown.rs
---


## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="markdown-view/basic" interactive caption="MarkdownView · Markdown view — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::{style::DesignSystem, widgets::{MarkdownBlock, MarkdownBlockKind, MarkdownView}};

let system = DesignSystem::default();
let blocks = [MarkdownBlock { kind: MarkdownBlockKind::Heading, text: "Plan" }];
let view = MarkdownView::new(&blocks, &system);
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `markdown-view/basic` | Markdown view | **Primary (live preview)** |
| `markdown-view/narrow` | Narrow MarkdownView | Narrow terminal |
| `markdown-view/no-color` | Markdown no-color | no color |
| `markdown-view/streaming` | Markdown streaming fence | streaming |
| `markdown-view/table` | Markdown responsive table | table |
| `markdown-view/unicode` | Unicode MarkdownView | Unicode / wide glyphs |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `markdown-view/basic`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/markdown.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/markdown.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `markdown-view/basic`
