---
title: DiffView
description: 'A vertically scrollable, syntax-neutral presentation of projected diff lines.'
---

{/* Generated by scripts/gen-component-pages.ts. Edit component-docs.ts instead. */}

## Previews

Lookbook-rendered SVGs from the same path as the TUI CLI gallery
(`termrock-lookbook render`). These match deterministic terminal paint, not hand-drawn mockups.

### `diff/basic`

Diff view

![DiffView — Diff view](/component-previews/diff-basic.svg)

### `diff/narrow`

Narrow DiffView

![DiffView — Narrow DiffView](/component-previews/diff-narrow.svg)

### `diff/unicode`

Unicode DiffView

![DiffView — Unicode DiffView](/component-previews/diff-unicode.svg)

## Usage

```rust
use termrock::{Theme, widgets::{DiffKind, DiffLine, DiffState, DiffView}};

let theme = Theme::default();
let lines = [DiffLine { text: "+added", kind: DiffKind::Added }];
let diff = DiffView::new(&lines, &theme);
let mut state = DiffState::default();
state.offset = 1;
let _visible_offset = state.offset;
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | caller-owned |
| Mouse | caller-owned |
| Focus | caller-owned |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

- `diff/basic` — Diff view
- `diff/narrow` — Narrow DiffView
- `diff/unicode` — Unicode DiffView
