---
title: Toggle
description: 'Toggle widget.'
component: Toggle
demo: toggle/pressed
interaction: activation
actions:
  - "Space/Enter toggle"
  - "click toggle"
  - "move pointer to hover"
expectedOutcomes:
  - The typed Rust outcome and Ratatui cells change after an accepted action.
source: crates/termrock/src/widgets/toggle.rs
---


## Live terminal (Ghostty-class)

This preview is a persistent Rust demo. Focus it, then use the exact actions shown below; Reset remounts fresh state.

<TerminalPreview story="toggle/pressed" interactive caption="Toggle · Toggle pressed — persistent Rust preview; available actions appear below" />

## Usage

```rust
use termrock::widgets::Toggle;

// See handbook / lookbook story toggle/basic.
```

## Interaction contract

| Axis | Status |
|---|---|
| Keyboard | covered |
| Mouse | covered |
| Focus | covered |
| Non-color cues | covered |
| Unicode | covered |
| Narrow terminal | covered |

## Stories

Lookbook coverage for this component. Only the **primary** story is embedded above;
use the interactive preview (and Studio lookbook) to explore transitions instead of
a static multi-image gallery.

| Story | Title | Focus |
|---|---|---|
| `toggle/pressed` | Toggle pressed | **Primary (live preview)** |
| `toggle/icon` | Toggle icon-only | icon |
| `toggle/indeterminate` | Toggle indeterminate | indeterminate |
| `toggle/narrow` | Narrow Toggle | Narrow terminal |
| `toggle/unicode` | Unicode Toggle | Unicode / wide glyphs |


## Try it

- `Space/Enter toggle`: routes through the component public interaction API.
- `click toggle`: routes through the component public interaction API.
- `move pointer to hover`: routes through the component public interaction API.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The demo preserves widget state between events. The component owns hit geometry, focus/selection mechanics, and typed outcomes; the host applies domain effects.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `toggle/pressed`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Unsupported input remains ignored without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/toggle.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/toggle.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `toggle/pressed`
