---
title: Sheet
description: 'Bottom-edge drawer (shadcn Sheet metaphor) — same engine as Drawer with DrawerEdge::Bottom default.'
component: Sheet
demo: drawer/sheet
interaction: passive-paint
actions: []
expectedOutcomes: []
source: crates/termrock/src/widgets/drawer.rs
---

## Live terminal (Ghostty-class)

This is an honest paint-only preview. It accepts no keyboard or pointer input; resize the page to inspect its responsive rendering.

<TerminalPreview story="drawer/sheet" interactive caption="Sheet · shared Rust demo" />

## Usage

```rust
use termrock::widgets::{Sheet, DrawerState};

let mut state = DrawerState::sheet();
state.open();
Sheet::new("Actions", &system).paint(area, buf, &mut state);
```

## Interaction contract

Same as [Drawer](/docs/components/drawer) with bottom-edge placement.

## Stories

| Story | Title | Focus |
|---|---|---|
| `drawer/sheet` | Sheet bottom | **Primary (live preview)** |


## Try it

- No actions. This component is passive in this demo; the page remains navigable.

Accepted actions update the same mounted Rust value. The status line shows the latest outcome; **Reset** creates a fresh instance.

## State and typed outcomes

The widget owns deterministic paint and layout. The host owns all surrounding state and effects; this demo intentionally exposes no fake cursor, click, or keyboard behavior.

## Common mistakes

- Do not treat separate Lookbook stories as runtime states or a slide sequence.
- Do not duplicate hit testing, focus, or keyboard rules in the host.
- Do not infer an action from visual styling; handle the widget's typed outcome.

## Test recipe

Mount `drawer/sheet`, dispatch only the actions listed above, and assert both the returned outcome and changed Ratatui cells. Passive demos must reject input without trapping page navigation.

## Source and related material

- Rust source: [`crates/termrock/src/widgets/drawer.rs`](https://github.com/tailrocks/termrock/blob/main/crates/termrock/src/widgets/drawer.rs)
- Native preview: `cargo run -p termrock-lookbook -- terminal`
- Demo id: `drawer/sheet`
